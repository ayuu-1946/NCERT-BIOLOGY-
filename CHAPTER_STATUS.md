# Chapter Production Status

Tracking every chapter against the **v6 gated pass workflow** defined in `SUPREME COMMAND PROMPT.md`.

**Pipeline:** `Pass 1 → [Gate 1] → Pass 2 → [Gate 2: check_pdf.py green] → Pass 3 → [Gate 3: zero confirmed defects] → deliver`

**Gate legend**
- **Gate 1** — Frozen, numbered inventory + figure manifest + figure-label matrix complete (Pass 1).
- **Gate 2** — `check_pdf.py` exits 0 (no FAILs); any WARN eyeballed and confirmed benign.
- **Gate 3** — Zero confirmed defects remain AND `check_pdf.py` still green; full chapter folder delivered.

**Deliverables per chapter (§0.5):** `<Chapter>.pdf` · `<Chapter>.py` · `<Chapter>_inventory.md` (with figure-label matrix) · `assets/`. Shared repo-level files: `neet_template.py`, `check_pdf.py`.

**Status legend:** ✅ complete · ⚠️ green but not independently re-verified this cycle · ⬜ not started · ▶️ in progress

---

## Overview table

| # | Chapter | Class | Gate 1 (inventory) | Gate 2 (`check_pdf.py`) | Gate 3 (defects) | Deliverables | Overall |
|---|---------|-------|--------------------|-------------------------|------------------|--------------|---------|
| 1 | The Living World | 11 | ✅ 121 facts frozen · 14/14 labels · 3 summary-unique folded | ✅ WARN (0 fail, **1 benign warn**) · 7 pp · 1 mono img | ✅ zero confirmed defects | ✅ pdf · py · inventory · assets | **✅ FULLY COMPLETE — CLOSED** |
| 2 | Biological Classification | 11 | ✅ 192 facts frozen · 26/26 labels · 2 summary-unique folded | ✅ WARN (0 fail, **1 benign warn**) · 15 pp · 6 mono imgs | ✅ zero confirmed defects (2 found + fixed) | ✅ pdf · py · inventory · assets | **✅ FULLY COMPLETE — CLOSED** |
| 3 | Plant Kingdom | 11 | ✅ **PASSED** — 215 facts frozen · 35/35 genuine labels (30 distinct, 11 rows) · 6 summary-unique folded · 11/11 mono assets | ✅ WARN (0 fail, **1 inspected benign warn**) · **10 pp** · 11 mono imgs · re-run confirmed | ✅ zero confirmed defects (215/215 covered) | ✅ pdf · py · inventory · assets | **✅ FULLY COMPLETE — CLOSED** |
| 8 | Cell: The Unit of Life | 11 | ✅ 325 facts frozen · 82/82 labels · 6 summary-unique folded | ✅ WARN (0 fail, **1 benign warn**) · 18 pp · 14 mono imgs | ✅ zero confirmed defects | ✅ pdf · py · inventory · assets | **✅ FULLY COMPLETE — CLOSED** |
| 9 | Biomolecules | 11 | ✅ **PASSED** — **280 facts** (277 frozen F001-F277 + F194a/F221a/F225a added at Pass 3(b)) · 15 label rows / 49 in-figure labels (48 distinct) · 13 summary-unique folded · 15/15 mono assets | ✅ **PASS (0 fail, 0 warn under `--strict`, exit 0)** · **15 pp** · 15 mono imgs · 280/280 rows ticked · rebuild text-identical | ✅ **PASS — full-read Pass 3(b): 3 confirmed defects (D9 MISSING, D4 MISSING ×2, D6 DRIFTED) → all fixed** · 280/280 verified present in PDF · 15/15 pages inspected · Gate 2 re-confirmed green after rebuild | ✅ pdf · py · inventory · assets | **✅ FULLY COMPLETE — CLOSED (Gate 3 earned on full-read evidence)** |
| 10 | Cell Cycle and Cell Division | 11 | ✅ **PASSED** — 167 facts frozen (F001-F167) · 8 label rows / 24 in-figure labels · 11 summary-unique folded · 8/8 mono assets | ✅ **PASS (0 fail, 0 warn — also green under `--strict`)** · **11 pp** · 8 mono imgs | ✅ **re-run full read: 3 defects found (2 DRIFTED + 1 MISSING) → all fixed** · now 175/175 covered · Gate 2 re-confirmed green | ✅ pdf · py · inventory · assets | **✅ FULLY COMPLETE — CLOSED (Gate 3 re-earned)** |
| 14 | Breathing and Exchange of Gases | 11 | ✅ **PASS 1 COMPLETE — GATE 1 CLOSED (2026-08-31).** Normal 3-pass protocol · inventory **FROZEN at 139 rows, `F001`–`F139`, contiguous, 0 gaps, 0 duplicate IDs, 0 ticked** = 107 Facts (`1-S`) + 14 heading (`1-H`) + 12 opener (`1-O`) + 0 summary-unique (`1-Z`) + 6 figure-label matrix (`1-F`) = **139**, equal to the highest ID · type census (10 values, all lowercase, machine-derived): `concept` 47 · `definition` 27 · `number` 18 · `heading` 14 · `opener` 12 · `process` 9 · `figure-label` 6 · `example` 3 · `name` 2 · `list` 1 = 139 · heading census **10 numbered + 4 unnumbered = 14** · openers **12** · summary classification **18 = 18 BODY-PRESENT + 0 SUMMARY-UNIQUE** (nothing to fold) · exercise-gap scan **14 exercises / 3 gaps** (hypoxia, high-altitude respiration, sigmoid-curve reason), each with a planned home · **figures extracted in a prior task: 5 numbered figures → 6 assets** (`14.2` split a/b), all 300 dpi `mode=L`, individually verified · **6 label rows; `_extract_labels` (imported from `check_pdf.py`) returns 47 labels / 6 figure rows / no doubling / no phantom `Fig #` row** | ⬜ **NOT STARTED** — no script, no PDF; `check_pdf.py` cannot run | ⬜ **NOT STARTED** | ▶️ frozen inventory · figure-label matrix · 6 assets | **▶️ IN PROGRESS — Pass 1 complete, Gate 1 CLOSED; Gates 2 + 3 not started. Excluded from the Done tally.** |
| 5 | Molecular Basis of Inheritance | 12 | ✅ **PASS 1 COMPLETE — GATE 1 CLOSED (2026-08-22).** Big-chapter 5-pass protocol (31 pp) · **646 frozen Facts rows, `F001`–`F646`, contiguous, 0 gaps, 0 duplicate IDs, monotonic, 0 ticked** · type census: `concept` 264 · `figure-label` 136 · `definition` 56 · `number` 37 · `list` 33 · `question` 31 · `heading` 30 · `opener` 27 · `name` 18 · `example` 13 · `table` 1 = 646 · all six text sweeps complete · summary classification 33 = 29 BODY-PRESENT + 4 SUMMARY-UNIQUE · exercise-gap scan **17 rows / 5 genuine gaps** · **16 numbered figures / 17 assets** including split `5.4a`+`5.4b`, all 300 dpi, true mono and individually opened/verified · **136 label rows; `_extract_labels` returns 136 labels / 15 label-bearing figures / no doubling / no phantom `Fig #`; `5.4b` and `5.15` genuinely label-free** · required `Central dogma` and `anticodon loop` present | ✅ **PASS 2 COMPLETE — GATE 2 CLOSED** · script + **30-page PDF** · **646/646 rows ticked** · `check_pdf.py --strict` **PASS 0 fail / 0 warn** · 17 embedded mono images · 136/136 labels in text | ▶️ **Gate 3a COMPLETE, Gate 3b NOT STARTED** — 30/30 pages + 18/18 assets walked; badges **FIXED** (`5.2a`/`5.2b`/`5.9a`, call sites only, template untouched); Quick Recap bullets **FIXED** (12 inline `&bull;` re-hung as `<bullet>`; the inherited "21 literal glyphs" claim re-derived as **false** — 0 typed U+2022); watermark + baked-in double borders (`5.4b`/`5.6`/`5.7`/`5.9`) **ACCEPTED — do not re-raise**; strict re-run after fixes still **PASS 0/0** | ✅ frozen inventory · tracker · 18 assets · script · PDF | **▶️ IN PROGRESS — Gates 1 + 2 CLOSED, Gate 3a closed, Gate 3b outstanding** |
| 6 | Evolution | 12 | ▶️ **GATE 1 OPEN — only session `1-F` (figures) has run (2026-08-26).** Normal 3-pass protocol (17 pp) · inventory is a **working file, NOT frozen**: it holds **13 figure-label rows and nothing else** · `1-S`, `1-H`, `1-O`, `1-Z` **not started**, so there is no Facts table, no heading/opener census and no IDs (matrix IDs are `—`, to be assigned by `1-Z`) · **0 ticked, by design** · **11 numbered figures → 13 assets** (`6.3` and `6.4` each split a/b; `6.8` deliberately kept as ONE asset because its three panels share a frame and the arrows carry the meaning) · **no unnumbered bonus plate**, census taken from all 17 page images · **no scientist portrait anywhere in the chapter**, so §4.4's photograph hard-no has no manifest row · all 13 assets 300 dpi, `mode=L`, **each opened at full size individually** · **13 label rows; `_extract_labels` (imported from `check_pdf.py`) returns 116 labels / 9 label-bearing figures / no doubling / no phantom `Fig #`** — the 4 unlabelled rows contribute 0 **by design** · **the source is a 100% scanned raster** (0 text-layer words, 0 drawings on all 17 pages), so skill audit checks A and B are **vacuous** and a raster gate (`audit_figures.py`: A' interior ink slack + B' edge-band probe, `BAND_TOL` 12 not 40) carries the numeric side — **GATE PASSED, 13/13, all edge ink named** | ⬜ **NOT STARTED** — no script, no PDF; `check_pdf.py` cannot run | ⬜ **NOT STARTED** | ▶️ 13 assets · `extract_figures.py` · `audit_figures.py` · working inventory (`1-F` only) | **▶️ IN PROGRESS — Pass 1 partial (1 of 5 sessions). Gate 1 OPEN. Must not be counted in any completion tally.** |
| 7 | Human Health and Disease | 12 | ✅ **PASS 1 COMPLETE — GATE 1 CLOSED (2026-08-23).** Normal 3-pass protocol (22 pp) · **346 frozen rows, `F001`–`F346`, contiguous, 0 gaps, 0 duplicate IDs, 0 ticked** · blocks: 276 Facts (`1-S`) + 29 heading (`1-H`) + 28 opener (`1-O`) + 2 summary-unique (`1-Z`) + 11 label matrix (`1-F`) = **346**, equal to the highest ID · type census (17 values, **all lowercase**): `fact` 86 · `term` 46 · `heading` 29 · `opener` 28 · `example` 24 · `prevention` 19 · `figure-text` 18 · `mechanism` 14 · `symptom` 14 · `label` 11 · `process` 11 · `diagnosis` 10 · `cause` 9 · `transmission` 9 · `number` 7 · `treatment` 7 · `structure` 4 = 346 · all five mandatory sessions ran with their own machine-derived counts · headings **16 numbered + 13 unnumbered = 29** (includes the four innate-barrier run-in heads the source sets in mixed italic/bold-italic — the Ch9 D4 profile) · openers **28** (`29 - 2 + 1`: title plate and EXERCISES have no opening sentence; the unheaded chapter-intro prose gets `F306`) · summary classification **18 = 16 BODY-PRESENT + 2 SUMMARY-UNIQUE** (`F334` "psychological" qualifier, `F335` "cholera" — both folded pre-freeze) · exercise-gap scan **17 exercises / 2 gaps**, both with a planned home · **11 numbered figures / 11 assets**, all 300 dpi, `mode=L`, individually opened at full size · **11 label rows; `_extract_labels` (imported from `check_pdf.py`) returns 21 labels / 4 labelled figures / no doubling / no phantom `Fig #`** — the 7 unlabelled rows contribute 0 **by design**, since a text sweep returns **0** labels for this chapter (every callout is artwork) | ✅ **PASS 2 COMPLETE — GATE 2 CLOSED (2026-08-23)** · script **1527 lines** · **20-page PDF** · `check_pdf.py` **exit 0 (0 fail, 1 inspected benign warn)** · **335/335 Facts rows ticked** — the 11 matrix rows sit outside check 7's `## Facts` scope **by design** and are gated by check 6 instead (**21/21 labels in running text, 0 partial, 0 missing**); `335 + 11 = 346` · 11 embedded mono imgs · smallest text **6.0pt** · 20/20 pages A4 · checks 1,2,3,5,6,7,8,9,10 green · rebuild reproducible (identical text SHA, 20 pp / 51,556 chars / 11 imgs twice). **`x` is the repo-wide ticked marker** — re-derived across all six delivered chapters (175/196/196/200 rows), so `check_pdf.py` line 462 is correct and was **NOT** patched. *(Superseded text, kept as history per §7: this cell read "**NOT STARTED** — no script, no PDF; `check_pdf.py` cannot run yet. **0/346 rows ticked**". Those claims were already false when read — a later PR had written the full script and PDF.)* | ⬜ **NOT STARTED — now UNBLOCKED** (Gate 2 green). Requires 3(a) **20/20** pages (**only 2/20 viewed**: pp. 6, 11) and 3(b) the **bidirectional** full read — especially direction 2 (source → inventory), the Ch9/Ch13 failure mode. The Pass 2 token screen is **evidence only and may clear no row** (§6 hard bar) | ✅ frozen inventory · 11 assets · `extract_figures.py` · **script · 20-page PDF** | **▶️ IN PROGRESS — Gates 1 + 2 CLOSED; Gate 3 OPEN. NOT done; must not be counted in any completion tally.** |
| 8 | **Microbes in Human Welfare** (note: **Class 12**, not the Class 11 Ch8 *Cell: The Unit of Life* two rows up) | 12 | ✅ **PASS 1 COMPLETE — GATE 1 GREEN (machine re-verified from disk in a later session).** 12 source pages, 6 numbered sections (§8.1–§8.6) + 3 sub-sections, single-pass protocol · inventory **FROZEN at 207 rows, `F001`–`F207`, contiguous, 0 duplicates, 0 ticked** = **198 Facts + 9 figure-label matrix rows** *(later grown to **209 rows / 200 Facts** by the two Pass 3(b) additions `F055a`/`F085a` — logged as a real Pass 1 gap, **never back-dated into the freeze**)* · type census: `fact` 96 · `name` 29 · `term` 19 · `heading` 15 · `opener` 14 · `label` 9 · `caption` 8 · `crossref` 6 · `question` 6 · `number` 5 = 207, all lowercase, no strays · heading census **15 = 9 numbered + 6 unnumbered** (the 9 in-body banners re-derived independently from the source PDF and matching) · opener census **14** · `_extract_labels` returns **17 labels across 5 parsed figure rows** — 9 matrix rows, the four `No in-figure labels` rows (`F203`–`F206`) contributing 0 **by design** — **no doubling, no phantom `Fig #` row** · summary classification 18 = 16 BODY-PRESENT + 2 SUMMARY-UNIQUE (both folded into `F195`/`F196`) · exercise-gap scan **4 terms, each with a planned home** · **8 numbered figures → 9 assets** (Figure 8.2 split into `8_2a` panels a+b and `8_2c`; **0 unnumbered plates**, so the denominator is 9 everywhere) · **9/9 `mode=L`** at 300 dpi, re-confirmed on disk · rects **hand-pinned** off 110 dpi grid overlays and cross-checked against page geometry, each carrying a comment recording what pinned it · **four-part crop gate passed** — A word-grazing, B drawings-extent, **B2 raster-extent (added this chapter)**, C border ink · all five sessions ran with machine-derived counts (`1-F`/`1-S`/`1-H`/`1-O`/`1-Z`) · re-validation fixed one **count restatement** (the inventory twice described the parse as "9 figure rows" where the parser returns 5); **no frozen row touched, no count changed** | ✅ **PASS 2 COMPLETE — GATE 2 GREEN (2026-08-23), exit 0 (0 fail, 1 inspected WARN)** · script written **linearly from the frozen inventory** in Content Order, rows ticked as written · **12 pp A4 portrait (595x842pt)** · **9 embedded images, 9/9 monochrome** · **200/200 Facts rows ticked** (198/198 when Gate 2 first closed; re-derived as 200/200 from disk after the Pass 3 fixes, with `check_pdf.py` re-run and still exit 0) · **17/17 in-figure labels present in running text** (0 partial, 0 missing — all transcribed from `F199`–`F207`, since the source PDF has them as artwork) · smallest rendered text **6.0pt** · **no banned glyphs** (`CO2`/`CH4`/`H2`/`B12`/`100 C` all plain ASCII as the inventory rows specify) · no footer/header band text · **88 badge plates** clear of their banners · **37 banner headings** all followed by content on their own page · the lone WARN is **check 4's portrait heuristic**, firing on the literal word "photograph" in `F050` and `F203`–`F206` plus the four `raster photograph` manifest rows — **all four plates opened and inspected: 8.4 (fermentor vessels), 8.6 (aeration tank) and 8.7 (aerial sewage plant) contain no people whatsoever**; **8.5 is an industrial fermentation-plant photograph containing two incidental workers** and is **kept embedded by explicit user decision**, since it is NCERT Figure 8.5 itself and the check exists to block a *scientist portrait*, not the chapter's own artwork. **Do not remove `fig_8_5` to silence this WARN.** | ✅ **PASS — GATE 3 CLOSED (2026-08-23).** 3(a): **12/12 PDF pages + 9/9 assets** opened and inspected — no clipped badges, orphaned captions or collisions. 3(b): run in **both directions**, direction 2 as a **full read of source pp. 3–11 start to finish under an explicit grep prohibition**, per-section reading claim recorded, **no grep / coverage % / similarity score used to clear any row**. **3 defects → all fixed: 2 UNINVENTORIED + 1 DRIFTED, 0 MISSING, 0 FABRICATED** — (D1) the household curd/idli/dosa framing sentence → new row **`F055a`**; (D2) NCERT's sewage-disposal rhetorical question + its "you can understand why" nudge → new row **`F085a`**; (D3) an Exercise-Q11 lead-in claiming the samples were "collected before water treatment" while the table's own row calls one a **secondary effluent** → rewritten to match the data. **Both additions are the same family Ch13 hit 7 times** (rhetorical questions / framing sentences) — a repeat finding across two consecutive chapters, now a known Pass 1 blind spot. Post-fix: **200/200 ticked**, `check_pdf.py` exit 0 (0 fail, 1 accepted WARN), all three fixes re-confirmed **in the rebuilt PDF's text layer**, pages **5/6/12** (the three that reflowed) re-rendered and re-inspected, and the build shown **content-deterministic** (same text SHA-256 across three builds) | ✅ frozen inventory (**209 rows**) · tracker · `extract_figures.py` · assets (9 mono PNGs) · **`Ch8_MicrobesInHumanWelfare.py`** · **`Ch8_MicrobesInHumanWelfare.pdf` (12 pp)** | **✅ FULLY COMPLETE — CLOSED (Gate 3 earned on full-read evidence)** |
| 9 | Biotechnology: Principles and Processes | 12 | ✅ 200 facts frozen · 38/38 labels | ✅ WARN (0 fail, **1 benign warn**) · 13 pp · 7 mono imgs | ✅ zero confirmed defects | ✅ pdf · py · inventory · assets | **✅ FULLY COMPLETE — CLOSED** |
| 10 | Biotechnology and its Applications | 12 | ✅ 147 facts frozen · 10/10 labels · 14 summary rows | ✅ PASS (0 fail, **1 benign warn**) · 8 pp · 3 mono imgs | ✅ zero confirmed defects | ✅ pdf · py · inventory · assets | **✅ FULLY COMPLETE ��� CLOSED** |
| 11 | Organisms and Populations | 12 | ✅ **COMPLETE — third full re-audit of all 268 rows (2026-08-21)** — **268 facts** (F001-F265 + F030a/F189a/F252a) · 5 label rows / 22 in-figure labels re-read off the images · 18/18 headings · 16 opener rows · 17/17 summary sentences classified, 5 summary-unique folded · 10/10 exercises · 6/6 mono assets re-verified · **direction 2 clean — 0 UNINVENTORIED content** · 3 mechanical defects found + fixed (F146 unescaped pipes, F001 half-frozen heading, F252a markdown inside quote) | ✅ **PASS (0 fail, 1 inspected benign warn)** · **14 pp** · 6 mono imgs · 268/268 rows ticked · 22/22 labels in text · **checks 9 + 10 (orphaned headings, badge/banner collision) added and green** | ✅ **PASS — re-audited: 4 confirmed defects (D1 orphaned caption, **D2 + D3 orphaned headings**, **D4 badge plate clipped by banner**) → all fixed** · 14/14 pages inspected (4 D4 sites re-rendered at 700 dpi) · full read both directions, 0 UNINVENTORIED · rebuild reproducible (text-identical, only timestamps differ) | ✅ pdf · py · inventory · assets | **✅ FULLY COMPLETE — CLOSED (Gate 3 re-earned at session 6 after D4 was found by pixel inspection and fixed at template level)** |
| 12 | Ecosystem | 12 | ✅ 196 facts frozen (F001-F196, contiguous, 0 unticked — re-parsed) · 7 label rows / 67 label strings (67 unique, no doubling, no phantom row) · 10 heading rows · 6 opener rows · 8 summary-unique folded · 5 exercise-gap terms · 7/7 mono assets re-viewed | ✅ **PASS (0 fail, 1 benign warn) · exit 0 — GATE 2 GREEN**, re-run on the **post-D4 rebuild** · **10 pp** · 7 mono imgs · 196/196 rows ticked · **67/67 figure labels in text** · checks 1,2,3,5,6,7,8,9,10 all green | ✅ **PASS — Gate 3 CLOSED.** 3(a): 10/10 pages inspected (110 + 300 dpi 1-bit); 3(b): **bidirectional full read re-run three times**, most recently under an explicit **grep prohibition**, 11/11 source pages read start to finish, **0 UNINVENTORIED** (×3). **4 confirmed defects (D1 row-ID leak · D2 F114 drift · D3 contradictory Ex 1(e) answers · D4 mislabelled fix comments) → all fixed.** Reproducibility: 3 builds identical (10 pp / 25,683 chars / 7 imgs / same text SHA-256) | ✅ pdf · py · inventory · assets | **✅ FULLY COMPLETE — CLOSED (Gate 3 earned on the third full-read audit; two earlier PASSes withdrawn as premature)** |
| 13 | Biodiversity and Conservation | 12 | ✅ **PASS 1 COMPLETE — GATE 1 CLOSED (2026-08-22).** Figure census closed 4 ways (2 numbered figures, no 13.3+ exists) · 2/2 assets 300 dpi clip-rendered, `mode=="L"`, 0 colour px, each opened and read against checks (a)-(f) · **23 in-figure labels** harvested off the images (18 + 5; text layer yields **zero**), **folded into the Facts table as F039/F081 and confirmed by the real `_extract_labels`: 23 labels / 2 figures / 0 duplicate pairs / no phantom `Fig #` row** · **189 facts frozen, F001-F189 contiguous, 0 gaps, 0 dup IDs** (the Pass 1 freeze; **now 196 rows** after the 7 `a`-suffixed Pass 3(b) additions) · **21 heading rows · 8 opener rows at the freeze** (both corrected from a hand-tally slip of 22/9 — no row added, removed or reclassified; openers are **9** once `F112a` joins at 3(b)) · 25 summary sentences classified (19 BODY-PRESENT, 6 SUMMARY-UNIQUE) · exercise-gap table done · every header count re-derived by machine parse and matching | ✅ **PASS 2 COMPLETE — GATE 2 GREEN (2026-08-22), exit 0 (0 fail, 1 inspected benign warn)** · **11 pp** · 2 mono imgs · **189/189 rows ticked at Gate 2** (**196/196** on the post-3(b) rebuild, re-confirmed at the closure session) · **23/23 figure labels in running text** · smallest text 6.0pt · checks 1,2,3,5,6,7,8,9,10 all green · the lone WARN is check 4 matching "**photo**synthesis" in F143, not a portrait row | ✅ **PASS — GATE 3 CLOSED (2026-08-22d).** 3(b) bidirectional full read done: 13/13 source pages read start to finish against the named `# ---- 13.n ----` blocks, per-section reading claim recorded, **no grep/coverage evidence used to clear any row**. **7 confirmed defects, all UNINVENTORIED (direction 2 only): D1 insect-diversification question · D2 the two "inventory of our biological wealth" questions · D3 the tropics framing sentence · D4 "What exactly is stability���?" · D5 "no direct answers to such naive questions" · D6 the Evil Quartet opening sentence · D7 the ex-situ "beyond enclosures" sentence → all 7 fixed**, 7 rows added (`F035a`, `F048a`, `F065a`, `F085a`, `F092a`, `F112a`, `F171a`) and logged as a real Pass 1 gap, never back-dated. 0 MISSING · 0 FABRICATED · 0 DRIFTED. 3 further flags dismissed as false positives (FP-B1/B2/B3). Gate 2 re-run on the rebuilt PDF: exit 0, **196/196 rows ticked**. **11/11 pages re-rendered and re-inspected** after the reflow. Rebuild reproducible: 2 builds identical (11 pp · 2 imgs · 35,632 chars · same text SHA-256). Prior record: **3(a) COMPLETE (2026-08-22c) — 11/11 pages inspected, 0 confirmed defects.** Every page rendered twice (150 dpi + 300 dpi 1-bit B&W) and opened · figure aspect ratios exact to 4 dp (1.0467 / 1.1648), both `colorspace=1` · style census returns one size per element class (H1 10.5 · H2/table-head 9.5 · H3 9.0 · badge 6.0/6.21 · step digit 8.0) and exactly 6 template greys · NOTE vs MEMORY AID still distinct at 1-bit · Gate 2 **re-run this session, exit 0** · 3 flags raised → all 3 dismissed as false positives (FP-A1 inherited badge numbers, FP-A2 p2 KeepTogether whitespace, FP-A3 2.3pt table-stroke overhang). That prior record's closing line ("3(b) not started") is **superseded** — 3(b) is done, as the head of this same cell records. | ✅ pdf · py · inventory (196/196 ticked + Gate 3 record) · assets (2 figs) | **✅ FULLY COMPLETE — CLOSED (Gate 3 earned on a bidirectional full read; re-derived from the artefacts at the closure session, when the three status documents were also reconciled)** |

---

## Chapter 5 (Class 12) — Molecular Basis of Inheritance — ▶️ IN PROGRESS — PASSES 1 + 2 COMPLETE · GATES 1 + 2 CLOSED · GATE 3a COMPLETE

**Gate named explicitly (§7 rule 4): Gates 1 and 2 are CLOSED. Gate 3 is OPEN. Gate 3a (the page/asset walk) is COMPLETE and both of its fixable defects are FIXED; Gate 3b (the bidirectional full read of all 646 rows against the source) has NEVER been run, and it is the only thing that can close Gate 3 — so this chapter does not enter the Done tally.**

*(Superseded text, kept as history per §7: this block previously read "Gate 1 is CLOSED. Pass 2 has not started, there is no script or generated PDF, and 0 of 646 rows are ticked". All four claims are now false — the script and PDF exist, and 646/646 rows are ticked. The bullets below that still describe the pre-Pass-2 state were written in that era and are corrected in the same pass.)*

- **Protocol:** 31 source pages, 10 numbered sections plus Summary and Exercises; big-chapter protocol `1a → 1b → 2a → 2b → 3`. The whole-chapter `1-F` figure sweep and `1-Z` freeze are complete.
- **Environment (§0.2):** `/vercel/share/neetenv` was rebuilt before source work; reportlab, pdfplumber, pymupdf and Pillow imports were confirmed.
- **Frozen inventory:** **646 Facts rows, `F001..F646`, contiguous, 0 gaps, 0 duplicate IDs, monotonic, and now 646/646 TICKED** *(corrected: read "0 ticked", true only before Pass 2)*. Census: `concept` 264 · `figure-label` 136 · `definition` 56 · `number` 37 · `list` 33 · `question` 31 · `heading` 30 · `opener` 27 · `name` 18 · `example` 13 · `table` 1 = **646**.
- **Figures:** **16 numbered figures / 18 assets** — Figure 5.4 splits into `5.4a` and `5.4b`, and the unnumbered central-dogma plate on p4 (`fig_5_central_dogma`) is extracted as a bonus asset. All 18 are caption-anchored 300 dpi clips, verified mode `L` by machine (0 non-grayscale), smallest asset 1214x392 px, and individually opened after conversion to verify identity, complete labels/leader lines and print legibility.
- **Label parser:** `check_pdf.py`'s own `_extract_labels` returns **136 labels across 15 label-bearing figures**, with no doubling and no phantom `Fig #` row. `5.4b` and `5.15` are genuinely label-free; required labels `Central dogma` and `anticodon loop` are present.
- **Other Pass 1 evidence:** 33 summary sentences = 29 BODY-PRESENT + 4 SUMMARY-UNIQUE; exercise-gap table = **17 rows / 5 genuine gaps**.
- **Deliverables:** frozen inventory, `Ch5_TRACKER.md`, **18** verified PNG assets, **`Ch5_MolecularBasisOfInheritance.py`, and the generated 30-page `Ch5_MolecularBasisOfInheritance.pdf` with 17 embedded mono images** (`fig_5_15` retained on disk but removed from the PDF by owner decision). *(Corrected: this read "No `.py` or generated `.pdf`, correctly reflecting that Pass 2 has not started" — Pass 2 is complete and both artefacts exist.)*
- **Latest session — Gate 3a fix pass (Gate 3a CLOSED; Gate 3 still OPEN pending 3b).** The venv was **absent again** at session start (the expected §0.2 state) and was rebuilt before anything was diagnosed. Three owner decisions were taken and executed, and **one inherited finding was re-derived and disproved**:
  - **Badges — FIXED.** Three heading badges carried a non-unique or non-numeric label because the sub-heading is unnumbered in NCERT: two read `5.2` (p6, colliding with the parent §5.2 banner) and one read `Goals` (p24). All three now take a **letter suffix — `5.2a` "Transforming Principle", `5.2b` "Biochemical Characterisation of Transforming Principle", `5.9a` "Goals of HGP"**. The change was made **at the three call sites only**, so this is an **authoring convention and `neet_template.py` was NOT modified** — the shared frozen template stays untouched for every other chapter. `QR` and `EX` remain intentional non-numeric mnemonic badges and were deliberately left alone.
  - **Bullets — FIXED, but not the defect the handoff described.** The inherited handoff claimed **21 literal `•` glyphs "bypassing Check 5"**. A machine scan of the script found **zero typed U+2022 characters**: all 21 bullets come from ReportLab markup �� **9 as proper hanging `<bullet>&bull;</bullet>`** and **12 in Quick Recap as inline `&bull;` with no hanging indent**. So Check 5 was never bypassed and **`check_pdf.py` was correctly left unpatched**; the real defect was **markup inconsistency**. The **12 Quick Recap paragraphs were normalized to `<bullet>&bull;</bullet>`**; `Bullet1` already carried `firstLineIndent=-8`, so they now hang identically to the rest. The large `●` on p24 was investigated and is **`keyterm()`, a distinct template component — not an inconsistent bullet**, so it was left as-is.
  - **Baked-in double borders — ACCEPTED and CLOSED.** `fig_5_4b`, `fig_5_6`, `fig_5_7` and `fig_5_9` carry NCERT's own thin frame inside the crop, so they render as a double border inside the template's figure box. This is an **inherited cosmetic artifact of the source artwork**, given the **same disposition as the source watermark**: accepted, not a defect to fix. Re-cropping four assets is real session cost for no pedagogical gain. **Do not re-raise this in a future session.**
  - **Verification after the fixes:** `check_pdf.py --strict` re-run returns **PASS, 0 fail / 0 warn** — **30 pages, 646/646 rows ticked, 136/136 labels fully in text, 17 embedded mono images, smallest text 6.0 pt, no badge-plate collisions, no orphaned headings** — every metric identical to the pre-fix run, confirming the edits introduced no regression. Pages **6, 24 and 29 were re-rendered at 140 dpi and visually re-inspected** to confirm the three new badges and all 12 re-hung bullets.
  - **Carried forward:** **Gate 3b has never been run.** It is the mandatory bidirectional full read of all 646 inventory rows against the NCERT source (Direction 1: every row traced to the source; Direction 2: every source sentence checked for UNINVENTORIED facts), and per the Ch13 precedent it is where real Pass-1 gaps surface. **Gate 3 cannot close until 3b is complete.**
- **Prior session — stale-instruction audit (nothing advanced; Gate 1 still OPEN):** the venv was **absent again** (expected §0.2 state) and was rebuilt to identical versions before anything was diagnosed. Every current-state fact was re-derived by machine and **all matched**: **510 rows `F001..F510`**, 0 gaps, 0 dups, monotonic, **0 ticked**; census sums to **510**; **30 heading / 27 opener / 0 figure-label** rows; the real `_extract_labels` returns **0 labels / 0 figures / no phantom `Fig #` row**; the exercise-gap table measures **17 data rows / 5 GAP rows** (the earlier 16→17 correction **confirmed, not reverted**); H1 still `# Working Inventory (NOT FROZEN)`; no `.py`, `.pdf` or `assets/`; Done tally still **11 ✅ rows**. **The defect this session was stale *instructions* rather than stale numbers** — the more dangerous kind, since numbers get re-derived while instructions get obeyed. `Ch5_TRACKER.md` **§4 was still headed "NEXT SESSION — `1a-H`"** and told the next agent to append heading rows continuing from **`F231`**, an ID occupied since `1a-S`; obeying it would have collided with 279 existing rows and re-run a finished sweep. §4 now carries the real **`1-F`** brief (17 assets / 16 figure numbers, label rows from `F511`, acceptance criteria, explicit "Gate 1 stays OPEN when `1-F` closes"), and the entire `1a-H` scoping record is preserved **verbatim** in a new **§4a marked HISTORICAL / do not action**. Three adjacent stale claims were reconciled in the same pass: §5 still listed the completed `1a-O` as a forward note and `1-Z` as wholly pending; §5's **"Gate 1 is judged only after `1b`"** implied the gate should now be closed; and the inventory's Facts-table scope note still read *"after session 1a-S: prose facts of the first half only"*, which could have convinced a future agent that heading and opener rows were still missing. All corrected with the superseded text quoted rather than deleted. **No Facts row touched, no history rewritten, no freeze run.**
- **Prior session — documentation-consistency audit (nothing advanced; Gate 1 still OPEN):** every Ch5 claim re-derived from disk under the rebuilt `/vercel/share/neetenv` (the venv was absent at start, the expected state, and was rebuilt before anything was diagnosed). Confirmed: **510 rows `F001..F510`**, contiguous, 0 gaps, 0 dups, IDs monotonic, **0 ticked**; census sums to 510; **30 heading / 27 opener / 0 figure-label rows**; the real `_extract_labels`, imported from `check_pdf.py` rather than replicated, returns **0 labels / 0 figures / no phantom row**; H1 still `# Working Inventory (NOT FROZEN)`; folder holds only the inventory and the tracker — no `.py`, `.pdf` or `assets/`. **Fixed one substantive inconsistency, in five places:** the exercise-gap count read **"16 scanned items"** across the inventory (Gate 1 table, gap-section prose, session log) and the tracker (ledger row, machine-metrics row), but a machine parse of the table's own length returns **17 rows**. The old figure was a hand-tally of *questions* reported as if it were *rows* — the 14 questions occupy 16 rows because `Q3`+`Q4` share one and `Q8`/`Q14` each split, and the in-body `F288`/`F289` pair is the 17th. All five now read **17 rows / 5 gaps** and state their basis. **`CHAPTER_STATUS.md` itself was the worst-drifted document** and was rewritten in both places: its overview row and this section still described the `1a-S`-only state (231 rows, "0 heading / 0 opener by design", "next session `1a-H`") after six sweeps and `1-Z` steps 7/8/10 had landed.
- **Prior session's carry-overs, now closed:** the `_extract_labels` re-run under a real venv (done — 0 labels) and the tracker/inventory session-count basis (done). `scratch/ch5mbi/full_text.txt` remains recorded as a **non-deliverable working aid** — facts come from the PDF, never the dump.
- **Detail layer:** `notes/class 12/Ch5_MolecularBasisOfInheritance/Ch5_TRACKER.md` holds the 9-session ledger, the verified source map, the re-derivation commands, per-session acceptance criteria and the corrections log. Where this summary and that tracker disagree about Ch5, the tracker is re-derived from disk and wins — and the fix must land in both files in the same session.

---

## Chapter 6 (Class 12) — Evolution — ▶️ IN PROGRESS — PASS 1 SESSION `1-F` ONLY · GATE 1 OPEN

Source: `Chapter/class 12/Chapter 6 - Evolution.pdf` (17 pp, page box 612 x 820.8 — **not**
the usual 612 x 792). Deliverables so far: `assets/` (13 PNGs), `extract_figures.py`,
`audit_figures.py`, and `Ch6_Evolution_inventory.md` — a **working file, not a freeze**.

- **What has run:** session `1-F` only (§4.4 / §6 Pass 1 step 6) — figure census, rect
  pinning, extraction, the raster audit gate, label harvest, and the documentation of all
  four. Sessions `1-S`, `1-H`, `1-O` and `1-Z` have **not started**, so the inventory has
  no Facts table, no heading or opener census, and no `F###` IDs; the 13 matrix rows carry
  `—` and `1-Z` must **move** them into the single `## Facts` table and number them there.
- **Figure census (from the page images, not the caption numbers):** **11 numbered figures
  6.1-6.11 on artwork pages 3, 5, 6, 7, 8, 9, 11, 13, 14, 16 → 13 assets.** `6.3` and `6.4`
  each print two labelled sub-panels and split a/b per the skill rule; `6.8` is deliberately
  kept as **one** asset because its (a)/(b)/(c) panels share a single rounded frame and the
  arrows *between* them carry the meaning. **No unnumbered bonus plate exists** (checked
  against every page, unlike Ch5's central-dogma schematic), and **no scientist portrait
  exists anywhere in the chapter** — §4.4's photograph hard-no has no manifest row here.
  `fig_6_11`'s human/chimpanzee heads are line-art illustrations, not photographs; that
  adjudication is recorded so Pass 2 does not "fix" it by dropping the asset.
- **The whole PDF is a scanned raster — proved, not assumed.** `get_text("words")` returns
  **0 words** and `get_drawings()` returns **0 drawings** on all 17 pages. Skill audit
  check A (word grazing) is therefore **vacuous — it cannot fail**, and check B (drawings
  extent) is vacuous too. Captions had to be transcribed from rendered caption strips, and
  all **116 in-figure labels** had to be harvested by opening each asset; a text sweep
  would have returned an empty label set and passed every downstream check silently.
- **Replacement gate — `audit_figures.py` (committed):** A' interior ink slack per edge
  (`SLACK_MIN -0.5 pt` to absorb one pixel of quantisation, `SLACK_MAX 22 pt`) plus B' an
  8 pt edge-band dark-pixel probe whose every hit must be **named** in an `EXPLAINED` dict.
  **`BAND_TOL` is 12 px, not the skill's 40**, because two real clipped-label defects in
  this chapter sat under 40 and passed clean. Re-run from disk this session:
  **GATE PASSED — 13 figures, slack in range, all edge ink explained.** Every B' hit is a
  neighbouring sub-panel, an excluded caption band, the leaf/branch page furniture, or the
  decorative right-margin band.
- **Labels:** 13 rows; the real `_extract_labels` **imported from `check_pdf.py`** returns
  **116 labels / 9 label-bearing figures / no doubling / no phantom `Fig #` row**. The 4
  rows for unlabelled assets (`6.4a`, `6.4b`, `6.5`, `6.11`) are worded so they do **not**
  begin `Figure labels`, contributing 0 by design. Panel letters, the finch numerals 1-4
  and fig 6.10's 0-350 mya axis are excluded from quoting and are logged instead as **Pass 2
  content obligations** — the time axis one is marks-critical.
- **Eight rects were re-pinned during `1-F`, and four were caught by eye, not by the gate**
  — including `fig_6_1` shipping five lines of body prose (full-width prose reads as figure
  ink to an extent probe), `fig_6_3a`/`fig_6_3b` losing whole panels to an `x0=392` pin that
  assumed the figure sat only in the right half, `fig_6_4b` slicing its "(b)" label in half,
  and `fig_6_10` rendering "arboniferous". All 13 assets were then opened individually at
  full size. Details, with the measurement behind each pin, are in the inventory and in the
  per-rect comments in `extract_figures.py`.
- **Monochrome defect fixed in-session:** the first `extract_figures.py` autocontrasted the
  RGB pixmap, shipping all 13 assets as `mode=RGB`. The pipeline is now
  clip-render → `.convert("L")` → `autocontrast(cutoff=1)`; all 13 re-verified `mode=L`.
- **Not done:** four of five Pass 1 sessions, the freeze, the script, the PDF, and Gates 1,
  2 and 3. **Gate 1 stays OPEN when `1-F` closes**, and this chapter must not appear in any
  completion tally.

---

## Chapter 8 (Class 12) — Microbes in Human Welfare — ✅ FULLY COMPLETE — GATE 3 CLOSED

*(Not to be confused with **Class 11 Chapter 8 — Cell: The Unit of Life**, which has its own section further down.)*

**Current state (closure session): Gate 3 is CLOSED and Ch8 is DONE — 209 inventory rows, 200/200 Facts rows ticked, all 3 Pass 3(b) defects fixed and independently re-verified.** Pass 3(a) walked **12/12 PDF pages and 9/9 assets**. Pass 3(b) was completed as a **bidirectional** read: direction 1 confirmed every then-existing row present *and* faithful in the PDF; direction 2 read **source pp. 3–11 start to finish under an explicit grep prohibition**, with a per-section reading claim and **no grep, coverage percentage or similarity score used to clear any row**. It found **3 confirmed defects — 2 UNINVENTORIED + 1 DRIFTED**: **D1** the household curd/idli/dosa framing sentence → new row **`F055a`** · **D2** NCERT's sewage-disposal rhetorical question plus its "you can understand why" nudge → new row **`F085a`** · **D3** an Exercise-Q11 lead-in asserting the three samples were "collected before water treatment" when the table's own row calls one a **secondary effluent** → rewritten to match the data. **0 MISSING · 0 FABRICATED.** The two additions took the inventory **207 → 209 rows** (198 → **200** Facts) and are logged as a **real Pass 1 gap, never back-dated into the freeze**.

**The repeat finding that matters more than the count.** Both UNINVENTORIED defects are **the same family Ch13 hit 7 times** — NCERT's rhetorical questions and framing/opening sentences. Two consecutive chapters have now lost rows to the same blind spot, so it is a **systematic Pass 1 weakness, not chapter-specific bad luck**: a sweep hunting facts reads this connective tissue as filler and skips it. Future Pass 1 sweeps should treat interrogatives and section-opening framing sentences as candidate rows by default.

**Why the gate was still open when this session started, and what this session did.** The Pass 3 audit session **ended mid-update** — the same half-finished-handoff failure as Ch13. `Ch8_TRACKER.md`'s header already claimed "GATE 3 GREEN / 209 rows / 200 ticked", while its own **§2 ledger, §3 on-disk block, §5 Gate 1 table and §6 check 7 still said 207/198**, its **§7 was still a forward-looking "NEXT SESSION — Pass 3" brief**, and both roll-ups still said **Gate 3 OPEN**. Under §7 rule 8 that cross-document disagreement **is itself the defect**. Per §7, inherited state was not trusted: the venv was rebuilt first (absent at session start, the expected §0.2 state) and every claim was **re-derived from the artefacts** before any status line was touched.

| Re-derived claim | Method | Result |
|---|---|---|
| Row count / ticks | machine re-parse of the Facts table, not the header | **209 rows · 0 duplicates · 200/200 Facts ticked · 0 unticked**; `F001..F207` contiguous + exactly 2 `a`-rows |
| Type census | re-tallied from the parsed rows | sums to **209**, all lowercase, no strays |
| 3 fixes present in the PDF | pdfplumber extract of the committed PDF, searched per phrase | **3/3 present** — D2 renders as prose rather than a verbatim question string, which is spec-compliant |
| Gate 2 still green after the fixes | `check_pdf.py` re-run from disk | **exit 0 · 0 fail · 1 WARN** — the known, inspected, accepted portrait heuristic |
| PDF matches the committed script | rebuilt from the committed `.py` and compared | **12 pages, same text SHA-256** |
| Rebuild reproducibility | three builds compared | **content-deterministic** — identical text SHA-256; embedded timestamps/IDs differ, so `git status` always shows the binary as modified (**expected, not a defect**) |
| Reflowed pages sound | pages **5, 6, 12** re-rendered at 110 dpi and visually inspected | no clipped badges, orphaned captions or collisions |

**The accepted WARN — do not "fix" it.** `check_pdf.py` check 4's portrait heuristic fires on the literal word "photograph" in `F050`/`F203`–`F206` and the four `raster photograph` manifest rows. All four plates were opened: **8.4 (fermentor vessels), 8.6 (aeration tank) and 8.7 (aerial sewage plant) contain no people at all**; **8.5 is an industrial fermentation-plant photograph with two incidental workers**, kept embedded **by explicit user decision** because it is NCERT Figure 8.5 itself and the check exists to block a *scientist portrait*, not the chapter's own artwork. **Do not remove `fig_8_5` to silence this WARN** — it would also break the 9-asset denominator used throughout.

**Detail layer:** `notes/class 12/Ch8_MicrobesInHumanWelfare/Ch8_TRACKER.md` — §2 session ledger (all 5 Pass 1 sweeps + Pass 2 + Pass 3 + this closure), §3 the on-disk state, §5/§6 the Gate 1 and Gate 2 records, and **§7 the Gate 3 closure record** (formerly the stale "NEXT SESSION" brief, rewritten so no future agent re-runs a completed pass). Where this summary and that tracker disagree about Ch8, the tracker is re-derived from disk and wins — and the fix must land in both files in the same session.

---

## Chapter 13 (Class 12) — Biodiversity and Conservation — ✅ FULLY COMPLETE — GATE 3 CLOSED

**Current state (closure session, fifth visit): Gate 3 is CLOSED and Ch13 is DONE — 196/196 rows ticked, all 7 Pass 3(b) defects fixed and independently re-verified.** Pass 3(b) was completed as a bidirectional full read — 13/13 source pages read start to finish against the named `# ---- 13.n ----` blocks, with a per-section reading claim and **no grep/coverage evidence used to clear any row**. It found **7 confirmed defects, all UNINVENTORIED and all one family** (NCERT's rhetorical questions and framing/opening sentences): D1 the insect-diversification question · D2 the two "inventory of our biological wealth" questions · D3 the tropics framing sentence · D4 "What exactly is stability…?" · D5 "no direct answers to such naive questions" · D6 the Evil Quartet opening sentence · D7 the ex-situ "beyond enclosures" sentence. All 7 fixed; 7 rows added (`F035a`, `F048a`, `F065a`, `F085a`, `F092a`, `F112a`, `F171a`) and logged as a **real Pass 1 gap, never back-dated into the freeze**. 0 MISSING · 0 FABRICATED · 0 DRIFTED. Full evidence: the inventory's `## Gate 3 record`.

**Why the gate was still open when this session started, and what this session actually did.** The 3(b) audit session **ended before it could update this file and `CHAPTER_TRACKER.md`**, so the inventory read "Gate 3 CLOSED" while both trackers still read "3(b) not started". Under Gate 3(b) rule 2 that cross-document disagreement **is itself the defect**, and it was the one thing genuinely outstanding. Per §7, inherited state was not trusted: every claim was **re-derived from the artefacts** before these documents were touched.

| Re-derived claim | Method | Result |
|---|---|---|
| 7 fixes present in the PDF | pdfplumber extract of the committed PDF, searched per phrase | **7/7 present** |
| 7 fixes are NCERT's own wording | re-extracted the **source** PDF and read each in context | **7/7 verbatim** |
| Audit trail intact | read every `# [VERIFICATION FIX]` hit with its block marker | **7 tags**, each naming its row ID + direction 2 + UNINVENTORIED |
| Row count / ticks | machine re-parse of the Facts table, not the header | **196 rows · 196 unique · 196 ticked · 0 unticked**; `F001..F189` contiguous + exactly 7 `a`-rows |
| `Type: opener` = 9 | machine re-parse by type | **9** — incl. `F112a` as the 9th |
| Gate 2 | `check_pdf.py` re-run on a rebuilt venv | **exit 0** (0 fail, 1 inspected benign warn) · 196/196 ticked · 23/23 labels · 55 headings none orphaned · 83 plates none colliding · smallest glyph 6.0pt · 2/2 mono · 11/11 A4 |
| Reproducibility | rebuilt from the script, compared to the committed PDF | **identical** — 11 pp · 2 imgs · 35,632 chars · text SHA-256 `6ba3b2f26e92b6c6…` |
| Pass 3(a) after the reflow | re-rendered and inspected **11/11** pages at 110 dpi | **0 defects**; figures intact; repeated table headers hold |

**A search artefact worth keeping.** Naive substring searches for D4 and D5 first reported *not found* against the source, and **both were faults in the search string, not the fix**: D4's sentence is split by NCERT's running header (*"…stability for a biological **Reprint 2026-27 BIODIVERSITY AND CONSERVATION** community?"*), and D5 prints *"nä**i**ve"* with a diaeresis while the script correctly renders plain ASCII under the §4 banned-glyph rule. Both were resolved by reading the surrounding source text. This is exactly why §6 bars grep as Gate 3(b) evidence — **a false *absence* misleads as badly as a false presence.**

**Stale count corrected (§6: "the rows win and the count changes").** The inventory's line-2 header still read `Rows: **189** (F001..F189)`, the pre-3(b) value, while its own machine-derived table and `check_pdf.py` check 7 both reported **196**. The line was corrected to match the rows; **no Facts row was added, removed, reworded or reclassified** to achieve it.

**Environment note (this session):** `/vercel/share/neetenv` was missing again — the fourth consecutive session in which the sandbox reset. It was rebuilt before any verification ran, per §0.2.

**Prior state (session 2026-08-22, second visit): Gate 2 closed.** Pass 2 was written linearly from the frozen 189-row inventory into `Ch13_BiodiversityAndConservation.py`, which imports the repo-level frozen `neet_template.py` and re-declares no style, geometry, colour or font. The build is `Ch13_BiodiversityAndConservation.pdf` — **11 pages, 34,117 extracted characters, 2 embedded monochrome images** — and `check_pdf.py` exits **0** on it (0 fail, 1 warn).

**Gate 2 evidence (final rebuilt PDF, not a carried-forward verdict):** checks 1, 2, 3, 5, 6, 7, 8, 9, 10 all PASS — no text in the 1.4 cm bands; smallest rendered glyph **6.0pt** (FAIL floor 5.0); both images monochrome; no banned Unicode; **23/23 figure labels found in running text, 0 partial, 0 missing**; **all 189 Facts rows ticked**; 11/11 pages A4 portrait; 54 banner headings none orphaned; 82 badge plates none colliding. The single **WARN is check 4 and is a confirmed false positive**: it keys off "photo" and matched **F143** ("through **photo**synthesis, 20 per cent of the total oxygen…"), a number row, not a portrait row. The Gate 1 figure census already established this chapter has **no photograph of any kind**, and check 3 confirms the PDF embeds exactly the 2 figure assets.

**All seven Pass 1 carry-overs were actioned:** Fig 13.2's caption states the lost-colour distinction in words (mid-grey curve = arithmetic-scale rectangular hyperbola, near-black line = the same relationship on a log-log scale); `S = CA<super>Z</super>` and `log S = log C + Z log A` are both written into running text so F081 matched **without editing the frozen row**; degrees stayed spelled out; F037's four-class Fungi comparison and F181's broader all-vertebrates form are carried together in one NOTE; 448 sanctuaries is the body figure with the summary's "more than 450" noted; the inventory's `Type`-column casing was left untouched. The four exercise gaps have their planned homes: Q4 (slope significance) in a NOTE at the end of 13.1.2 (ii), Q8 (flood/soil-erosion services) in a NOTE in 13.2.1 explicitly attributed as summary-sourced plus inference, Q9 (72 per cent) in a NOTE under 13.1.1, and Q10 (deliberate extinction) in a MEMORY AID marked "not in NCERT" — all four also tabulated in the closing **Terms used in the exercises** appendix.

**Gate 3 is fully closed — all five conditions hold.** 3(a) is closed on an every-page inspection claim (11/11, re-inspected 11/11 after the 3(b) reflow); 3(b) is closed on a bidirectional full read with a per-section reading claim. **Ch13 is Done and is now counted in the roll-up totals** (Class 12: 5/13 · overall 11/32, both re-derived by counting the ✅ rows rather than incrementing).

| Gate 3 condition (§6) | State |
|---|---|
| 1. Zero confirmed defects remain | ✅ all 7 fixed, re-verified in the rebuilt PDF this session |
| 2. `check_pdf.py` green on the final rebuilt PDF | ✅ exit 0 (0 fail, 1 inspected benign warn) |
| 3. Pass 3(a) covered every page | ✅ 11/11, then 11/11 again post-reflow |
| 4. Pass 3(b) full read, both directions, per-section claim | ✅ 13/13 source pages against named script blocks; direction 2 found all 7 |
| 5. Rebuild reproducible | ✅ two builds identical (11 pp · 2 imgs · 35,632 chars · same text SHA-256) |

| Artifact | State |
|---|---|
| Environment (§0.2-0.3) | ✅ **rebuilt this session** — the sandbox was fresh and `/vercel/share/neetenv` did not exist. venv recreated, CPython 3.13.11 · reportlab · pdfplumber · pymupdf · Pillow 12.3.0 |
| Figure census | ✅ closed — **2 figures**, established four independent ways |
| Extraction → mono → per-figure verification | ✅ 2/2, each image opened and read; assets re-verified this session (`L` mode, 1501×1434 and 1117×959) |
| Figure manifest | ✅ written |
| Figure-label matrix (23 labels) | ✅ **folded into the Facts table with real IDs (F039, F081); provisional table deleted** — the documented trap did not fire |
| Fact inventory | ✅ **196 rows** — 189 frozen at Pass 1 (`F001-F189` contiguous, 0 gaps, 0 duplicate IDs) **+ 7 `a`-suffixed rows added at Pass 3(b)**; re-parsed by machine this session |
| Heading / opener census | ✅ **21 heading rows · 9 opener rows** — the opener count is 8 frozen at Pass 1 **+ `F112a`** added at Pass 3(b); re-derived by machine this session |
| Summary classification | ✅ 25 sentences classified — 19 BODY-PRESENT, 6 SUMMARY-UNIQUE |
| Exercise-gap table | ✅ written |
| **Gate 1** | ✅ **CLOSED — every header count re-derived by machine parse and matching** |
| Chapter script | ✅ `Ch13_BiodiversityAndConservation.py` — linear, `# ---- 13.n ----` block markers, imports `neet_template.py`, inventory row IDs named in comments |
| Chapter PDF | ✅ `Ch13_BiodiversityAndConservation.pdf` — 11 pp, **35,632 chars** (post-3(b) rebuild; the earlier 34,117 pre-dates the 7 insertions and was measured with a different extractor), 2 mono images, both figures inline at their topic |
| Inventory ticks | ✅ **196/196 ticked** — only the `Ticked` column changed, no frozen row reworded |
| **Gate 2** | ✅ **GREEN — `check_pdf.py` exit 0 (0 fail, 1 inspected benign warn on check 4)** |
| **Gate 3(a)** | ✅ **CLOSED (2026-08-22c)** — 11/11 pages rendered at 150 dpi **and** 300 dpi/1-bit and inspected; 0 confirmed defects; 3 flags dismissed as false positives; frame containment, figure aspect ratio and cross-page style consistency all measured. Evidence: inventory `## Gate 3 record` |
| **Gate 3(b)** | ✅ **CLOSED** — bidirectional full read, 13/13 source pages against the named `# ---- 13.n ----` blocks, per-section reading claim recorded. **7 UNINVENTORIED defects found by direction 2 → all fixed**, 7 rows added and logged as a real Pass 1 gap. No coverage/similarity/grep evidence was used to clear any row |
| **Gate 3 overall** | ✅ **CLOSED** — all five conditions hold; re-derived from the artefacts in the closure session, not carried forward |
| Document consistency | ✅ inventory · `CHAPTER_STATUS.md` · `CHAPTER_TRACKER.md` **all agree**, reconciled in one session per Gate 3(b) rule 8 (atomic closure) |

### Figure census (why "only 2 figures" is a finding, not a shortfall)

§4.4 warns that a label-free or figure-thin result is a red flag about method, so the census was run four ways and all four agree: caption sweep of all 13 pages (2 hits: p3, p5); in-text `Fig 13.n` reference sweep (**no 13.3 or higher is referenced anywhere**, so no figure lost its caption); per-page vector-drawing density (p3 = 93,887 ops for the three pie charts, p5 = 72 for the graph, every other page 4-18 — the same baseline as figure-free p4, so no hidden vector diagram); and a raster census in which every embedded image resolves to page furniture (page background, `© NCERT` watermark, header band, p1 QR code, p1 tilted thumbnail of Fig 13.1, pp10-11 grain strip). **The chapter contains no photograph of any kind and no scientist portrait**, so §7's person-photograph check has nothing it could fire on truthfully.

### Figures extracted, converted and verified

| Fig # | Asset | Source | Px @300 dpi | Mono evidence | Labels |
|---|---|---|---|---|---|
| Fig 13.1 — global biodiversity pie charts | `assets/fig_13_1.png` | p3 (book p218), clip `(170,234,530,578)` | 1501×1434 | `mode=="L"`, 0 colour px, greys 0-255 | 18 |
| Fig 13.2 — species-area relationship | `assets/fig_13_2.png` | p5 (book p220), clip `(56,228,324,458)` | 1117×959 | `mode=="L"`, 0 colour px, greys 0-255 | 5 |

Both were taken as **clip renders, not embedded-object grabs** — Fig 13.1 is pure vector and Fig 13.2 mixes vector strokes with vector text, so an object extraction would have mangled both. Each clip stops just above its caption (578 < 581 and 458 < 463), so neither crop swallowed its own caption or a neighbour.

**Colour-loss carry-over into Pass 2 (mandatory, not optional):** Fig 13.2's two plots were separated in the original by hue — a blue arithmetic-scale curve and a crimson log-log line. After conversion they read as mid-grey vs near-black and each still carries its own text label, but the caption and running text must state **in words** which plot is the rectangular hyperbola and which is the log-log straight line. Fig 13.1's wedges survive on grey level + black boundary stroke + their own text labels, and NCERT prints **no percentages inside** that figure — the proportions (>70 per cent animals, no more than 22 per cent plants, >70 per cent of animals are insects) live in the running text and must be carried there.

### Labels: 23 harvested by eye, 0 recoverable by text extraction

Fig 13.1 (18): Invertebrates · Other animal groups · Crustaceans · Molluscs · Insects · Vertebrates · Fishes · Mammals · Birds · Reptiles · Amphibians · Plants · Mosses · Ferns and allies · Fungi · Angiosperms · Algae · Lichens. Fig 13.2 (5): Species richness · Area · S = CAZ · Log S = log C + Z log A · log-log scale.

All 23 were confirmed this session by running `check_pdf.py`'s **real** `_extract_labels` against the frozen inventory — 18 under `Fig 13.1`, 5 under `Fig 13.2`, **zero duplicated (figure, label) pairs and no phantom `Fig #` row**. Note the third Fig 13.2 label is recorded as `S = CAZ`, the extraction-normalised form of the figure's `S = CA` with a superscript Z; carry-over 2 in the inventory forbids "correcting" it to a caret or a Unicode superscript, since either would guarantee an unfixable check-6 FAIL.

`page.get_text()` on pp3 and p5 returns the captions and body prose and **zero** in-figure labels — every one of the 23 is a vector stroke. This is exactly the silent failure §4.4 describes: a text-extraction harvest would have produced an empty matrix that passes Gate 1 and check 6 trivially.

### How Gate 1 was closed (session 2026-08-22)

The three resume steps from the previous session's handoff were executed, and the documented trap was checked rather than assumed:

1. **Label fold-in verified, not trusted.** The provisional `FIG-1`/`FIG-2` table is gone and the labels live in the Facts table as F039 (18 labels) and F081 (5). Running the actual `_extract_labels` from `check_pdf.py` returns **23 labels across exactly 2 figures, 0 duplicate pairs, no phantom `Fig #` row** — so the double-count trap did not fire.
2. **Header counts re-derived by machine, per §6 step 8.** A parse of the frozen table confirms 189 contiguous rows (no gaps, no duplicate IDs), 25 summary sentences (19 + 6), and 23 labels split 18/5.
3. **Defect 1 (the one real defect) fixed.** The header claimed **22** heading rows and **9** opener rows; the true values are **21** and **8**.

**Defect 1 was a hand-tally error, not a missing row — this matters.** The heading census listed 9 numbered IDs plus an unnumbered list that has only ever contained **12** entries, while asserting "= 13"; 9 + 12 = 21. The opener census named **8** real IDs, and the phantom 9th was a *structural finding* (13.1.2 and 13.2 have no opener prose of their own) written up as though it were a row. Both were corrected in the header table, in the Gate 1 checklist rows that restate them, and in the prose census sections — all four places, so nothing contradicts anything else. **No Facts row was added, removed or reclassified**, which is what keeps the inventory legitimately frozen.

**A third, minor finding was recorded rather than silently fixed:** the `Type` column mixes `caption` (F038, F080) with `Caption` (F039, F081), so a case-sensitive tally splits 4 caption-ish rows into two classes. It breaks no Gate 1 criterion — no header count derives from that type, and `_extract_labels` keys off the row wording, not the type — so it is logged as carry-over 7 instead of editing frozen rows for a cosmetic gain.

**Environment note:** the sandbox was fresh and `/vercel/share/neetenv` did not exist, so the venv was rebuilt before any verification ran. The previous handoff attributed a failed step-3 run to a script bug; the real cause was simply the missing interpreter.

### State at the hard stop

**Superseded — kept for the record.** This paragraph described the *first* session's hard stop, when Gate 1 had just closed and no script or PDF existed yet. Both were built in the second session (Gate 2 green) and the render pass ran in the third (Gate 3(a) closed); the live state is the top of this section. The inventory's **7 carry-overs**, all since actioned by Pass 2, were: Fig 13.2 colour-loss caption, the `S = CAZ` superscript rule, the log-form equation, spelled-out degree signs, F037/F181 both appearing, F164-vs-F184 sanctuary counts, and the `Type`-casing note.

---

## Chapter 12 (Class 12) — Ecosystem — ✅ FULLY COMPLETE — GATE 3 CLOSED

**Current state (session 2026-08-21b): all three gates green, chapter delivered and recorded.** 10 pp · 7 mono figures �� 196/196 rows ticked · `check_pdf.py` 0 fail / 1 accepted benign warn across all 10 checks. Gate 3 was earned on the **third** independent bidirectional full read; the two earlier PASS claims are withdrawn in the record as premature (see below).

| Gate | Status | Evidence |
|---|---|---|
| Gate 1 | ✅ GREEN | 196 facts (F001-F196, contiguous, 0 unticked — machine re-parsed) · 7 label rows / 67 label strings (67 unique, no doubling, no phantom `Fig #` row) · 10 heading rows · 6 opener rows · 8 summary-unique folded · 5 exercise-gap terms · 7/7 assets mono+verified |
| Gate 2 | ✅ GREEN — re-run on the **post-D4 rebuilt** PDF, 0 fail / 1 benign warn, exit 0 | all of checks 1,2,3,5,6,7,8,9,10 PASS · 67/67 labels in text · 196/196 rows ticked |
| Gate 3(a) | ✅ **CLOSED** — 10/10 pages inspected; pages 3 and 10 re-inspected after the D4 fix | this section |
| Gate 3(b) | ✅ **CLOSED** — bidirectional full read completed and independently re-run **three times**, the last under an explicit grep prohibition. 11/11 source pages read start to finish; **0 UNINVENTORIED** every time | inventory `## Gate 3 record` → *Third independent re-verification* |

### Why this chapter took three audits to close

Ch12 is the project's clearest case of the failure mode §6 warns about. It was declared closed **twice** while still defective, and each time the next session found a defect **inside material the previous session had explicitly marked clean**:

1. **First closure claim** — inventory said "GATE 3: PASS", with the Exercises block marked *clean in both directions* and 0 remaining defects. But `CHAPTER_STATUS.md`/`CHAPTER_TRACKER.md` were never updated, so the claim was unrecorded ��� and the mismatch was the tell.
2. **Second session** — re-read that same "clean" Exercises block and found **D3**: two different answers to the same exercise blank (Ex 1(e)), one of them using non-NCERT wording ("oceans"). It fixed D3 and again wrote "PASS — chapter closed" in the inventory, but **again left both tracking files saying "3(b) pending"**.
3. **Third session (this one)** — instructed to trust neither verdict and forbidden from using grep for any content claim. Re-ran both directions from the source PDF in a rebuilt environment and found **D4** in that *same* Exercises block: the script's `[VERIFICATION FIX]` comments contradicted the inventory's defect register (the D3 fix was labelled "D2", and D1/D2 sites carried no ID at all), which would send a future session to the wrong block. Fixed it, then **updated both tracking files in the same session** — the step both prior sessions skipped.

**The lesson recorded for future chapters:** a prior session's "clean" verdict on a block is not evidence about that block, and an inventory that says PASS while the tracker says pending should be read as *not closed*. Three of Ch12's four defects were found by re-reading already-cleared text; none of the four was catchable by any `check_pdf.py` check, and **D3 in particular could not have been found by any grep** — both of its contradictory answers were legitimate chapter vocabulary.

### Environment (re-established this session, §0.2–0.3)

Venv rebuilt at `/vercel/share/neetenv` (Python 3.13.11); reportlab 5.0.1 · pymupdf 1.28.2 · Pillow 12.3.0 · pdfplumber OK — matches the known-good reference set. Every command run through `/vercel/share/neetenv/bin/python`, never bare `python3`.

### Pass 3(a) — visual render check: 10/10 pages inspected

Every page rendered twice and viewed directly: at 110 dpi for layout, and at **300 dpi + a true 1-bit B&W threshold** for print-safety and cross-page style consistency. Not spot-checked — all ten pages.

| Page | Content | Verdict |
|---|---|---|
| 1 | Title block + DNA motif, chapter opener, 12.1 opening, 2 tables, NOTE box, 4-step process flow | clean |
| 2 | 12.1 pond sub-heading, 4-row table, 12.2 Productivity, 2 tables, 2 NOTE boxes | clean |
| 3 | 12.3 Decomposition, **5-step process flow**, 2 NOTE boxes, factor table | **D1 found here (5 leaks)** |
| 4 | **Fig 12.1** (bordered, mono, captioned), 12.4 Energy flow, NOTE box | clean |
| 5 | Consumer-category table, GFC 3-step flow, DFC, ecosystem table, NOTE, trophic-level table | clean |
| 6 | **Fig 12.2** + caption, standing crop, 10 per cent law | clean |
| 7 | **Fig 12.3** + caption (colour-dependency stated in words), 12.5 Ecological pyramids, 4-column pyramid-values table | clean |
| 8 | NOTE box + **Fig 12.4 (a)(b)(c)(d)** — 4 figures, each with its own caption, correct order | clean |
| 9 | Pyramid exceptions, limitations NOTE, 12.6 Nutrient cycling, Quick Recap, appendix heading | clean |
| 10 | Exercise-gap term table, NCERT exercises appendix, closing statement | **D1 found here (1 leak)** |

**Mechanical style-consistency check (replaces hand-hunting for drift).** Extracted every `(font, size)` class in the PDF and the pages it appears on — one Times-only canon, no stray font, each element type identical wherever it recurs:

| Element | Style | Pages seen |
|---|---|---|
| Body / bold running text | Times-Roman / Times-Bold 10.8pt | all 10 |
| Table cells / heads | Times-Roman / Times-Bold 9.5pt | 1,2,3,5,7,9,10 |
| NOTE + MEMORY-AID italics | Times-Italic / Times-BoldItalic 10.2pt | 1,2,3,4,5,8,9 |
| Section-number badges | Times-Bold 6.21pt | 1,2,3,4,7,9 |
| H1/H2 banner text | Times-Bold 10.5pt | 1,2,3,4,7,9 |
| Figure captions | Times-Italic 9.5pt | 4,6,7,8,10 |

- **Frame integrity:** every page's text bbox starts at left = 48.5pt (> the 42.5pt margin) with nothing in the top/bottom 1.4 cm bands — consistent with check 1 PASS.
- **Figure geometry:** 7 image placements, each ≤ 450.7pt wide inside the 15.9 cm column, aspect ratios intact, none upscaled or squashed; Fig 12.4(a)-(d) each sit with their own caption.
- **B&W print safety:** at 1-bit threshold the NOTE double-rule borders, banner reversed-out text, triangle step badges and their digits all remain legible and tell-apart-able; no meaning rests on fill alone.

### Confirmed defect (Pass 3(a))

| ID | Defect | Class | Where | Fix |
|---|---|---|---|---|
| **D1** | **Inventory row IDs leaked into reader-facing text** — the PDF printed internal bookkeeping tokens to the student: `(F060)`, `(F061)`, `(F062)`, `(F064-F066)`, `(F067)` inside the §12.3 decomposition process flow, and `(F146)` in the exercise-gap table on p10. 6 tokens total. | Content pollution (fabricated, non-NCERT text) | `# ---- 12.3 DECOMPOSITION ----` and `# ---- Terms used in the exercises ----` | Row IDs moved out of the strings into adjacent `# F0xx` code comments, so traceability is preserved in the script while the reader-facing text carries only NCERT content. Tagged `# [VERIFICATION FIX]`. |

**Why no linter caught it:** `check_pdf.py`'s checks cover geometry, legibility, glyphs, mono images and label coverage �� none inspect for *extra* text that has no business being in a student-facing document. This is exactly the class §6 reserves for the human pass.

**Post-fix verification:**
- Regex sweep of the rebuilt PDF's text layer: **0 remaining `(Fnnn)` tokens** (was 6).
- `check_pdf.py` re-run on the rebuilt PDF: **still green — 0 fail, 1 benign warn, exit 0**, all of checks 1,2,3,5,6,7,8,9,10 PASS, 67/67 labels still in text, 196/196 rows still ticked.
- Page count unchanged at 10; pages 3 and 10 re-rendered and re-inspected — text reflowed cleanly, no new orphan, no clipping, flow badges still aligned with their vertical rule.

### Accepted WARN — true negative, not a suppressed finding

**Check 4 (no person photograph)** fires on this chapter as a **benign false positive** and is accepted. The heuristic matches the substring `photo` inside ordinary biological vocabulary — `photosynthesis`, `photosynthetically`, `photosynthetic` — in rows F036, F041, F048, F075, F076, F193. NCERT Class 12 Chapter 12 contains **no scientist profile and no portrait at all**, so there is nothing that could have been embedded; the manifest lists 7 diagram figures and zero photographs. Recorded here so a later session does not mistake this true negative for a real finding.

### Pass 3(b) — bidirectional full read: COMPLETE (third independent run, 2026-08-21b)

Both directions re-run from the source PDF in a freshly rebuilt §0 environment (venv `/vercel/share/neetenv`, CPython 3.13.11, reportlab 5.0.1 · pymupdf 1.28.2 · Pillow 12.3.0 — matching the §0.3 reference set). Source re-extracted independently with `pdfplumber` (11 pp / 20,457 chars) and **read start to finish**; the entire 680-line script read start to finish. **Grep/ripgrep/regex was prohibited as evidence for any content claim** this session — machine parsing was confined to structural facts (ID contiguity, tick state, `_extract_labels` counts, hashes, page/char/image counts) and to locating line numbers to read from.

Per-section reading claims (what was read against what):

| Source read | Script block read | Direction 1 | Direction 2 |
|---|---|---|---|
| p1 opener + p2 §12.1 | `# ---- Chapter opener ----`, `# ---- 12.1 ... ----` (L60-172) | F001-F033 + F173/F175 COVERED | clean |
| pp2-3 §12.2 | `# ---- 12.2 PRODUCTIVITY ----` (L175-226) | F034-F053 + F177-F181 COVERED | clean |
| pp3-4 §12.3 | `# ---- 12.3 DECOMPOSITION ----` (L228-313) | F054-F072 + F182-F183 COVERED | clean |
| pp5-7 §12.4 | `# ---- 12.4 ENERGY FLOW ----` (L315-461) | F073-F127 + F184-F186 COVERED; D2's restored phrase re-read in place at L406 | clean |
| pp7-10 §12.5 | `# ---- 12.5 ECOLOGICAL PYRAMIDS ----` (L463-554) | F128-F153 + F196 COVERED | clean |
| p10 Summary (20 sentences) | `# ---- Nutrient cycling ... ----`, `# ---- Quick Recap ----` (L556-607) | F171-F190 COVERED | clean — 8 SUMMARY-UNIQUE folded, nothing summary-only |
| pp10-11 Exercises 1-11 | `# ---- Terms used in the exercises ... ----` (L609-662) | F191-F196 COVERED; D3's fix re-read side by side and confirmed consistent; **D4 FOUND** | clean on content |

- **Direction 2: 0 UNINVENTORIED items** — third independent confirmation. 10/10 heading rows and 6/6 opener rows (F003, F012, F035, F055, F074, F129) re-walked as their own lists; Ch12 is structurally flat with **zero unnumbered sub-headings**, so the Ch9 "D4" failure mode is structurally impossible here.
- **D3 class re-checked by the method that found it.** Every exercise answer read side by side against its appendix row and its frozen row. Ex 1(e) now reads "the atmosphere or hydrosphere (the gaseous-cycle reservoir)" in the exercises list and "atmosphere or hydrosphere" in the appendix table nine lines above — the two agree and both match F189 verbatim; "oceans" is gone. Confirmed by reading the script **and** by viewing rendered page 10.
- **All 7 figures re-opened and viewed directly**, not mode-checked: fig_12_1 (7 labels), fig_12_2 (17, incl. NCERT's uncommaed "Birds, fishes wolf"), fig_12_3 (Sun + 4 trophic captions + every Heat marker), fig_12_4a (3 / 3,54,000 / 708, 000 / 5,842,000), fig_12_4b (1.5 / 11 / 37 / 809), fig_12_4c (PC 21 above P 4, correctly inverted), fig_12_4d (10 / 100 / 1000 / 10,000 J over 1,000,000 J). Every numeric label matches the script's Figure 12.4 table exactly.
- **Reproducibility:** committed PDF + two fresh builds all **10 pp / 25,683 chars / 7 images**, identical text SHA-256 `558e0346fbb82bdf7cfb915f2a75c809a0beca09c6b35921def219d009c48ec5`; two further post-D4 builds reproduce the same hash, proving the D4 fix moved no reader-facing text.

### Confirmed defect (Pass 3(b), this session)

| ID | Defect | Class | Where | Fix |
|---|---|---|---|---|
| **D4** | **The script's `[VERIFICATION FIX]` comments disagreed with the inventory's defect register.** The Ex 1(e) fix was labelled `D2`, but D2 is the separate F114 fix in §12.4; the Ex 1(e) fix is **D3**. The two D1 sites and the D2 site carried no defect ID at all. Per §1 the `.py` is a deliverable whose block markers exist so a later session can jump to a flagged fix — a comment naming the wrong defect sends it to the wrong block. | Deliverable-integrity / audit-trail (script-internal; **never reached the student**) | `# ---- 12.3 DECOMPOSITION ----`, `# ---- 12.4 ENERGY FLOW ----`, `# ---- Terms used in the exercises ... ----` | All four sites now name their defect ID and match the register, with an inline note that the D3 comment previously mislabelled itself as D2. Comment-only: rebuilt text layer is **byte-identical** (same 25,683 chars, same SHA-256), linter green, pages 3 and 10 re-inspected. |

**Why no linter caught it:** comments never enter the PDF text layer, so no `check_pdf.py` check can see them — and no grep for content would flag it either. It was found only by reading the exercises block start to finish, which is also how D3 was found one session earlier in that same block.

---

## Chapter 11 (Class 12) — Organisms and Populations — ✅ FULLY COMPLETE — CLOSED

**Current state (session 5, 2026-08-21): all three gates green, chapter delivered.** 14 pp · 6 mono figures · 268/268 rows ticked · `check_pdf.py` 0 fail / 1 accepted warn across all 9 checks. The Gate 1 narrative below is the historical session-2 record and describes a point where the script and PDF did not yet exist; read it as history, not as current status.

| Gate | Status | Where the evidence lives |
|---|---|---|
| Gate 1 | ✅ GREEN — three independent full re-audits of all 268 rows | inventory `## Gate 1 record` |
| Gate 2 | ✅ GREEN — 0 fail, 1 inspected benign warn (check 4 portrait true negative), checks 1-9 | inventory `## Gate 2 record` |
| Gate 3 | ✅ **PASS — re-earned in session 5.** Session 4 declared PASS while two orphaned headings (D2, D3) were still in the build; that verdict is withdrawn in the record. D2/D3 were fixed in `neet_template.py` (`heading()` now guards with `CondPageBreak`) and are now gated automatically by **`check_pdf.py` check 9**, so the defect class cannot recur silently in any chapter. | inventory `## Gate 3 record` → *Session 5* |

**Cross-chapter effect of this chapter's Gate 3.** Two repo-level improvements came out of it and apply to every future chapter: `neet_template.heading()` can no longer strand a heading at a page foot, and `check_pdf.py` grew **check 9** to gate the orphaned-heading defect class that §6 Pass 3(a) names but nothing enforced.

---

### Historical record — session 2 (Gate 1 only)

Session 2 (2026-08-21) was scoped to **Pass 1 / Gate 1 only, then stop.** Session 1 had already produced an inventory and figure assets and declared Gate 1 GREEN; this session re-audited that claim from the source PDF instead of inheriting it, on the Ch9 precedent that an unverified green gate is how a defective chapter ships. **Pass 2 was deliberately not started** — there is no chapter script and no chapter PDF, so Gate 2 and Gate 3 are explicitly **not** claimed.

| Workflow stage | Evidence |
|----------------|----------|
| **Environment (§0, full redo)** | Sandbox fresh (`.venv` is not persisted by git). Rebuilt with `reportlab 5.0.1` / `pdfplumber` / `pymupdf 1.28.2` / `Pillow 12.3.0`; all four imports verified. |
| **§0.4 smoke test** | Re-run through `neet_template.py` (never re-declaring styles): H1/H2/H3 + section-number badges, `data_table`, `process_flow`, `note`, `memory_aid`, `title_block`, `figure`. 2 pp, both A4 portrait. Smallest rendered glyph **6.0pt** — above the 5.0pt FAIL floor, so badge text and step digits are print-legible (defects 2-3 stay fixed). No footer/header band text (defect 1 stays fixed). 1-bit B&W threshold render confirms NOTE (solid double rule + `!` icon) and MEMORY AID (dashed border + star) remain tell-apart-able with their text labels readable. |
| **Figure-conversion proof** | Deliberately used **Fig 11.5, an originally full-colour photograph**, not a line drawing: embedded as `mode == "L"`, colorspace 1, extrema (0, 255), and the bee still separates cleanly from flower and black background after `convert("L")` + `autocontrast`. |
| **Linter dry-run** | `check_pdf.py` against the throwaway smoke PDF: **exit 0, 0 fail / 0 warn**, checks 4/6/7 correctly SKIP (no inventory passed). Confirms the gate itself runs before the real chapter exists. |
| **Source re-extraction** | Text re-extracted independently with `pdfplumber` (17 pp) to `scratch/ch11op/source_fresh.txt`, read start to finish, then swept a second time source→inventory (heading walk, section-opener walk, sentence walk). |
| **Header-count defect found** | The first freeze's header claimed "232 facts · 6 label rows / 25 labels · 21 heading rows · 14 opener rows". Machine re-count of the table gives **265 rows** (F001-F265 contiguous, 0 duplicates) · **5** label rows / **22** labels · **18** heading rows · **16** opener rows. The table was right and the header was wrong; header corrected in place with the discrepancy recorded rather than quietly overwritten. |
| **3 real Pass 1 gaps found and closed** | **F189a** — the whole NCERT sentence *"Strong and persuasive circumstantial evidence does exist however in some cases."* (11.1.4 (ii)) had no row; without it the Abingdon-tortoise case reads as proof rather than circumstantial evidence. **F030a** — the antecedent *"When you observe nature around you with a scientific frame of mind ... questions of both types"* tying the four embedded questions back to the how-/why-type pair. **F252a** — the Summary-only qualifier **"usually"** in "the growth is usually exponential", where the body states it unhedged. All three logged as genuine Pass 1 gaps, **not back-dated into the freeze**. |
| **Direction-2 discipline** | These were invisible to an inventory→source check by construction: with no row to classify, the sections report clean. Found only by reading the NCERT sentences and asking which row carries each one. |
| **False positives / deliberate non-rows** | 7 candidates examined and rejected with reasons kept in the inventory (transitional filler "Here is an example."; framing clauses "In earlier chapters you have learnt that…", "Whatever might be the ultimate reasons,", "To give you some idea about the magnitude of r values,"; the chess-anecdote narrative restatement of the doubling rule already frozen as F109; the pointer sentence "Here you can see how co-evolution operates."; the `+`/`-` signs on the Fig 11.2 arrows, whose meaning is carried by F080 and Table 11.1's sign convention). |
| **Figure manifest** | All 6 assets re-checked on disk: `fig_11_1`, `fig_11_2`, `fig_11_3`, `fig_11_4a`, `fig_11_4b`, `fig_11_5` — every one `mode == "L"`, extrema (0, 255), and each visually confirmed to be the figure its caption claims. In-figure labels re-read off the images themselves (Fig 11.1 → 6, Fig 11.2 → 5, Fig 11.3 → 7; Fig 11.4a/b and 11.5 carry no in-figure text). **No figure requires manual attention.** |
| **Label-matrix parse check** | Ran `check_pdf.py`'s own `_extract_labels` against the inventory: all **22** labels parse off all **5** rows, no markdown header row mis-parsed as data — so Pass 2's check 6 will audit real labels. |
| **Portrait rule (check 4)** | The source's one photograph of a person — the RAMDEO MISRA headshot (p2) — is deliberately not extracted; the profile is text-only (F011-F022) and no portrait asset exists in `assets/`. Recorded as a true negative, not a suppressed finding. |
| **Gate 1 status** | ✅ **GREEN — re-earned on this session's evidence** (independent source re-read + machine re-count + 3 gaps closed), not inherited from session 1's claim. |
| **Gate 2 / Gate 3 status** *(as at session 2)* | ⬜ Not started, not claimed at that time. Pass 2 was to begin by writing `Ch11_OrganismsAndPopulations.py` linearly from the 268-row frozen inventory, importing `neet_template.py`, ticking rows as each `# ---- N.N ----` block was written, then looping render→`check_pdf.py` until green. **Both gates are now GREEN — see the current-state table above.** |
| **Deliverables** *(as at session 2)* | `Ch11_OrganismsAndPopulations_inventory.md` · `assets/` (6 verified monochrome PNGs) · `extract_figures.py`. The PDF and script were still owed at that point; **both now exist and are delivered.** |

**Chapter shape (for Pass 2).** Post-rationalisation NCERT Ch11 has a single numbered section, **11.1 Populations**, with 11.1.1 Population Attributes, 11.1.2 Population Growth (Growth Models → (i) Exponential growth, (ii) Logistic growth), 11.1.3 Life History Variation and 11.1.4 Population Interactions ((i) Predation … (v) Mutualism), plus Table 11.1, 6 figures, Summary and 10 exercises. The old "Organism and Its Environment" half of the chapter is **not** in this edition and must not be written in from memory (Rule 5).

**Exercise gaps to close in Pass 2 (Rule 2).** (1) **community** — Exercise 6 says "Define population and community" but the body never defines community explicitly; (2) **solving r from a doubling time** — Exercise 2 needs `N_t = N_0 e^rt` rearranged, which the chapter never demonstrates. Both are planned for the *Terms used in the exercises* appendix, built only from the chapter's own statements.

---

## Chapter 1 — The Living World — ✅ FULLY COMPLETE (CLOSED)

Verified closed under the supreme command prompt v6 on 2026-08-19. Resumed mid-workflow after a prior run stopped on credit limit immediately after "Task 0 of 5 complete"; Gate 1 was independently re-verified against current files (not trusted from prior-run prose) before Pass 2 began. Pass 3 (dual verification) run to Gate 3 closure in this cycle.

| Workflow stage | Evidence |
|----------------|----------|
| **Environment resume** | Sandbox was fresh (venv not persisted by git); rebuilt `.venv` with `reportlab 5.0.0` / `pymupdf 1.28.2` / `Pillow 12.3.0`. Not a §0 restart — no re-extraction, no re-render of source, no Fig 1.1 re-extraction; all reused as-is. |
| **Gate 1 (independently confirmed)** | 121 Facts rows (F001-F121) contiguous, all ticked, header `Rows: 121` matches; all 6 required inventory sections present; figure-label matrix present and parses to 7/7 real labels incl. exact phrase "Phylum or Division"; summary classification (10 rows, 3 SUMMARY-UNIQUE); exercise-gap terms (10 rows, Q1-Q10); figure manifest (1 row, `Mono: yes`, `Verified: yes`); `assets/fig_1_1.png` on disk, `mode=L`, 0 colored px; `git status`/`git diff HEAD` clean on the Ch1 folder — no corruption since prior run. |
| **Pass 2 / Gate 2** | `check_pdf.py` → **VERDICT WARN, 0 fail / 1 warn, exit 0**. Checks 1,2,3,5,6,7,8 PASS. 7 × A4-portrait pages, **1 monochrome image** (Fig 1.1), all 121 rows ticked, 14/14 figure labels covered in running text. |
| **The 1 warning** | Check 4 photo-keyword heuristic — a **confirmed benign false positive**. Fires on the 10 Ernst Mayr biography rows (F013-F022) because their inventory `Type` column is literally "Profile", and `"profile"` is one of the checker's `PORTRAIT_HINTS` substrings. Independently confirmed only 1 image is embedded in the whole PDF (Fig 1.1, colorspace=1/gray) — the Mayr content is a text-only block, no photograph. |
| **Fixes applied** | (1) Removed an invented figure-caption sentence ("Reading the arrows upward…") not present in NCERT; restored exact frozen F108 wording. (2) Replaced a `process_flow` misuse for the taxonomic hierarchy (falsely implying species→kingdom is a sequential procedure) with a rank `data_table`, ascending order matching Fig 1.1, no new facts introduced. (3) Dropped the decorative `has_table=True` flag on two headings — the frozen template renders its table-icon cell outside the banner fill, leaving a stray glyph; omitted at chapter level rather than patching the frozen module. (4) Wrapped Table 1.1's intro line + 4 rows + reading NOTE in `KeepTogether` to fix an orphan-row page split. |
| **False positives correctly rejected** | Check 6's apparent "2 missing labels" traced to `_extract_labels` mis-parsing the Figure-label matrix's own markdown header row as data; fixed by rewording only the column caption (no fact/label/tick touched). The "two implicit questions" `process_flow` at 1.1 was left as-is — the source itself says "the first… the second", so numbering is source-grounded. |
| **Colour-dependent figures** | Fig 1.1 is a monochrome hierarchy diagram with no colour-encoded meaning; verified legible at 1-bit B&W threshold render. |
| **Deliverables** | `Ch1_TheLivingWorld.pdf` · `.py` · `_inventory.md` (with figure-label matrix) · `assets/fig_1_1.png`. |
| **Pass 3 / Gate 3** | **Zero confirmed defects.** Pass 3(a): all 7 pages rendered + inspected; cross-page style identity confirmed (template held). **One layout defect found and fixed** — the `Table 1.1` banner was orphaned at the bottom of page 5, split from its table; fixed by folding `heading("Table 1.1", …)` into the table's `KeepTogether` (`# [VERIFICATION FIX]`). Pass 3(b): full-read cross-check of all 121 rows against source → **121/121 COVERED, 0 MISSING / 0 FABRICATED / 0 DRIFTED**; every number/date verbatim; Fig 1.1's 7 labels all in text. `check_pdf.py` re-run after the fix: still **0 fail / 1 benign warn, exit 0**, 7 pp. |

---

## Chapter 2 — Biological Classification — ✅ FULLY COMPLETE (CLOSED)

Pass 2 verification completed under the supreme command prompt v6 on 2026-08-19. Pass 3(a) — the visual render check — completed on 2026-08-20. **Pass 3(b) — the full-read content cross-check �� completed 2026-08-20, Gate 3 closed the same day.** The sandbox was fresh at the start of the Pass 3(b) session (venv is not persisted by git); `.venv` was rebuilt with `reportlab 5.0.0` / `pymupdf 1.28.2` / `Pillow 12.3.0`. Not a §0 restart — no re-extraction of figures, no re-render of source assets; Gate 2 was re-confirmed green on the existing PDF before Pass 3(b) began.

| Workflow stage | Evidence |
|----------------|----------|
| **Pass 2 / Gate 2** | `check_pdf.py` → **VERDICT WARN, 0 fail / 1 warn, exit 0**. Checks 1, 2, 3, 5, 6, 7, 8 PASS. 15 × A4-portrait pages, **6 monochrome images**, all 192 Facts rows ticked. Gate 2 re-confirmed green on 2026-08-20 before Pass 3 began (smallest text 6.0pt; 26/26 labels in text; all pages A4 portrait). |
| **The 1 warning** | Check 4 photo-keyword heuristic — a **confirmed benign false positive**. It matches “photo” inside textual mentions of photosynthetic biology terms/profile hints (F007, F032, F041, F048, F064, F070, F076, F085); no scientist portrait is embedded. Check 3 independently confirms the 6 embedded images are the required monochrome biology figures. |
| **Pass 3(a) — visual render check** | **PASS — zero layout defects.** All 15 pages rendered with pymupdf at actual size (150 dpi) and at a 1-bit B&W print-safety threshold, each inspected directly. Verified: no overflow/clipping, no orphaned headings, no squashed figures, no table running off the page, process-flow rules aligned with their triangle badges (fungal sexual cycle p8; virus discovery p12), figures boxed with captions, NOTE (solid rule + `!`) vs MEMORY AID (dashed border + star) tell-apart-able in B&W. Cross-page style identity confirmed by comparing H1/H2/H3 banners, tables, boxes and figure boxes from ≥3 points in the chapter — **template held, no drift**. |
| **Badge micro-text reviewed, not a defect** | The `Quick Recap` and `Terms Used in the Exercises` headings feed text labels (`"Recap"`, `"Appendix"`) into the section-number badge. `neet_template._badge_section` grows the plate sideways to fit at a ≥6pt floor (never clips), and the closed, Gate-3-passed Ch1 uses the identical `heading("Recap", …)` / `heading("Appendix", …)` calls. Kept as-is for cross-chapter consistency (check 2 passed at 6.0pt smallest). |
| **Source watermark** | Faint NCERT "not to be republished" watermark is visible on some extracted figures (Fig 2.1, 2.2, 2.4, 2.6). It originates in the source PDF, not the layout. Flagged for optional cleanup during Pass 3(b); not a layout defect. |
| **Genuine layout defects** | **None.** No `# [VERIFICATION FIX]` edit to the `.py` was required by Pass 3(a). |
| **Pass 3(b) — content cross-check** | **Complete.** Fresh `pdfplumber` re-extraction of all 13 source pages, read start-to-finish (not grepped) against all **192 Facts rows** and all **6 figure-label rows**, section pair by section pair. Result after fixes: **192/192 COVERED, 0 MISSING / 0 FABRICATED / 0 DRIFTED**; every scientist, date, taxon name, qualifier and Table 2.1 cell verbatim; 26/26 in-figure labels found in running text at their own topic. Figure-label rows L01-L06, previously unticked, are now ticked in the inventory (Gate 2's check 7 only gates Facts rows, so they had never been machine-gated). |
| **Defect 1 found + fixed (F169, MISSING)** | The 2.6a virus-discovery process flow gave Beijerinck only "called the fluid as *Contagium vivum fluidum*" and had dropped NCERT's "**named the new pathogen 'virus'**" — the naming of the virus itself, a directly examinable fact. Restored verbatim ahead of the fluid name (`# [VERIFICATION FIX]`). |
| **Defect 2 found + fixed (F163, DRIFTED)** | The 2.6 opening had been reflowed to "no mention of some acellular organisms like viruses and viroids, prions and lichens", which reads **lichens as acellular**. NCERT groups them differently: "no mention of **lichens** *and* some acellular organisms like viruses, viroids and prions" — a lichen is a *cellular* alga+fungus association, excluded from the five kingdoms for a different reason. Original grouping restored plus one explicit clarifying clause (`# [VERIFICATION FIX]`). |
| **Re-verification after the fixes** | PDF regenerated; both fixes land on rendered page 12, which was re-rendered and inspected: text correct, process-flow badges 1/2/3 still aligned to their rules, no orphan/overflow introduced, page count unchanged at 15. `check_pdf.py` re-run: still **0 fail / 1 benign warn, exit 0**. Only the fixed block was re-verified — nothing else changed. |
| **Documentary fix** | The script's source-spelling policy header listed 2 normalised source typos; the full read turned up a third — "Multiceullar" in the Table 2.1 body-organisation row for Fungi (normalised to "Multicellular" in the table). Added to the policy header and to the inventory's Source problems, so all three normalisations are recorded rather than silent. |
| **Source watermark — accepted, not a defect** | Faint NCERT "not to be republished" watermark in the source artwork of Fig 2.1, 2.2, 2.4, 2.6. Re-examined during Pass 3(b) as flagged: it carries no meaning and obscures no in-figure label (all 26 read cleanly), so it was left as-is — retouching an original NCERT figure is a larger risk than the watermark. |
| **Deliverables** | `Ch2_BiologicalClassification.pdf` (15 pp) · `.py` · `_inventory.md` (figure-label matrix ticked + section-wise coverage + Gate 3 closure + Coverage note) · `assets/` (6 mono PNGs). |
| **Pass 3 / Gate 3** | **Zero confirmed defects remain AND `check_pdf.py` green.** Gate 3 closed 2026-08-20 — chapter delivered. |

---

## Chapter 3 — Plant Kingdom — ✅ FULLY COMPLETE (CLOSED)

**Gate 3 CLOSED on 2026-08-20.** Pass 3 was run to completion on the Gate-2-green 10-page PDF: Pass 3(a) inspected all 10 rendered pages (zero layout defects, cross-page style identity confirmed) and Pass 3(b) full-read cross-checked all 215 Facts rows and 11 figure-label rows against a fresh `pdfplumber` re-extraction of the 14 source pages → **215/215 COVERED, 0 MISSING / 0 FABRICATED / 0 DRIFTED**. No `# [VERIFICATION FIX]` edit was needed, so the PDF was not rebuilt and `check_pdf.py` stayed green (0 fail / 1 benign warn, exit 0). All 215 Facts rows and 11 label rows remain ticked.

> **Correction notice (Gate-2 closure).** The Gate-2 figures first recorded for this chapter were overstated and have been replaced with independently re-verified values. `55/55 labels` counted 20 synthetic panel/part markers (`(a)`, `(b)`, …) as in-figure labels; panel markers are **not** labels, so the audited count is **35 genuine label occurrences / 30 distinct strings** across the 11 matrix rows, with the markers held in a separate unaudited column. `11 pages` / `11 A4 pages` is stale rather than a miscount: the PDF **genuinely was 11 pages** when that figure was first written, and became **10 pages** later in the same PR when the orphan fix at `Ch3_PlantKingdom.py:204` was applied (see the row below) — the prose was simply never updated to follow the rebuild. The current and correct count is **10 pages**. No fact, label, tick, asset or line of `Ch3_PlantKingdom.py` was changed to reconcile these numbers — only the claims were corrected to match the artefact.

> **Self-correction (supersedes the line above as first published).** The Gate-2 closure notice originally asserted that `11 pages` "was an off-by-one carried from the 11-asset count" and that the PDF "is and always was 10 pages." **That was wrong**, and it is retracted here. Direct inspection of the committed artefacts proves the page count really did change: `git show e304ee13^` renders **11 pages** and `git show e304ee13` renders **10 pages**. The coincidence with the 11-asset count was misleading, not causal.

| Workflow stage | Evidence |
|----------------|----------|
| **Environment** | Rebuilt `/vercel/share/venv` with Python 3.13, `reportlab 5.0.0`, `pdfplumber 0.11.10`, `pymupdf 1.28.2`, and `Pillow 12.3.0`. |
| **Pass 1 / Gate 1** | Previously passed and re-confirmed: **215** consecutive Facts rows (F001-F215, all ticked), **11 figure-label rows (L01-L11) carrying 35 genuine in-figure label occurrences / 30 distinct strings**, **11/11** verified monochrome assets, 6 summary-unique folds, and 5 exercise gaps with planned appendix homes. Panel/part markers are recorded separately and are deliberately **excluded** from the audited label count. |
| **Pass 2 build** | Created `Ch3_PlantKingdom.py` and generated `Ch3_PlantKingdom.pdf` (**10 A4 portrait pages** at 595x842pt, **11 embedded monochrome figures** — 11 assets across 10 pages, not one per page). The script imports the frozen template, uses section markers, embeds figures inline, preserves Table 3.1's verified geometry-derived cells, and closes all 5 exercise gaps in the appendix. |
| **Gate 2 linter** | `check_pdf.py "notes/class 11/Ch3_PlantKingdom"` returns **VERDICT WARN, 0 fail / 1 warn, exit 0** — Gate 2 green. Checks 1, 2, 3, 5, 6, 7 and 8 PASS. Header reports **10 pages**; figure-label coverage is **35/35 fully in text (0 partial, 0 missing)**; inventory coverage is **215/215 Facts rows ticked**; check 3 confirms **all 11 embedded images monochrome**; check 8 confirms **all 10 pages A4 portrait**; check 2 smallest rendered text 6.0pt. |
| **Gate 2 closure re-verification (2026-08-20)** | Re-run from a fresh sandbox (venv is not persisted by git; rebuilt at the same pinned versions). Reproduced independently of the prior run: **10 pages**, all A4 portrait 595x842pt, 11 embedded images, 11/11 assets `mode=L`, **35/35** labels, **215/215** rows, **0 fail / 1 warn, exit 0**. `check_pdf.py --strict` reports the identical **0 fail / 1 warn** body and exits **1** *by design* — `--strict` promotes any WARN to a non-zero exit (`if n_fail or (args.strict and n_warn)`, and the script's own header states "Exit code … 1 = at least one FAIL (or WARN under `--strict`)"). This is the documented strict-mode contract for the one accepted benign warning, **not** a new defect and not a Gate-2 failure; Gate 2 is judged on the normal run's exit 0 per §6. |
| **The 1 warning — inspected, confirmed benign** | Check 4's manifest keyword heuristic (`PORTRAIT_HINTS = portrait, photo, photograph, headshot, profile`) fires on 9 rows by pure substring match, none of which is a person: **F038, F039, F102, F146** contain `photosynthesis`/`photosynthetic`/`photosynthetic gametophyte` (the `photo` substring); **F067** matches on `the stalk, the …` wording; and label rows **L07, L08, L09, L11** literally read "habit **photograph**" because those panels are plant-habit artwork with no anatomical labels. Independently confirmed benign: all 11 embedded images are plant diagrams or plant-habit figures (check 3 verifies each is monochrome), and **no person photograph is embedded anywhere in this chapter**. Same known false-positive class already accepted at Gate 3 on the closed Ch1, Ch2 and Ch8. |
| **`# [VERIFICATION FIX]` marker at `Ch3_PlantKingdom.py:204` — investigated** | This chapter's script carries one `# [VERIFICATION FIX]` comment, which reads as Pass-3 vocabulary and appeared to contradict "Pass 3 has not begun." Investigated by `git blame`: the line comes from commit **`e304ee13`** ("refactor: adjust PDF content references and metadata", landed in **PR #39**, during **Pass 2**) — not from the original build `ec5a7978`, and not from any Pass-3 activity. It is a **Pass-2 layout fix that borrowed the Pass-3 tag wording**. The fix itself is genuine and load-bearing: it relocates the *Diplontic life cycle* exercise-gap definition into the §3.5 angiosperm topic. Verified against the committed artefacts — before the fix the final page held **only that single orphaned bullet (272 characters of text)**; after it, that page is gone and the last page carries 2,361 characters. **This is the exact commit that took the PDF from 11 to 10 pages**, and it is the true origin of the stale `11 pages` claim. Conclusion: the marker is correctly placed and nothing about it needs changing; only the misleading impression it created is resolved here. Per instruction, the tag wording was **left untouched** (retagging would edit the script, force a PDF rebuild, and needlessly re-open Gate 2). |
| **Asset fix carried in** | `fig_3_4c` had a grey photocopier-plate cast from the source scan; corrected with `white_point=180` in the §4.4 pipeline (asset extrema now `(0, 254)`). The other **10** assets were left byte-identical — verified unchanged, not regenerated. |
| **Deliverables** | `Ch3_PlantKingdom.py` · `Ch3_PlantKingdom.pdf` (10 pp) · `Ch3_PlantKingdom_inventory.md` (with figure-label matrix) · `assets/` (11 verified monochrome PNGs) · `extract_figures.py`. **No cache or scratch artefacts in the chapter folder** — the tracked contents are exactly these items. |
| **Repo hygiene** | Removed two generated Python bytecode files committed by mistake: `__pycache__/check_pdf.cpython-313.pyc` and `__pycache__/neet_template.cpython-313.pyc`. Neither is a deliverable under §0.5. `.gitignore` now excludes `__pycache__/`, `*.py[cod]` and the venv so they cannot be recommitted. |
| **Pass 3(a) — visual render check** | **PASS — zero layout defects.** All 10 pages rendered with pymupdf at 150 dpi and inspected directly. No overflow/clipping, no orphaned headings, no squashed figures; Table 3.1 splits pages 3→4 with its header repeated; every process-flow triangle badge (algae reproduction p1, bryophyte life cycle p4, pteridophyte life cycle p6, gymnosperm fertilisation p8) aligned to its vertical rule; all 11 figures boxed with italic captions. Cross-page style identity confirmed from ≥3 points — template held, no drift. NOTE (solid rule + `!`) vs MEMORY AID (dashed border + star) tell-apart-able in B&W by border style. NCERT "not to be republished" source watermark re-examined: obscures no label (35/35 read cleanly), left as-is. |
| **Pass 3(b) — content cross-check** | **Complete.** Parallel subagents unavailable, so the section-pair cross-check was done sequentially by full read (not grep): fresh pdfplumber re-extraction of all 14 source pages read start-to-finish against all **215 Facts rows** and **11 figure-label rows**. Result: **215/215 COVERED, 0 MISSING / 0 FABRICATED / 0 DRIFTED**; every scientist (Whittaker, Linnaeus, Bentham, Hooker), date (1969), number (70 species, 100 m, "at least half"), taxon name and qualifier verbatim; Table 3.1's 21 cells match printed NCERT wording; gymnosperm 5-step fertilisation in source order; 35/35 in-figure labels in running text. Recap Q4 ploidy (8/8), Q10 matches (4/4) and Q5/Q8 exercise-gap additions re-verified correct and correctly flagged as background not in the NCERT body. |
| **Drift caught and fixed** | **None.** No `# [VERIFICATION FIX]` edit required by Pass 3 — a clean foundation from a thorough Pass 1. The single pre-existing `# [VERIFICATION FIX]` at `Ch3_PlantKingdom.py:204` is the Pass-2 orphan fix documented above; Pass 3 did not touch it. Because nothing changed, the PDF was not rebuilt. |
| **Pass 3 / Gate 3** | **Zero confirmed defects remain AND `check_pdf.py` still green** (0 fail / 1 inspected-benign warn, exit 0, 10 pp). Gate 3 closed 2026-08-20 — chapter delivered. The 1 warning is check 4's photo-keyword false positive (`photosynthesis`/`photosynthetic` rows + "habit photograph" label rows); 11/11 embedded images are plant figures, no person photograph in the chapter. |
| **Deliverables (Gate 3)** | `Ch3_PlantKingdom.pdf` (10 pp) · `Ch3_PlantKingdom.py` · `Ch3_PlantKingdom_inventory.md` (figure-label matrix + Pass 3 closure + Coverage note) · `assets/` (11 verified monochrome PNGs). |

---

## Chapter 8 — Cell: The Unit of Life — ✅ FULLY COMPLETE (CLOSED)

Big-chapter 5-pass protocol (`1a → 1b → [Gate 1] → 2a → 2b → [Gate 2] → 3 → [Gate 3]`). Verified closed under the supreme command prompt on 2026-08-19.

| Workflow stage | Evidence |
|----------------|----------|
| **Pass 1 / Gate 1** | Frozen inventory of **325 Facts rows** (F001-F325) inventoried in two halves into one file; figure manifest + figure-label matrix (**82/82** in-figure labels present in running text); **6 summary-unique** facts folded into their named body sections (F308, F310, F312, F314, F317, F319); 2 exercise gaps closed from chapter facts (Q9 division of labour, Q13 centrosome). |
| **Pass 2 / Gate 2** | `check_pdf.py` → **VERDICT WARN, 0 fail / 1 warn, exit 0**. Checks 1,2,3,5,6,7,8 PASS. 18 × A4-portrait pages, **14 monochrome images** (13 figures, Fig 8.3 split a/b), all 325 rows ticked. |
| **The 1 warning** | Check 4 photo-keyword heuristic — a **confirmed benign false positive**. Fires only on the `phot`+`o` substring inside **photosynthetic** (F112) and **photosynthesis** (F213); both are running-text biology rows, neither a figure-manifest row nor a person image. No person photograph embedded; the G.N. Ramachandran biography is a text-only `[NOTE]` box. |
| **Pass 3 / Gate 3** | **Zero confirmed defects.** Pass 3(a): all 18 pages rendered actual-size + print-DPI B&W 1-bit, zero layout defects, cross-page style identity confirmed (template held). Pass 3(b): fresh source re-extraction full-read against all 325 rows → **325/325 COVERED, 0 MISSING / 0 FABRICATED / 0 DRIFTED**; every qualifier, number and µm value preserved verbatim. Historical page-16 Fig 8.13 "overlap" re-confirmed as a false positive on the fresh render. No `# [VERIFICATION FIX]` edit needed. |
| **Colour-dependent figures** | Fig 8.4 (fluid mosaic) and Fig 8.10(b) (cilia/flagella): each caption restates every part by position/shape/geometry so the distinction survives monochrome. |
| **Source problems** | Micron glyph rendered `mm` by pdfplumber (re-read from render as µm); "endoplasmic reticulun" typo; body-vs-summary plastid-location contradiction (body kept, flagged); "Central microtuble" label typo (preserved verbatim). All handled, none unrecoverable. |
| **Deliverables** | `Ch8_CellTheUnitOfLife.pdf` �� `.py` · `_inventory.md` (with figure-label matrix + Gate 3 closure + Coverage note) · `assets/` (14 mono PNGs). |

---

## Chapter 9 (Class 11) — Biomolecules — ✅ FULLY COMPLETE (CLOSED)

**Gate 3 CLOSED on 2026-08-21 — after a THIRD audit that completed Pass 3(b) as a true full read and found 3 more real defects.** This chapter has now been closed-and-reopened twice, and both reopenings had the same root cause: **Pass 3(b) was signed off on a token-coverage screen instead of the full source-vs-script read v6 mandates.** The first reopening found the mandatory Quick Recap missing and a stale Pass 1 tick-semantics note in the inventory. This one — the first time the 16-page source was actually re-read start to finish against every script block — found **D9 (a dropped NCERT antecedent sentence in 9.8.2)** and **D4 (both of NCERT 9.8.4's sub-headings missing)**, neither of which a token screen can see, plus it verified the D6 heading fix in the rendered PDF and finished the D5 narration sweep. All fixed; Gate 3 is now genuinely earned on full-read evidence. Pass 1, Pass 2 and figure extraction were **not** restarted — no defect was found in them; the 15 frozen assets were reused as-is after re-verification.

**Final tally for this audit: 3 confirmed defects found and fixed — D9 (MISSING, 1 row), D4 (MISSING, 2 rows) and D6 (DRIFTED, heading).** These are counted separately from the earlier sweeps' fixes (the missing Quick Recap, the F269/F270/F271 summary-unique folds and the D5 narration cleanup), which are retained and re-verified rather than re-counted. The inventory grew **277 → 280 rows** because D9 and D4 each needed a row that the freeze never had. **D1, D2, D3 and D9's trailing active-site clause are false positives / user-rejected items and were deliberately NOT restored** — that distinction is preserved throughout this record and in the inventory. Gate 3 = **PASS**, 0 defects remaining.

> **Audit lesson (second occurrence in this repo).** Two separate sessions closed this chapter while Pass 3(b) had never been done as a full read. `check_pdf.py --strict` green + 276/277 rows at ≥78% token coverage passed a chapter that was still missing a whole NCERT sentence and two NCERT sub-headings. **A green linter and a token screen are Gate 2 evidence, never Gate 3 evidence.** Gate 3 requires the full read, and a MISSING heading or a dropped antecedent clause is invisible to token matching by construction.

| Workflow stage | Evidence |
|----------------|----------|
| **Environment resume** | Sandbox was fresh (venv not persisted by git). Rebuilt into `/vercel/share/venv` with `uv` — `reportlab 5.0.0` / `pymupdf 1.26.7` / `Pillow 12.4.0` / `pdfplumber`. Note: bare `pip` in this sandbox is a `uv` shim and rejects `--break-system-packages`; use `uv venv` + `uv pip install --python <venv>`. Not a §0 restart. |
| **Gate 1 (independently confirmed)** | Frozen at 277 Facts rows (F001-F277), **now 280 after the Pass 3(b) full read added F194a, F221a and F225a** for NCERT content the freeze had genuinely missed — suffixed IDs keep the original numbering and every existing cross-reference stable. All 280 ticked (machine-confirmed by check 7). 15 label rows (L01-L15) carrying 49 in-figure labels (48 distinct); figure manifest 15/15 with `Mono: yes` / `Verified: yes`; 11/11 exercise-gap rows; 28/28 summary sentences classified (15 BODY-PRESENT, 13 SUMMARY-UNIQUE); inventory in the correct folder per §0.5. The three added rows are recorded as a **real Pass 1 gap**, not back-dated into the freeze. |
| **Reproducibility** | The committed PDF was copied aside and `Ch9_Biomolecules.py` re-run from scratch: regenerated PDF is **text-identical** to the committed one — same 15 pages, same extracted character count, same 15 embedded images. The script is genuinely the single source of truth. |
| **Pass 2 / Gate 2** | `check_pdf.py "notes/class 11/Ch9_Biomolecules" --strict` → **VERDICT PASS, 0 fail / 0 warn**. All 8 checks PASS: no footer/header band · smallest text 6.0pt · 15/15 monochrome images · no person photograph · no banned glyphs · 49/49 labels in running text · all rows ticked (277 at the time of this run, **280 after the Pass 3(b) additions** — see the final Gate 2 row below) · 15/15 pages A4 portrait. Notably this chapter has **no benign warn** — check 4 does not fire here, unlike Ch1/Ch2/Ch3/Ch8; that is a true negative, not a suppressed finding. |
| **Assets** | 15/15 PNGs re-probed this session: every one `mode == "L"` with extrema `(0, 255)` — true monochrome, no colour channel. No person photograph. |
| **Pass 3(a) — visual render check** | **PASS (re-run in full after the final edits).** All **15/15** pages rendered at 110 dpi and inspected individually — not a sample. Verified: no footer/header, H1/H2/H3 banners consistent across the chapter, Table 9.5 correctly repeats its header row across the p6→p7 break, figures boxed with verbatim captions and correct aspect, the 6-class enzyme table intact, process-flow badges 1-4 legible and aligned to the rule, MEMORY AID / NOTE blocks distinguishable, no orphaned headings, no clipping or overflow, no accidental artifacts. Pages affected by reflow from the D4/D9 edits (10, 12, 13) re-inspected specifically. Print-safety re-checked by 1-bit threshold render of pp. 9, 12, 13: both new H3 banners stay solid black with legible white text. |
| **Pass 3(b) — content verification (true full read)** | **Re-done as a genuine full read of source + inventory + script — the earlier token screen was NOT Gate 3 evidence.** The 16-page source PDF was re-extracted fresh and read start to finish (all sections, summary and all 11 exercises), then cross-read against every script block and every inventory row. This found **3 further real defects** (below) that the prior session's ≥78% token screen had passed, exactly as the Ch10 lesson predicts: a dropped antecedent sentence and two missing sub-headings all leave token overlap high. Figure rows re-verified row-by-row: 15/15 manifest rows correct source page, `Mono: yes`/`Verified: yes`, correct asset, caption number/text correct, and each figure placed at its own topic (9.1 panels in 9.1, Fig 9.2 in 9.5, 9.3 panels in 9.7, Fig 9.4 in 9.8.2, 9.5 panels in 9.8.4). |
| **Defects found and fixed (this session, Pass 3(b) full read)** | **1. D9 MISSING (9.8.2).** NCERT: *"The chemical or metabolic conversion refers to a reaction."* The block opened straight at the substrate definition, leaving "conversion" — the word in its own heading — undefined. Restored as new inventory row **F194a**. **2. D4 MISSING (9.8.4), two rows.** NCERT 9.8.4 carries two sub-headings, *"Temperature and pH"* and *"Concentration of Substrate"*; the content was present but both headings were absent, so 9.8.4's sub-structure was untraceable against the book (§3 traceability). Restored as H3 blocks, new rows **F221a** and **F225a**. **3. D6 DRIFTED (9.8.2 heading)** — fixed earlier in the audit and **verified in the rendered PDF this session**: reads *"How do Enzymes bring about such High Rates of Chemical Conversions?"* verbatim. All fixes tagged `# [VERIFICATION FIX]`, PDF rebuilt, page count **unchanged at 15**, and `--strict` re-run **0 fail / 0 warn** with **280/280** rows ticked. |
| **D5 summary-meta narration — sweep completed** | Full contextual sweep finished. Gratuitous provenance narration ("the summary fixes the counts", "as the summary states it", "taking the summary's fuller list", "the mention of fungi comes from the summary", "the summary generalises…") is **fully removed** — those facts now read as plain content (e.g. the fungi cell-wall fact, the oxygen fact, F271). The **only** surviving attributions are the two carrying NCERT's genuine three-vs-four macromolecule conflict (9.3 NOTE box and Appendix Q1), which **Rule 4 requires** be kept attributed. Verified: *"In summary, if we represent the chemical composition…"* in 9.3 is **NCERT's own verbatim phrase**, not narration — retained correctly. |
| **User-rejected items (not restored)** | D1 ("You have learnt in chemistry…"), D2 ("two types" classification), D3 (Chapter 12 respiration cross-reference) and D9's *"We have already understood the idea of an 'active site'"* clause were explicitly rejected by the user. The fresh full read **confirms** these decisions do not conflict with the source or inventory requirements: each is scene-setting, a forward/backward cross-reference, or a restatement of a fact already carried (the active-site definition is present verbatim in 9.8 as F165). Not restored. |
| **Earlier defects (prior sessions, retained)** | The `# [VERIFICATION FIX]` blocks from the earlier sweeps — the missing Quick Recap (Content Order item 8), the summary-unique folds F269/F270/F271, and the D5/D6 corrections — are all present and intact in the script. |
| **Documentation defect fixed** | The inventory's stale Pass 1 tick-semantics note was replaced with a real **Pass 2 / Pass 3 verification record** carrying the reproducibility result, the per-row coverage statistics, the Gate 2 strict verdict, the Gate 3 findings table and an explicit Coverage note — so `check_pdf.py` check 7 is no longer passing against ticks that were merely aspirational. |
| **Source problems** | §9.3 forward-references a non-existent "section 9.10" (chapter ends at 9.8) — recorded in F073, cross-reference dropped rather than repointed. NCERT typos carried in the inventory and normalised only in the rewritten body: "bound to the the enzyme" (F244), "cordination" (F252), "physiologial" (F076). F044's `A`/`B`/`C` protonation scheme has no figure number and is carried as text. NCERT's own three-vs-four macromolecule tension (F080 vs F272) preserved on both sides, resolved by F089 "Lipids are not strictly macromolecules". |
| **Deliverables** | `Ch9_Biomolecules.pdf` (15 pp, 765 KB) · `Ch9_Biomolecules.py` (826 lines, 10 `# [VERIFICATION FIX]` sites) · `Ch9_Biomolecules_inventory.md` (280 Facts rows + figure-label matrix + amendment note + Pass 2/Pass 3 verification record + Gate 2 strict verdict + Gate 3 defect table + false-positive record + Coverage note) · `assets/` (15 mono PNGs). All four committed together with this status file. |
| **Gate 2 (final, re-run at closure)** | `check_pdf.py "notes/class 11/Ch9_Biomolecules" --strict` → **VERDICT PASS, 0 fail / 0 warn, exit 0**, all 8 checks PASS, **280/280** Facts rows ticked, 49/49 labels in text, 15/15 mono images, 15/15 pages A4 portrait. Not carried forward from an earlier session — re-run against the final rebuilt PDF in a freshly rebuilt venv (`reportlab 5.0.1` / `pymupdf 1.28.2` / `Pillow 12.3.0`). |
| **Final consistency check** | Committed PDF copied aside and the script re-run: regenerated PDF **text-identical** (15 pages, 39,315 extracted characters, 15 embedded images both). All four fix sites confirmed present exactly once in the final text layer: the D9 antecedent, both D4 sub-headings, the corrected D6 heading and the Quick Recap. Inventory row count reconciled to the artefact (header previously read `Rows: 292 (F001-F277)`; corrected to 295 total / 280 Facts) — **the only inconsistency found, and it was a stale claim, not a content error**. No fact, tick, label or line of the script was changed to reconcile it. |
| **Gate 3 (final)** | **PASS — genuinely satisfied on full-read evidence.** 3 confirmed defects found and fixed in this audit (D9, D4 ×2, D6); 0 remaining. `check_pdf.py` still green under `--strict`. Chapter closed. |

---

## Chapter 10 (Class 11) — Cell Cycle and Cell Division — ✅ FULLY COMPLETE (CLOSED)

**Gate 3 CLOSED on 2026-08-20 — after a verification re-run that found and fixed 3 real defects.** The chapter had previously been marked "FULLY COMPLETE" while Pass 3(b) was in fact incomplete: that session ended on credit limit at the documentation step, and its zero-defect claim came from a token screen rather than the full read v6 requires. A follow-up session re-opened the chapter, re-ran Pass 3(b) properly, found **2 DRIFTED + 1 MISSING**, fixed them via `# [VERIFICATION FIX]` blocks, rebuilt the PDF and re-confirmed Gate 2. **Gate 3 is now genuinely satisfied.** Pass 1, figure extraction and Pass 2 were **not** restarted — no real defect was found in them; the 8 frozen assets were reused as-is after re-verification.

> **Audit lesson recorded.** A green `check_pdf.py` plus a ≥85% token-coverage screen passed this chapter with 3 defects still in it. Both defects of the DRIFTED class were invisible to the mechanical gate by construction: a dropped clause and a hedge-to-absolute swap both leave token overlap high. Pre-ticked inventory rows plus a token screen must never be read as Gate 3 evidence — only the full source-vs-script read is.

> **Branch-vs-chapter naming.** The branch is named `chapter-9-working`, but the chapter in progress on it is **Class 11 Chapter 10 — Cell Cycle and Cell Division** (the chapter whose inventory PR #45 added). It is *not* Class 11 Ch9 (Biomolecules), which remains not started. Note also that two different chapters in this repo are numbered 10: this one (Class 11) and *Biotechnology and its Applications* (Class 12, closed earlier). Their folders and asset prefixes collide by number (`fig_10_1.png` exists in both), so they are distinguished by class throughout.

| Workflow stage | Evidence |
|----------------|----------|
| **Environment resume** | Sandbox was fresh (venv is not persisted by git). The v6 §0.2 command (`pip install --break-system-packages`) fails here — this image's `pip` is a `uv` shim that rejects `--break-system-packages`, and the default `python3` (3.13.11) is a uv-managed, externally-managed interpreter. Installed into the system 3.9.25 environment with `uv pip install --system` and ran every step with `/usr/bin/python3`: `reportlab 5.0.1`, `pdfplumber` OK, `pymupdf 1.26.5`, `Pillow 11.3.0`. Not a §0 restart — no re-extraction, no re-render of source assets. |
| **Gate 1 (independently re-verified, not trusted from prose)** | Inventory read in full, start to finish, before anything was modified. **167 Facts rows (F001-F167)** contiguous; **8 figure-label rows (L01-L08)** carrying **24 in-figure labels**; **31-row summary classification** with **11 SUMMARY-UNIQUE** facts each mapped to a body home (F157-F167); **7 exercise-gap rows** (Q1, Q9, Q11, Q13ii, Q14, Q15, Q16) each with a planned home; **8-row figure manifest**, every row `Mono: yes` / `Verified: yes`. All 8 assets re-verified on disk this session: `mode == "L"`, extrema `(0, 255)`. Inventory sits in the correct chapter folder. **Gate 1 green.** |
| **Gate 1 caveat recorded, not waived** | Pass 1 had **pre-ticked all 175 rows before any script existed**, so `check_pdf.py` check 7 would pass trivially and the ticks did not yet mean what the tick legend says. The ticks were therefore treated as unverified and re-earned during Pass 2/3; the inventory now carries an explicit "Pass 2 / Pass 3 verification record" section stating this, so a later audit is not misled. Same class of issue as Ch2's unticked label rows, in the opposite direction. |
| **Pass 2 build** | `Ch10_CellCycleAndCellDivision.py` written as one linear `story.append(...)` sequence in Content Order (§5), importing the frozen `neet_template.py` only — no style, colour, font, geometry or helper re-declared. Every block carries its `# ---- N.N ----` marker (10.1, 10.1.1, 10.2, 10.2.1-10.2.5, 10.3, 10.4, 10.4.1, 10.4.2, 10.5, Quick Recap, appendix). All 8 figures embedded inline at their topic via the shared `figure()` wrapper. Output: **11 A4 portrait pages against an 11-page source** — no compression. |
| **Gate 2 linter** | `python3 check_pdf.py "notes/class 11/Ch10_CellCycleAndCellDivision"` → **VERDICT PASS, 0 fail / 0 warn, exit 0 — green on the first render**, and **also green under `--strict`** (exit 0), which no previously closed chapter achieved. Checks 1-8 all PASS: no text in the 1.4 cm bands; smallest rendered glyph **6.0pt** (above the 5.0pt floor and outside the 5.0-6.0pt review band entirely); **all 8 embedded images monochrome**; no portrait row in the manifest; no banned Unicode arrows/sub-superscripts/Greek/emoji; **25/25 labels fully in running text** (0 partial, 0 missing); **175/175 rows ticked**; all 11 pages A4 portrait 595x842pt. |
| **Zero warnings — why this chapter escaped the usual benign WARN** | Every previously closed chapter carried one accepted check-4 WARN from the `PORTRAIT_HINTS` substring heuristic (`photo`/`profile`). This chapter's source has **no scientist profile box and no photograph of any person at all**, and its inventory contains no `photo`/`profile`/`portrait` substring in any manifest row, so the heuristic has nothing to fire on. The §4.4 hard no and §5 item 3 are satisfied trivially rather than by an accepted exception. |
| **Phase-notation decision** | "G1", "S", "G2", "G0", "2C", "4C", "2n"/"n" are written as plain ASCII rather than with `<sub>` tags. Unicode sub/superscript codepoints are banned outright by §4, and a Times `<sub>` inside a 9.5pt `data_table` cell renders at roughly 5.5pt — inside check 2's WARN band. ASCII keeps the chapter at a 6.0pt floor and `--strict` green without losing a fact. Recorded in the script header and the inventory. |
| **Pass 3(a) — visual render check** | **PASS — zero layout defects.** All 11 pages rendered with `pymupdf` and inspected individually. No overflow, no clipping, no orphaned heading, no table running off the page, no figure squashed to the wrong aspect ratio, and every process-flow vertical rule aligned with its triangle badges (the cyclic cell-cycle flow on p2, the mitosis 4-stage flow on p3, the prophase-I 5-substage flow spanning p8-p9). Cross-page style identity confirmed by pulling H1/H2/H3 banners, `data_table`, NOTE, MEMORY AID, process flow and figure boxes from three separate points each — **template held, no drift**. |
| **Pass 3(b) — first attempt (RETRACTED)** | The earlier session recorded "175/175 COVERED, 0 MISSING / 0 FABRICATED / 0 DRIFTED" and then ended on credit limit while writing documentation. **That verdict was premature and is retracted.** It rested on a token-coverage screen plus a partial block read. A token screen cannot detect a dropped clause inside an otherwise-covered row, nor a hedge hardened into an absolute — both keep token overlap above threshold. Gate 3 was therefore **not** actually satisfied when the chapter was marked closed. |
| **Pass 3(b) — re-run, full read (authoritative)** | **Complete, and it found real defects.** Source re-extracted and read start to finish (11 pp); the whole script read start to finish; all 175 rows (F001-F167, L01-L08) re-checked against both. The token screen again returned **0 rows below 85%** — and the full read still surfaced **3 genuine defects**, which is precisely why v6 §Pass 3 forbids confirming by grep. Verdict before fixes: **172 COVERED / 1 MISSING / 2 DRIFTED / 0 FABRICATED**; after fixes: **175/175 COVERED**. Separately, a fabrication probe for non-chapter vocabulary (cyclin, CDK, checkpoint, p53, apoptosis, cancer, cohesin, separase, APC, telomere) returned **zero hits**. All 3 embedded in-text question boxes (F036, F044, F045) preserved and answered from the chapter itself. |
| **Genuine defects — 3 found, 3 fixed** | **1. DRIFTED (F055/F058, 10.2.1)** — NCERT's prophase key-event bullet "Centrosome which had undergone duplication **during interphase**, begins to move towards opposite poles of the cell" had lost that clause from the bullet list; it survived only in the narrative paragraph, so the examinable list was incomplete. Restored. **2. DRIFTED (F093, 10.4 table)** — the "Products" cell read "**Two** diploid daughter cells with identical genetic complement", hardening NCERT's "Mitosis **usually** results in the production of diploid daughter cells��" into an absolute and inventing "Two", which NCERT does not write there. This is the same qualifier-drift class as the historical EcoRI defect 4. NCERT's "usually" restored, "Two" removed. **3. MISSING (10.3)** — NCERT's sentence "It is very essential to understand the significance of this division in the life of an organism." was absent; restored between F091 and F093. Each carries a `# [VERIFICATION FIX]` tag at its `# ---- N.N ----` block; **the PDF was rebuilt** and Gate 2 re-run. |
| **Post-fix Gate 2 re-verification** | `python3 check_pdf.py "notes/class 11/Ch10_CellCycleAndCellDivision"` → **PASS, 0 fail / 0 warn, exit 0**, still green under **`--strict`**. Still **11 pages**; all **8** embedded images still monochrome (`mode L`, extrema `(0,255)` re-checked on disk); **25/25** labels still fully in running text; 175/175 rows ticked; all pages A4 portrait. All 11 pages re-rendered and re-inspected after the rebuild — no reflow damage, no orphaned heading, no split table, figures still at their topics. |
| **Compression decisions** | Interphase phases (F027-F035 + summary-unique F160-F162) delivered as one 3-row table; animal-vs-plant cytokinesis (F083-F087) as a 2-row table; N/C-through-the-cycle (Q16) and mitosis-vs-meiosis (Q11) as tables that add no fact beyond 10.1.1-10.4. Every merged sentence's facts and qualifiers survive the merge. |
| **Exercise-gap closure** | All 7 rows covered — Q16 and Q11 in body tables (10.1.1, 10.4); Q1, Q9, Q13(ii), Q14, Q15 in the "Terms Used in the Exercises" appendix. Q9's equal/unequal size comparison and Q13(ii)'s specific example are **stated plainly as beyond what this chapter supplies** rather than filled in from outside the source (Rule 5 honoured over the temptation to answer). |
| **Colour-dependent figures** | **None.** Figure 10.1's phase wedges separate by greyscale tone *and* by their printed labels, all 10 of which are in the running text. |
| **Source problems** | NCERT's own typo "thererfore" (F085) — kept verbatim in the inventory's wording column, normalised in body prose only. Figure 10.3's panel labels read "Metaphase 1"/"Anaphase 1"/"Telophase 1" with the digit 1 while the body uses roman "I" — **both spellings preserved where each occurs**. Faint "not to be republished" watermark in the source artwork obscures no label (all 24 read cleanly); left as-is, consistent with Ch2/Ch3. |
| **Figures requiring manual attention** | **None.** All 8 assets re-verified on disk this session: `mode == "L"`, extrema `(0, 255)`, correct figure for each caption, every panel label legible. Placement re-checked page by page after the rebuild — Fig 10.1 at 10.1.1, Fig 10.2(a)-(e) at 10.2.1-10.2.5 respectively, Fig 10.3 at 10.4.1, Fig 10.4 at 10.4.2; all captions match the manifest verbatim. |
| **Final linter verdict** | **PASS — 0 fail, 0 warn, exit 0, and green under `--strict`** (re-run after the 3 verification fixes and the PDF rebuild). No accepted WARN, no waived check. |
| **Gate 3 status** | ✅ **SATISFIED.** Zero confirmed defects remain (3 found → 3 fixed → re-verified); `check_pdf.py` green including `--strict`; all 11 pages passed visual verification post-rebuild; inventory coverage verified by full read rather than by trusting pre-ticked rows; all 4 deliverables present. |
| **Deliverables** | `Ch10_CellCycleAndCellDivision.pdf` (11 pp) · `Ch10_CellCycleAndCellDivision.py` (now carrying 3 `# [VERIFICATION FIX]` blocks) · `Ch10_CellCycleAndCellDivision_inventory.md` (figure-label matrix + retraction-and-re-run verification record + Coverage note) · `assets/` (8 verified monochrome PNGs) · `extract_figures.py`. |

**Section-wise coverage confirmation**

| Section | Body facts | Summary-unique | Figures | Labels in text |
|---|---|---|---|---|
| Ch intro | 5/5 (F001-F005) | — | — | — |
| 10.1 | 8/8 (F006-F013) | 3/3 (F157-F159) | — | — |
| 10.1.1 | 32/32 (F014-F045) | 3/3 (F160-F162) | 1/1 (Fig 10.1) | 10/10 |
| 10.2 | 5/5 (F046-F050) | — | — | — |
| 10.2.1 | 10/10 (F051-F060) | 2/2 (F163-F164) | 1/1 (Fig 10.2a) | 2/2 |
| 10.2.2 | 11/11 (F061-F071) | — | 1/1 (Fig 10.2b) | 2/2 |
| 10.2.3 | 5/5 (F072-F076) | — | 1/1 (Fig 10.2c) | 1/1 |
| 10.2.4 | 5/5 (F077-F081) | 1/1 (F165) | 1/1 (Fig 10.2d) | 1/1 |
| 10.2.5 | 8/8 (F082-F089) | — | 1/1 (Fig 10.2e) | 1/1 |
| 10.3 | 10/10 (F090-F099) | — | — | — |
| 10.4 | 11/11 (F100-F110) | 1/1 (F166) | — | — |
| 10.4.1 | 33/33 (F111-F143) | 1/1 (F167) | 1/1 (Fig 10.3) | 4/4 |
| 10.4.2 | 9/9 (F144-F152) | — | 1/1 (Fig 10.4) | 4/4 |
| 10.5 | 4/4 (F153-F156) | — | — | — |
| **Total** | **156/156** | **11/11** | **8/8 embedded + verified mono** | **24/24** |

Every row above was **re-confirmed by full source-vs-script read** in the Pass 3(b) re-run, not carried over from the pre-ticked inventory. The three sections that required a fix were **10.2.1** (F055/F058 clause restored to the key-event bullet), **10.4** (F093 "usually" restored in the comparison table), and **10.3** (missing NCERT sentence restored); all three re-read clean afterwards. All other sections were clean on first re-read.

---

## Chapter 10 (Class 12) — Biotechnology and its Applications — ✅ FULLY COMPLETE (CLOSED)

Verified closed under the supreme command prompt.

| Workflow stage | Evidence |
|----------------|----------|
| **Pass 1 / Gate 1** | Frozen inventory of **147 Facts rows** + **14 summary rows**; figure manifest + figure-label matrix (**10/10** labels present in running text). |
| **Pass 2 / Gate 2** | `check_pdf.py` → **VERDICT WARN, 0 fail / 1 warn, exit 0**. Checks 1,2,3,5,6,7,8 PASS. 8 × A4-portrait pages, 3 monochrome images, all 147 rows ticked. |
| **The 1 warning** | Check 4 photo-keyword heuristic — a **confirmed benign false positive**. Fires on three *textual* mentions only (F103 "photographic film"; Fig 10.1 caption colour description; Fig 10.1 hand-in-source note). No person photograph is embedded (Check 3 confirms all 3 images monochrome). |
| **Pass 3 / Gate 3** | **Zero confirmed defects.** Cross-page visual render inspected; content cross-checked against frozen inventory. |
| **Grounding** | 147/147 rows ≥ 80% grounded against fresh source. **F129** (10.4 biopiracy "vigilance" qualifier) — full-read confirmed genuinely MISSING, restored verbatim-in-fact, now grounds **97.1%** (was 62%). |
| **Fixes preserved** | Fig 10.1 watermark removal · split-table `KeepTogether` · Fig 10.1/10.2/10.3 caption corrections · Fig 10.3 A/B chain clarification · appendix Q6 row · colour-dependent-figure corrections · GMO banner icon-table placement · F129 restoration. |
| **Deliverables** | `Ch10_BiotechnologyAndItsApplications.pdf` · `.py` · `_inventory.md` · `assets/`. |

---

## Chapter 9 — Biotechnology: Principles and Processes — ✅ FULLY COMPLETE (CLOSED)

Verified closed under the supreme command prompt on 2026-08-19.

| Workflow stage | Evidence |
|----------------|----------|
| **Pass 1 / Gate 1** | Frozen inventory of **200 Facts rows**; figure manifest + figure-label matrix (**38/38** labels present in running text). |
| **Pass 2 / Gate 2** | `check_pdf.py` → **VERDICT WARN, 0 fail / 1 warn, exit 0**. Checks 1,2,3,5,6,7,8 PASS. 13 × A4-portrait pages, **7 monochrome images** (the 7 numbered figures Fig 9.1–9.7), all 200 rows ticked. |
| **The 1 warning** | Check 4 photo-keyword heuristic — a **confirmed benign false positive**. Fires on the Section-wise coverage row `Boyer profile … text-only by design (no person photo)`; the heuristic matches the word "photo" inside the phrase *"no person photo"* — a sentence **denying** a photo. No Boyer portrait is embedded (Check 3 confirms all 7 images monochrome and are the numbered figures); the Herbert Boyer profile is text-only by design. **Accepted, not suppressed** — the "no person photo" phrasing is deliberately not reworded to silence the linter, exactly as Ch8's `photosynthetic` WARN was handled. |
| **Pass 3 / Gate 3** | **Zero confirmed defects.** Pass 3(a): all 13 pages inspected, zero layout defects, cross-page style identity confirmed (template held). Pass 3(b): fresh source re-extraction full-read against all 200 rows → **200/200 COVERED, 0 MISSING / 0 FABRICATED / 0 DRIFTED**; historical defects 4/5/6 re-tested at their exact original sites and re-confirmed correct against source. No `# [VERIFICATION FIX]` edit to the `.py` needed (the one documentary fix was the inventory header row-count line). |
| **Colour-dependent figures** | Fig 9.1 (vector vs foreign DNA), Fig 9.4 (pBR322 `ampR`/`tetR`/`ori`/`rop` arcs) and Fig 9.6 (PCR amplified region): each caption restates the distinction by outline/solid/label/boundary so meaning survives monochrome. |
| **Source problems** | Page-number digits bleeding into text (`168 15-100`, `117722`); unit-opening page interleaving intro with the chapter-list sidebar; degree glyph lost as `420C`; `β` lost from beta-galactosidase; "two sets of primers" split across a page break. All recovered by reading the rendered source page; none unrecoverable. The Herbert Boyer photograph is a deliberate exclusion, not a problem. |
| **Deliverables** | `Ch9_BiotechnologyPrinciplesAndProcesses.pdf` · `.py` (+ `convert_figures_mono.py` helper) · `_inventory.md` (with figure-label matrix + Gate 3 closure + Coverage note) · `assets/` (7 mono PNGs). |
