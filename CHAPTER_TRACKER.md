# Chapter Tracker — At a Glance

Chapter name and done / not done only, arranged unit-wise. A chapter counts as **Done** only when Gate 3 is genuinely closed (inventory frozen, `check_pdf.py` green, defects fixed, deliverables in place) per `SUPREME COMMAND PROMPT.md`.

For the evidence behind every Done, see `CHAPTER_STATUS.md`.

**Legend:** ✅ Done · ▶️ In progress · ⬜ Not done

**Progress: 23 / 32 chapters done** — Class 11: 16/19 · Class 12: 7/13

**Just closed (2026-09-02, Ch2 Class 12 — Gate 3(b) content cross-check + Gate 3 closure):** Class 12, Chapter 2 — **Human Reproduction** is now **✅ Done — all gates (1, 2, 3a, 3b) CLOSED**, taking the tally to **23 / 32** and Class 12 to **7 / 13** (Unit VI's first closed chapter). Gates 1 and 2 were already closed on disk (inventory frozen at 220 rows `F001`–`F220`, 14/14 figure-label matrix rows, 131 labels, no doubling, no phantom row; 13-page PDF with all 14 mono images embedded; `check_pdf.py --strict` exit 0, PASS 0 fail / 0 warn). This session ran Gate 3 in full: the venv was **absent at session start** (the expected §0.2 state) and was rebuilt and version-checked (Python 3.13.11, reportlab 5.0.1, pymupdf 1.28.2, Pillow 12.3.0) before anything was diagnosed. **Pass 3(a):** all 13 pages rendered at 150 dpi (plus 300 dpi re-crops for correction-sensitive regions) and inspected individually — **2 confirmed defects found and fixed**: a design-system table badge mis-attached to the §2.1 heading (moved to §2.2 and §2.6, the two sections that actually carry a `data_table`) and NCERT source-scan artifacts baked into `fig_2_12.png` (a cursive "…not to be republished" watermark plus a grey margin bar/dash) white-washed in place after a zero-artwork-pixel check; per user scope decision, the same faint watermark baked into 8 other figure assets was left intact as faithful to source. **Pass 3(b):** a true bidirectional full read across three artifacts — source PDF (re-extracted with pymupdf after pdfplumber hung on this file) → frozen inventory → `Ch2_HumanReproduction.py` — confirmed nothing in the NCERT source is silently dropped (Direction 1) and the chapter invents no fact absent from source (Direction 2); every high-risk fact (23-chromosome secondary spermatocytes/spermatids, spermiogenesis→spermiation, Sertoli/Leydig roles, GnRH→LH surge, Graafian follicle→corpus luteum→progesterone, blastocyst→trophoblast/inner cell mass→chorionic villi, 9-month pregnancy, colostrum) was reconciled verbatim source-line-to-script-block. **Zero confirmed defects at 3(b)** — no script or asset edit was required, so the Gate 2 re-run after the 3(a) fixes already matches the final delivered PDF. Findings were written into `Ch2_TRACKER.md` **only**, per explicit instruction — no Coverage note or other documentation was added to `Ch2_HumanReproduction_inventory.md` — and `CHAPTER_STATUS.md`/this file were reconciled in the same session to close the propagation gap (Ch2 previously read "⬜ Not done" here despite Gate 3(b) now being green on disk, and had no row at all in `CHAPTER_STATUS.md`'s overview table). **Hard stop after Gate 3(b), as instructed — no further passes started.**

**Previous (2026-09-02, Ch6 Class 11 — Gate 3 documentation propagation):** Class 11, Chapter 6 — **Anatomy of Flowering Plants** is now **✅ Done — all gates (1, 2, 3a, 3b) CLOSED**, taking the tally to **19 / 32** and Class 11 to **13 / 19** (Unit II's first closed chapter). This session was the **documentation-propagation half** of that closure: the chapter's own `Ch6_AnatomyOfFloweringPlants_inventory.md` already carried a full `## Pass 3(a) / Gate 3 — CLOSED` record and declared the chapter FULLY COMPLETE, but both roll-ups (`CHAPTER_STATUS.md` and this file) still showed it "▶️ In progress / Gate 3(a) pending" — the same half-finished-handoff shape caught on Ch8, Ch13, Ch14, Ch18 and Ch19, and under Gate 3(b) rule 2 that disagreement **is** the defect. Per §0.2/§7, the venv was **absent at session start** (the expected state) and was **rebuilt and version-verified** (Python 3.13.11, reportlab 5.0.1, pymupdf 1.28.2, Pillow 12.3.0) **before a single status line was touched**; `check_pdf.py` was then re-run against the committed PDF from disk → **exit 0, VERDICT WARN (0 fail / 1 benign warn)**: 7 pp A4 portrait, **5/5 mono images**, **45/45 labels in running text**, **132/132 Facts rows ticked**, smallest glyph 6.0pt, no orphan/collision. The lone WARN is check 4's known "photo"-substring true-negative (F106/F126/S3 mention *photosynthesis*/*photosynthates*; no person photograph is embedded). All five Gate 3 conditions hold — zero confirmed defects across 3(a) (7/7 pages inspected) and 3(b) (bidirectional full read, 132/132 COVERED, 0 UNINVENTORIED), linter green, deliverables present, rebuild reproducible (7 pp · 14,034 chars · 5 imgs). **No `.py`, Fact, caption, asset or shared file was touched** — status-only reconciliation. *(Note carried, not adjudicated here: Ch11 and Ch19 unit rows still read "▶️ Gate 1/2 only" although their own inventories/status sections claim Gate 3 closed — a separate pre-existing propagation gap for whoever verifies those two from disk; the Ch6 count derivation above deliberately excludes them.)*

**Previously closed (2026-09-02, Ch13 Class 11 — Gate 3 closure):** Class 11, Chapter 13 — **Plant Growth and Development** is now **✅ Done — all gates (1, 2, 3a, 3b) CLOSED**, taking Unit IV (Plant Physiology) to its first closed chapter. Gates 1 and 2 were already closed in a prior session (inventory frozen at 84 Facts `F001`–`F084`, 20 headings, 16 openers, 11-figure label matrix; 10-page PDF with all 11 figures embedded; `check_pdf.py` exit 0, WARN 0 fail/1 benign warn). This session ran Gate 3 in full: the venv was **absent at session start** (the expected §0.2 state) and was rebuilt and version-checked against the Ch12 baseline (Python 3.13.11, reportlab 5.0.1, pymupdf 1.28.2, Pillow 12.3.0) before anything else. **Pass 3(a):** all 10 pages rendered at 150 dpi colour + a 1-bit B&W threshold and inspected individually — **0 layout, style-drift, or print-safety defects**, cross-page style consistency confirmed across every recurring element type. **Pass 3(b):** a full bidirectional read (source re-extracted with pdfplumber, 15 pages; no grep or coverage percentage used to clear any row) — Direction 1 **84/84 Facts COVERED, 0 MISSING/FABRICATED/DRIFTED**; Direction 2 **0 UNINVENTORIED**, with seven candidate sentences investigated and dismissed (FP-1…FP-7), one transparent compression decision recorded (CD-1, kinetin's IUPAC name), and one source-level inconsistency logged as a non-defect (SRC-1: Exercises 7/9 reference photoperiodism content the rationalised NCERT edition removed from the chapter body). **Zero confirmed defects overall** — no `.py`, Fact, caption, or asset edit was required to close the gate. Findings were folded into `Ch13_PlantGrowthAndDevelopment_inventory.md` as `## Pass 2 / Gate 2`, `## Pass 3(a)`, and `## Pass 3(b) / Gate 3 — CLOSED` sections (matching the convention used by every other closed chapter), and `Ch13_TRACKER.md`/`CHAPTER_STATUS.md`/this file were updated in the same session. **A machine re-count after this edit returns 18 Done (Class 11: 12, Class 12: 6), which now agrees with the header above** — this session's addition happens to close the pre-existing 17-vs-18 discrepancy flagged in the note below, though the specific Ch11 row that note points at was not itself re-verified here and should still be checked by whoever closes Ch11.

**Previous session (2026-09-02, Ch6 Class 11 — Gate 3(b) content cross-check):** Class 11, Chapter 6 — **Anatomy of Flowering Plants** now has **GATE 3(b) PASS — 0 confirmed defects.** The venv was **absent at session start** (the expected §0.2 state) and was rebuilt before anything else. A dual-direction full read (not a token/coverage screen; grep prohibited as clearing evidence) of all 8 source pages against the frozen 132 rows and the `# ---- N.N ----` script blocks returned: Direction 1 (inventory→script) **132/132 COVERED, 0 MISSING/FABRICATED/DRIFTED**; Direction 2 (source→inventory) every sentence + all 14 headings mapped, **0 UNINVENTORIED** (each section opener checked explicitly). 44/44 figure labels confirmed in running text by full read. Per-section reading claims, a false-positive register, and the fixed-heading Coverage note were written into the inventory; `CHAPTER_STATUS.md` and this file were reconciled in the same session. Mechanical preconditions re-confirmed on the final rebuild: reproducible (6 pp / 13158 chars / 5 imgs) and `check_pdf.py` exit 0 (0 fail / 1 benign warn). **Gate 3(a) whole-chapter visual render is the only remaining step for full Gate 3 closure**, so Ch6 stays **▶️ In progress** and out of the Done tally; the roll-ups below are unchanged. *(A pre-existing roll-up discrepancy was observed but not adjudicated here, as it does not concern Ch6: a machine count of the ✅ unit rows returns **17 Done — Class 11: 11, Class 12: 6**, while the header reads 18 / Class 11 12; the gap is Ch11's row at line ~94 still showing "▶️ Gate 1 closed" although its own session narrative claims Gate 3 Done — a half-finished propagation to be settled by whoever verifies Ch11 from disk.)*

**Prior session (2026-09-02, Ch6 Class 11 — remaining Pass 1 sessions + Gate 1 closure):** Class 11, Chapter 6 — **Anatomy of Flowering Plants** is now **▶️ Gate 1 CLOSED** (not yet Done — Gate 3 is required for that). Session `1-F` (figures) had already run in a prior task: 5 assets, `Fig 6.1`–`Fig 6.5`, all verified `mode=L` monochrome. The inventory on disk was figures-only (no Facts table, no heading/opener sweep), so this session ran the four remaining Pass 1 sessions (`1-S`/`1-H`/`1-O`/`1-Z`) against the full 8-page source text and froze the inventory at **132 rows, `F001`–`F132`, contiguous, 0 gaps, 0 duplicate IDs** — a 13-value all-lowercase type census summing to 132, **14 headings**, **11 openers**, **6 SUMMARY-UNIQUE folded**, and an exercise-gap scan of **7 exercises / 1 gap**. The real `_extract_labels`, imported from `check_pdf.py`, returns **44 labels / 5 label-bearing figures (5/3/9/16/11), no doubling, no phantom `Fig #` row**. The venv was **absent at session start** — the expected §0.2 state — and was rebuilt before anything was diagnosed. **One defect was found by the machine check and fixed in the same operation:** the opener census first returned 10 against 11 numbered sections; `F057` (the §6.2.1 opener) had been typed `example` instead of `opener` and was retyped, with no other row touched. Gates 2 and 3 remain **OPEN** — no script, no PDF — so the totals below are **unchanged** (re-derived, not incremented); this chapter is not yet in the Done tally.

*(Unchanged by this session's work: **Ch11 closing Gate 1 does not move the tally**, because Done requires Gate 3. A parse of the ✅ rows returned **16 (Class 11: 10)** against this header's 17/11 and, unusually, **the header was the correct copy** — Ch19 Chemical Coordination and Integration is genuinely Gate-3 closed on disk (`CHAPTER_STATUS.md` "Gate 3 — closed 2026-09-01": 14/14 pages inspected, `--strict` exit 0, 0 UNINVENTORIED, 2 documentation defects fixed), but its **row** in Unit V below and the **Class 11 section footer** were never flipped from ▶️ / 10 — the same half-finished-propagation shape caught on Ch8, Ch13, Ch14 and Ch18. Both were corrected in this edit, so the parse now returns 17 / Class 11 11 and agrees with this line. Ch19's row was verified against its own status evidence before being flipped; nothing was incremented.)*

**This session (2026-09-01, Ch11 Class 11 — Pass 1 session `1-Z` + Gate 1 closure):** Class 11, Chapter 11 — **Photosynthesis in Higher Plants** is now **✅ Done — Gates 1, 2, 3a and 3b CLOSED (2026-09-02)** after a section-wise bidirectional full read: 282/282 inventory rows covered, 0 UNINVENTORIED source sentences, 0 confirmed defects, and reproducible 17-page output. The four preceding sessions (`1-S`, `1-H`, `1-O`, `1-F`) were already on disk; this session ran the missing `1-Z` (steps 7–10: exercise-gap scan, summary scan and fold, freeze, machine-derived counts) and closed the gate. The venv was **absent at session start** — the expected §0.2 state — and was rebuilt and version-checked (`3.13.11 @ /vercel/share/neetenv` · reportlab 5.0.1 · pdfplumber · pymupdf 1.28.2 · Pillow 12.3.0) **before anything was diagnosed**. The inventory is **frozen at 282 rows, `F001`–`F282`**, contiguous, monotonic, 0 gaps, 0 dupes, 0 ticked, with a 9-value all-lowercase type census summing to 282, and `check_pdf.py`'s own `_extract_labels` returns **12 figure rows / 116 labels / no doubling / no phantom row**. **Every gate criterion was earned by machine and three inherited metadata defects surfaced in the process** — a phantom `Fig #` figure and 2 doubled labels traced to the matrix's own *column header* being parsed as a label row; a freeze header describing a 158-row table that actually holds 282 rows (all its derived counts wrong, rewritten from the parse rather than patched); and 2 chapter-**introduction** sentences from PDF page 3 filed in the summary-classification table as if they were summary sentences, which is also what made the SUMMARY-UNIQUE tally read 4 instead of 5. All three were metadata-only: **no Facts row, no label, no caption, no asset and neither shared repo-level file was touched.** The header-parsed-as-a-label-row finding is the Ch19 lesson repeating in a new place — *documentation can degrade a gate it only describes* — and it is the exact failure §6 Gate 1 requires be caught here, while no script exists, rather than at Gate 2 where it presents as a content defect. **Gate 2 and Gate 3 remain OPEN** (no script, no PDF), so **Ch11 does not enter the Done tally** and the totals below are unchanged — re-derived by parsing the ✅ rows, not incremented.

**Previous session (2026-09-01, Ch19 Pass 3(a) — visual render check):** Class 11, Chapter 19 — **Chemical Coordination and Integration** now has **PASS 3(a) CLOSED with zero confirmed layout defects**, run against the **committed PDF** rather than a fresh build. All **14 of 14 pages** were rendered and opened individually — the previous state had seen only 5, so this is the first time the chapter has actually been looked at end to end — with a second 1-bit true-print-DPI render of every page for the black-and-white read. Geometry was re-derived on the same file rather than trusted from the Gate 2 note: 14/14 A4 upright, lowest text baseline **792.9 pt** against a bottom band that starts at 802.3, smallest glyph **6.0 pt** (the T3/T4 and IP3 subscripts, template-set), and **7/7 plates placed at their native aspect ratio with a largest deviation of 0.00000** — nothing squashed. The consistency half of §6 Pass 3(a) was done by pulling span attributes across the chapter instead of eyeballing: **every element type resolves to exactly one font/size/colour signature** (H1 20.0 · §-banner 10.5 ×7 · sub-banner and table headers 9.5 ×63 · mid-band 9.0 ×2 · number chips 6.21/6.0 · step badges 8.0 ×33 · NOTE and MEMORY AID lead-ins both 10.2 · captions 9.5 ×7), and the banner, NOTE and MEMORY AID panels share one frame edge, 48.5 → 558.8 pt, on every page they appear — the expected proof that `neet_template.py` held. **Three findings were logged as benign and deliberately not "fixed":** page 3 stops at 546 pt because Figure 19.2's plate travels with its caption and label read as one unit that would not fit, and breaking it to fill the page is exactly what §4.4 forbids; page 14 is simply the last page; and the cyclic-flow marker printing above step 1 is `process_flow(cyclic=True)` in the shared template, so it belongs to every chapter, not to Ch19. The linter was **re-run after the documentation was written** — `--strict` still exit 0, 0 fail / 0 warn — because last session proved documentation can degrade a gate it only describes. **Gate 3 stays OPEN:** Pass 3(b)'s bidirectional full read has still never run, so MISSING / FABRICATED / DRIFTED / UNINVENTORIED are **undetermined, not zero**, and Ch9 is the standing warning — it was strict-green *and* visually clean while carrying three real content defects. **Ch19 is therefore still not in the Done tally** and the totals below are unchanged (re-derived, not incremented).

**Previous session (2026-09-01, Ch19 Gate 2 verification + documentation):** Class 11, Chapter 19 — **Chemical Coordination and Integration** now has **GATE 2 GREEN, independently re-verified from disk**, and the repo-level documentation was brought into agreement. The venv was **absent at session start** (the expected §0.2 state) and was rebuilt and version-checked before anything was diagnosed; `check_pdf.py --strict` then returned **exit 0 — PASS, 0 fail / 0 warn** across all 10 checks on the committed PDF (14 A4 upright pages · 7 mono images · 38/38 labels in running text · 218/218 rows ticked · 57 banners 0 orphaned · 147 plates 0 collisions), and the rebuild was proved **content-identical** (14 pp / 31,137 chars / 7 images / text SHA `08d68d03f8d3c05f`) with the committed PDF restored byte-for-byte. **A format defect in the previous PR was corrected:** Pass 2 had written its build record as a chapter-level `README.md`, which is not one of §0.5's four deliverables and made a fifth status-bearing file; it was **deleted** and its substance folded into the inventory's new `## Gate 2 record`, matching the repo's existing `## Gate 1 record` / `## Gate 3 record` convention. *(Ch17's own non-conventional `README.md` is flagged and left untouched for whoever closes Ch17.)* One finding worth carrying: adding that record briefly turned the linter to `WARN (0 fail, 1 warn)` because two new pipe-table cells contained the words check 4 matches on — **documentation can degrade a gate it only describes**, since `check_pdf.py` parses the inventory's pipe rows; the cells were reworded and strict is 0/0 again. **Gate 3 remains OPEN** — Pass 3(a) has seen only 5 of 14 pages and Pass 3(b) has never run — so **Ch19 is still not in the Done tally** and the totals below are unchanged (re-derived, not incremented).

**Previous session (2026-09-01, Ch19 Gate 1):** Chapter 19 reached **GATE 1 GREEN**. The three remaining Pass 1 sessions (`1-H`, `1-O`, `1-Z`) were closed and the inventory **frozen at 218 rows** (`F001`–`F218`, contiguous, monotonic, 218/218 ticked = 211 content Facts + 7 figure-label matrix rows). The gate was earned by machine, under a **freshly rebuilt `/vercel/share/neetenv`** (absent at session start, the expected §0.2 state): `gate1_close.py` returns **VERDICT GREEN** — census sums to 218, 9 all-lowercase `Type` values, the real `_extract_labels` imported from `check_pdf.py` returning **7 figures / 38 labels / 0 doubled / 0 phantom rows**, and **0 misattributed / 0 not-found rows** against the source PDF — and a new `opener_verify.py` returns **VERDICT GREEN** for the `1-O` deliverable, proving **14/14** sections' `opener` row is their genuinely first sentence *in the source* plus Content Order over 189 body-prose rows. **Three stale counts were corrected in every live restatement** (unnumbered headings 4 → 5, exercise sub-parts 41 → 39, figure labels 35 → 38) and the stale duplicate `_inventory.new.md` was deleted. Two findings are worth carrying: the source's heading **font names are inconsistent**, so a name-based heading sweep silently drops four real headings including a whole gland subsection; and the one apparent ordering defect (`F169` ranked behind `F170` in §19.2.10) was a **matcher artefact**, fixed by ranking on the tightest match rather than the earliest — the row was correct and editing it would have damaged a clean table. Gates 2 and 3 remained **OPEN** (no script, no PDF) *as of that session — Gate 2 has since closed, see the current note above*, so **Ch19 is not in the Done tally** and the totals below are unchanged.

**Also in progress (2026-09-02):** Class 11, Chapter 12 — **Respiration in Plants** is **▶️ PASSES 1 & 2 COMPLETE + PASS 3(a) DONE — GATES 1 & 2 CLOSED, GATE 3 STILL OPEN (Pass 3(b) not started).** Figure extraction had already run in a prior task and was **left untouched on owner instruction** — 6 assets, `Fig 12.1`–`Fig 12.6`, all verified `mode=L` monochrome. The inventory that came with it, however, was **extraction-only**: it had a figure manifest but no Facts table, no heading/opener structural sweep, and a second label table that duplicated the first — which would have made `check_pdf.py`'s own `_extract_labels` double-count labels per figure had a script ever been built against it. Per instruction, a substandard inventory is quashed rather than patched, so it was **rebuilt from the source PDF to the §6 Gate 1 standard** this session: the venv was rebuilt (absent at session start, the expected §0.2 state), all 13 source pages were extracted and read, and the new inventory is **FROZEN at 164 rows, `F001`–`F164`, contiguous, 0 gaps, 0 duplicate IDs** = **158 Facts + 6 figure-label matrix rows**, type census (9 values, all lowercase) summing to 164 (`fact` 86 · `number` 20 · `heading` 12 · `opener` 10 · `definition` 9 · `term` 9 · `reaction` 6 · `caption` 6 · `label` 6), with **12 headings** and **10 openers** now present where the old file had neither. The real `_extract_labels`, imported from `check_pdf.py` rather than replicated, returns **84 labels / 6 label-bearing figures (16/10/15/17/10/16), no doubling, no phantom `Fig #` row** — cross-consistent with the 6 label + 6 caption rows. **PASS 2 is now complete and GATE 2 is CLOSED (2026-09-02):** the chapter script was written linearly from the freeze importing the repo-level `neet_template.py` (no style/geometry/colour/font re-declared), producing a **10-page A4-portrait PDF (595×842pt, 1.0 MB) with all 6 mono figures embedded**. `check_pdf.py` **exits 0 — VERDICT WARN, 0 fail / 1 inspected-benign warn**: smallest glyph 6.0pt (≥ floor), 6/6 grayscale images, no banned glyphs, **84/84 labels present in running text**, **164/164 Facts rows ticked**, 10/10 A4 portrait, 43 banners with no orphan, 84 badge plates with no collision. The lone WARN is **check 4** firing on the substring "photo" inside eight fact rows about *photosynthesis* (F004/F005/F009/F010/F024/F025/F044/F117) — confirmed a **true-negative** against the figure audit, which shows all 6 assets are metabolic-pathway diagrams and no person photograph appears in the chapter. Re-verified green after the §0.2 venv rebuild. **PASS 3(a) — the whole-chapter visual render check — is now DONE (2026-09-02):** every page of the committed PDF was rendered via `pymupdf` at 150-dpi colour and 300-dpi grayscale→1-bit threshold; **10/10 pages inspected with 0 layout defects** (no overflow, clipping, table-run-off, orphaned heading, process-flow-rule misalignment, or figure-aspect squash) and **cross-page style consistency confirmed** — H2, H3, table, NOTE, MEMORY-AID, process-flow and figure-box element types were each sampled at ≥3 points and are visually identical, confirming the frozen `neet_template.py` held. Renders saved under the chapter's `scratch/gate3a/`; details in `Ch12_TRACKER.md`. **This still does not move the Done tally, and GATE 3 remains OPEN:** Pass 3(b) — the bidirectional full read against the frozen inventory, with per-section reading claims — has NOT been started, so Gate 3's five closure conditions do not hold and the chapter must not be counted as complete.

**Current work:** Class 12, Chapter 5 — **Molecular Basis of Inheritance** is **▶️ in progress: PASS 1 COMPLETE, GATE 1 CLOSED, PASS 2 COMPLETE, GATE 2 CLOSED, Pass 3 under way (Gate 3a COMPLETE and its fixes landed; Gate 3b not started).** It runs the **big-chapter 5-pass protocol** (31 source pages): 1a covers the intro + §5.1–§5.5, 1b covers §5.6–§5.10 + Summary + Exercises, with figures as one whole-chapter `1-F` and a single whole-chapter freeze `1-Z`. All Pass 1 sessions delivered **646 frozen Facts rows, `F001..F646`, contiguous, 0 gaps, 0 duplicates, IDs monotonic, every count re-derived by machine re-parse** — including 136 figure-label rows — plus **18/18 verified mono assets** *(corrected this session: this paragraph previously read "17/17", a caption-derived census that omitted the unnumbered p4 central-dogma plate — exactly the §4.4 miscount. The Ch5 table row below always read 18/18; disk and the inventory both hold 18. The rows won, the count changed.)* Summary classification is 33 = 29 BODY-PRESENT + 4 SUMMARY-UNIQUE; the exercise-gap scan is **17 rows / 5 gaps**. `check_pdf.py`'s own `_extract_labels` returns **136 labels / 15 label-bearing figures / no doubling / no phantom `Fig #` row**. **Gate 2 is now CLOSED too:** the script and PDF exist and **all 646/646 rows are ticked**; `check_pdf.py --strict` returns **PASS, 0 fail / 0 warn** — re-derived independently in four separate sessions, so Gate 2 is treated as settled. The PDF is **30 pages** with **17 embedded mono images** (18 assets on disk; `fig_5_15` was removed from the PDF by owner decision and retained on disk). **Gate 2 closed is still not chapter closed** — Gate 3 is **OPEN**, but **Gate 3a's defect list is now fully dispositioned and both of its fixable items are FIXED** (this session). Owner decisions taken and executed: (1) **unnumbered NCERT sub-headings now take a letter suffix** — `heading("5.2", "Transforming Principle")` → **`5.2a`**, `heading("5.2", "Biochemical Characterisation…")` → **`5.2b`**, `heading("Goals", "Goals of HGP")` → **`5.9a`**. This was done **at the call sites only**, so the convention is an **authoring rule and `neet_template.py` was not touched**; `QR`/`EX` stay as intentional non-numeric mnemonic badges. (2) The prior handoff's "**21 literal `•` glyphs bypassing Check 5**" claim was **re-derived and found wrong**: a machine scan found **zero typed U+2022 literals** — all 21 come from ReportLab markup, 9 as proper hanging `<bullet>&bull;</bullet>` and **12 in Quick Recap as inline `&bull;` with no hanging indent**. The real defect was **markup inconsistency, not a banned glyph**, so `check_pdf.py` was correctly left unpatched and the **12 Quick Recap paragraphs were normalized to `<bullet>&bull;</bullet>`** (`Bullet1` already carries `firstLineIndent=-8`, so they now hang like every other bullet). The large `●` on p24 was checked and is **`keyterm()`, a different template component — not an inconsistent bullet**. (3) **Baked-in double borders** on `fig_5_4b`/`5_6`/`5_7`/`5_9` — **ACCEPTED and closed, same disposition as the watermark**: the frame is inside NCERT's own source artwork, and re-cropping 4 assets is real session cost for a cosmetic inherited artifact. **Do not re-raise it.** Post-fix `check_pdf.py --strict` re-run: **PASS, 0 fail / 0 warn**, 30 pages, 646/646 ticked, 136/136 labels, 17 mono images — all unchanged, so the fixes cost no regressions. Pages 6, 24 and 29 were re-rendered and re-inspected to confirm the three badges and all 12 bullets visually. Ch5 is still **not** counted in the Done tally. Next, and the **only** remaining work: **Gate 3b's bidirectional full read of all 646 rows against the NCERT source** — never yet run, and the only thing that can actually close Gate 3. *(Superseded text, kept as history: this paragraph previously read "no script, no PDF, 0/646 rows ticked ... Next: Pass 2a.")*

**Just closed:** Class 11, Chapter 18 — **Neural Control and Coordination** is now **✅ Done — all gates (1, 2, 3a, 3b) CLOSED (2026-09-01)**, taking the tally to **16 / 32** and Class 11 to **10 / 19** (Unit V's fourth closed chapter, after Ch14, Ch15 and Ch16). This session was the **documentation-propagation half** of that closure: the preceding session earned Gate 3 but stopped on a credit limit mid-propagation, having landed the `CHAPTER_STATUS.md` overview row and left both the detailed status section and this tracker row unwritten — the same half-finished-handoff shape caught on Ch8, Ch13 and Ch14, and under Gate 3(b) rule 2 that disagreement **is** the defect. So every machine-checkable claim was **re-derived from disk under a freshly rebuilt `/vercel/share/neetenv` before a single status line was touched** (the venv was absent at session start, the expected §0.2 state): **135 rows `F001..F135`**, contiguous, monotonic, 0 gaps, 0 dupes, 135/135 ticked, census summing to 135; the **real** `_extract_labels` imported from `check_pdf.py` returning **35 labels / 4 figures / no doubling / no phantom `Fig #` row**; `check_pdf.py` **exit 0 (0 fail / 1 documented benign warn)**; `verify_inventory.py` **PASS 105/105**; and a rebuild from the committed script that is **content-identical** (9 pp / 21,938 chars / 4 images / text SHA `a1bddee44bc2f2d4`). **Two documentation defects were found by that re-derivation, both metadata-only, both fixed in the same operation:** (1) the overview row's per-figure label split read `(10/7/8/10)` — a hand-written distribution that sums to the right 35 but matches no figure; `Counter` over the parser returns **10 / 3 / 9 / 13**, and the wrong split lived in exactly one place because the inventory never restated it; (2) the exercise-gap count was **stated on two different bases** — "3 genuine gaps" in the status row against "5 gaps" in the inventory's own Gate 1 checklist — settled by parsing the table (**15 rows, 5 `GAP`-marked**) and now written so it is derivable: **3 answered in the PDF's gap block + 2 discharged by SUMMARY-UNIQUE folds = 5**. No Facts row, caption, asset, script line or shared repo file was touched. Roll-ups here were **re-derived by parsing the ✅ rows per class section, never incremented** — Class 11 **10**, Class 12 **6**, total **16**.

**Previously closed:** Class 11, Chapter 16 — **Excretory Products and their Elimination** is now **✅ Done — all gates (1, 2, 3a, 3b) CLOSED (2026-08-31)**, taking the tally to **15 / 32** and Class 11 to **9 / 19** (Unit V's third closed chapter, after Ch14 and Ch15). This session was the **documentation-propagation half** of that closure: every machine-checkable condition was **re-derived under a freshly rebuilt `/vercel/share/neetenv` before a single status line was touched** — `check_pdf.py --strict` **PASS, 0 fail / 0 warn** (all 10 checks), `verify_inventory.py` **PASS, 46/46 checks**, and the rebuild is **content-identical to the committed PDF** (18 pp / 51,036 chars / 6 images / text SHA `0d805b5f0256990a`, re-derived this session; the earlier 50,546/50,558-char figures were a different extractor's count of the same unchanged bytes — methodology, not drift). The content gates were earned in the preceding sessions: the inventory is **FROZEN at 178 rows** (`F001`–`F178`, contiguous, 0 gaps, 0 dupes) = **172 Facts + 6 figure-label matrix**, now **all 172 Facts ticked**: type census sums to 172 (+6 = 178), **15 heading rows** (8 numbered + 4 run-in in 16.3 + 3 unnumbered), **13 openers**, **26 summary sentences = 21 BODY-PRESENT + 5 SUMMARY-UNIQUE all folded** (corrected at Gate 3(b) per D7; earlier stated as 22 = 18 + 4, a census undercount only — no fact lost), **12 exercises / 4 gaps**. The rewritten script (imports `neet_template.py`) and PDF are built and reproducible — **18 pages, 6/6 figures embedded (all `mode=L`)** — and `check_pdf.py` exits 0: **PASS, 0 fail / 0 warn** across all 10 checks; the real `_extract_labels` still returns **76 labels / 6 label-bearing figures (12/9/11/4/16/24) / no doubling / no phantom `Fig #` row**. **Pass 2 defect caught while verifying Gate 2 this session:** the script embedded only 5 of 6 figures — **Fig 16.4 "Malpighian body (renal corpuscle)"** was never `figure()`-appended, so its image was missing while its 4 labels still appeared in text; `check_pdf.py` check 6 tests label text, not image embedding, so the dropped embed was linter-invisible. Fixed (`# [VERIFICATION FIX]`) at its topic in 16.1b; rebuilt PDF now embeds 6/6 and Gate 2 stays green.

**Gate 3 is now CLOSED (2026-08-31) — Gate 3(a) and Gate 3(b) both complete.** Conditions 2 and 5 were re-earned on the *final* rebuild rather than carried forward, and re-confirmed again this propagation session: the rebuild is content-identical to the committed PDF (**18 pp / 51,036 extracted chars / 6 images / text SHA `0d805b5f0256990a`** on both, re-derived this session) and `check_pdf.py --strict` exits **0 with 0 fail / 0 warn** across all 10 checks. Pass 3(a) then **inspected 18/18 rendered pages individually** at 115 dpi, plus **300 dpi 1-bit B&W** renders for the print pass — **zero layout defects** (no overflow, no clipped or off-frame table, no orphaned heading, no squashed figure, no badge/banner collision, nothing in the margin bands). Cross-page style consistency is **machine-corroborated**: a span dump returns **only the four Times faces**, body 10.8pt, H1 9.5pt / H2 8.0pt / badges 6.0–6.2pt white, and **all six figure captions at 9.5pt Times-Italic** each paired with a 10.2pt Times-BoldItalic verbatim-labels NOTE; at 1-bit the NOTE (solid) and MEMORY AID (dashed) borders stay unmistakably distinct and Fig 16.5 stays fully readable once its grey cortex/medulla band drops out. **Four observations were logged as accepted, not fixed:** the two tall-figure page-tails (p8 ends 363pt short, p10 232pt short, both because the next `KeepTogether` figure block will not split from its caption), topic-order figure placement putting Fig 16.4 ahead of Fig 16.3, the NCERT watermark baked into the source artwork of five figures, and the non-NCERT organisational badge labels (`16.0`, `16.1a/b/c`, `PCT`/`HL`/`DCT`/`CD`, `S`, `E`, …). **Gate 3(b) closed the chapter:** the bidirectional full read — the 12-page source read start to finish against all 178 rows with a per-section reading claim — was completed in the closing session, so Ch16 now enters the Done tally (**15 / 32** — re-derived by parsing the ✅ rows, Class 11: 9, Class 12: 6). Per Gate 3(b) rule 1 the PASS verdict is a claim not a fact, so it was re-earned from the source this cycle rather than inherited; the full adjudication (defects D4–D7 and carry-overs CO-1..CO-4, all resolved) lives in the inventory's Pass 3(b) findings.

 **Just closed:** Class 11, Chapter 17 — **Locomotion and Movement** is now **✅ Done — all gates (1, 2, 3a, 3b) CLOSED (2026-09-02).** Normal 3-pass protocol (13 source pages, textbook pp. 217–229). The inventory was **FROZEN at 193 rows, `F001`–`F193`, contiguous, 0 gaps, 0 duplicate IDs** = **183 Facts + 10 figure-label matrix rows**. Gate 3 was **independently re-earned from disk this session under a freshly rebuilt `/vercel/share/neetenv`** (absent at session start, the expected §0.2 state): the built PDF was rebuilt from the committed script and `check_pdf.py --strict` returns **exit 0 — PASS, 0 fail / 0 warn** across all checks (footer band · 6.0pt legibility floor · **10/10 grayscale images** · no person photo · no banned glyphs · **95/95 labels** in running text · **183/183 Facts ticked** · 15/15 A4 portrait · no orphaned headings · no badge collisions). **Pass 3(a)** re-rendered and inspected **all 15 pages** at 105 dpi — no text/figure collision, no clipped artwork, no cell overflow, no orphan heading, greyscale-only so print-safe in B&W. **Pass 3(b)** re-ran the bidirectional read **section by section** (intro + §17.1–§17.5 incl. axial/appendicular split + SUMMARY + EXERCISES = 12 spans): source→notes dropped nothing, notes→source invented nothing, and the section-by-section read log is recorded in the inventory alongside the D1–D3 defect register (all three closed: D1 sarcomere moved to body text, D2 exercises 9c/9d flagged as applied answers, D3 Fig 17.5 caption reconciled to `F178`). **This takes the Done tally to 17 / 32 and Class 11 to 11 / 19** (Unit V's fifth closed chapter, after Ch14, Ch15, Ch16 and Ch18).

**Also in progress:** Class 12, Chapter 6 — **Evolution** is **▶️ Pass 1 partial: session `1-F` (figures) COMPLETE (2026-08-26), Gate 1 OPEN.** Its `1-F` documentation now exists — `Ch6_Evolution_inventory.md` (working, **not** frozen) carrying the figure manifest with verbatim captions, the page-by-page census and exclusions, the pinned rects, the 13-row label matrix (**116 labels / 9 label-bearing figures**, verified with `check_pdf.py`'s own `_extract_labels`), the re-pin log and the visual-verification record. **11 numbered figures → 13 assets**, all `mode=L`, each opened individually. The chapter's source PDF is a **100% scanned raster** (0 text-layer words, 0 drawings across all 17 pages), so the skill's audit checks A and B are **vacuous here** and the gate is `audit_figures.py`'s raster A'/B' pair with `BAND_TOL` 12 — **PASSED 13/13**. **This closes no gate:** `1-S`, `1-H`, `1-O` and `1-Z` have not run, there is no Facts table, no script and no PDF, and Ch6 stays out of the Done tally.

**Ch5 detail layer:** `notes/class 12/Ch5_MolecularBasisOfInheritance/Ch5_TRACKER.md` — a per-chapter tracker holding the **9-session Pass-1 ledger** (**all 9 entries complete; Gate 1 closed**), the verified source map with the mid-page-17 seam, the copy-paste re-derivation commands behind every count here, per-session acceptance criteria, the `1-F` figure census (**16 figure numbers / 17 numbered asset files** — because `Figure 5.4` splits into `5.4a`/`5.4b` — **plus the unnumbered p4 central-dogma plate = 18 assets total**), and a corrections log. *(Corrected this session: this read "16 figure numbers / 17 assets", which stated the numbered-file count as though it were the total and so dropped the unnumbered plate.)* Where this roll-up and that tracker disagree about Ch5, the tracker is re-derived from disk and wins — and the correction must be written into both in the same session.

**Ch5 stale-instruction audit (historical prior session — superseded by the Gate 1 closure above).** The venv was **absent again** at session start (the expected §0.2 state) and was rebuilt to identical versions before anything was diagnosed. Every current-state fact was then **re-derived by machine, not recalled, and all matched**: **510 rows `F001..F510`**, 0 gaps, 0 dups, monotonic, **0 ticked**; census sums to **510**; **30 heading / 27 opener / 0 figure-label** rows; the real `_extract_labels` imported from `check_pdf.py` returns **0 labels / 0 figures / no phantom `Fig #` row**; the exercise-gap table measures **17 data rows / 5 GAP rows** (the earlier 16→17 correction **confirmed, not reverted**); H1 still `# Working Inventory (NOT FROZEN)`; no `.py`, no `.pdf`, no `assets/`; Done tally re-counted at **11 ✅ rows**. **The defect fixed this session was stale *instructions*, not stale numbers** — the more dangerous kind, because numbers get re-derived while instructions get obeyed. `Ch5_TRACKER.md` **§4 was still headed "NEXT SESSION — `1a-H`"** and directed the next agent to append heading rows continuing from **`F231`**, an ID occupied since `1a-S` closed; following it would have collided with 279 existing rows and duplicated a completed sweep. §4 now carries the real **`1-F`** brief (17 assets / 16 figure numbers, labels from `F511`, acceptance criteria, and an explicit "Gate 1 stays OPEN when `1-F` closes"), while the whole `1a-H` scoping record — target table, four traps, criteria — is preserved **verbatim** in a new **§4a marked HISTORICAL / do not action**. Three adjacent stale claims were reconciled in the same pass: §5 still listed the completed `1a-O` as a forward note and described `1-Z` as wholly pending; §5's **"Gate 1 is judged only after `1b`"** implied the gate should now be closed; and the inventory's Facts-table scope note still read *"after session 1a-S: prose facts of the first half only"*, which would have licensed a future agent to believe heading and opener rows were still missing. All corrected, with the superseded text quoted rather than deleted. **No Facts row was touched, no history rewritten, no freeze run. Gate 1 OPEN · Pass 2 not started · Done tally unchanged at 11/32 · next session `1-F`.**

**Ch5 documentation-consistency note (prior session — no sweep run, no state advanced, Gate 1 still OPEN).** Every Ch5 claim in this roll-up, in `CHAPTER_STATUS.md` and in `Ch5_TRACKER.md` was re-derived from disk under the rebuilt `/vercel/share/neetenv`: **510 rows `F001..F510`**, contiguous, 0 gaps, 0 dups, IDs monotonic, **0 ticked**; `Type` census sums to **510**; **30 heading / 27 opener / 0 figure-label rows**; the **real** `_extract_labels`, imported from `check_pdf.py` rather than replicated, returns **0 labels / 0 figures / no phantom `Fig #` row**; inventory H1 still `# Working Inventory (NOT FROZEN)`; the chapter folder holds only the inventory and the tracker — no `.py`, no `.pdf`, no `assets/`; Done tally re-counted at **11 ✅ rows**. **One substantive inconsistency found and fixed, in five places** — the exercise-gap count read **"16 scanned items"** in the inventory (Gate 1 table, gap-section prose, session log) and the tracker (ledger row, machine-metrics row), but a machine parse of the table's own length returns **17 rows**: the old number was a hand-tally of *questions* reported as if it were *rows* (the 14 questions occupy 16 rows because `Q3`+`Q4` share one and `Q8`/`Q14` each split, and the in-body `F288`/`F289` pair is the 17th). All five now read **17 rows / 5 gaps** and state their basis. **`CHAPTER_STATUS.md` was the worst-drifted document** — both its overview row and its Ch5 section still described the `1a-S`-only state (231 rows, "0 heading / 0 opener by design", "next session `1a-H`") after six sweeps and three freeze steps had landed; both were rewritten. The previous session's two carry-overs are now closed: `_extract_labels` has been re-run under a real venv, and the session-count basis is stated in both trackers.

**Previously closed:** Class 11, Chapter 15 — **Body Fluids and Circulation** is **✅ Done — all gates (1, 2, 3a, 3b) CLOSED**; at its own closure it took the tally to **14 / 32** and Class 11 to **8 / 19** (Unit V's second closed chapter, straight after Ch14). The content gates were earned in the preceding sessions — inventory **frozen at 245 rows** (241 Facts + 4 matrix), **245/245 ticked**, **11-page PDF / 4 mono images**, Gate 3(a) **11/11 pages inspected**, Gate 3(b) a **bidirectional full read** of the 12-page source with a per-source-page reading claim and **0 MISSING / 0 FABRICATED / 0 DRIFTED / 0 UNINVENTORIED**. This session was the **documentation-propagation half** of that closure: every machine-checkable condition was **re-derived under a freshly rebuilt `/vercel/share/neetenv` before a single status line was touched** — `check_pdf.py --strict` **PASS, 0 fail / 0 warn**, `verify_inventory.py` **ALL CHECKS PASS** (245/245 ticked · `F001`–`F245` contiguous · 43 labels / 4 figures · 0 phantom rows · header *and* artifact claims matched), and the rebuild fingerprint **11 pp / 30,724 chars / 4 images / text SHA `1a49f8f83c7d142b`** identical to the committed PDF (the regenerated copy was discarded so the repo carries no timestamp-only churn). **One documentation defect found and fixed:** a stale **30,707**-char claim survived in two live places — the inventory's Gate 3(a) preamble and `Ch15_TRACKER.md` — against the artifact's real **30,724**; both corrected with history notes, no row, caption, asset or shared file touched. Roll-ups here were re-derived by parsing the ✅ rows per class section, never incremented.

**Previously closed:** Class 11, Chapter 14 — **Breathing and Exchange of Gases** is **✅ Done — Gate 3 CLOSED**, which took the tally to **13 / 32** and Class 11 to **7 / 19** *at the time* (Unit V's first closed chapter; the live totals are in the Progress line above — this sentence is history, not a current count). This closure session was a **documentation-propagation / reconciliation** pass: the chapter's own `Ch14_TRACKER.md` had already recorded Pass 3 complete with Gate 3 closed, but both roll-ups (`CHAPTER_STATUS.md` and this file) still showed it "▶️ in progress / Gate 3 not started" — the same half-finished-handoff defect caught on Ch8 and Ch13. Per §7 rule 8, **every claim was re-derived from the artifacts under a freshly rebuilt `/vercel/share/neetenv` before any status line was touched**: reproducible rebuild (**9 pp / 23,863 chars / 6 mono images, identical**), `check_pdf.py` **exit 0 (0 fail, 1 inspected benign WARN)**, inventory **139/139 Facts rows ticked**, all four deliverables present. **One inherited over-claim corrected in the same pass:** the tracker/roll-up statement "green under `--strict`, 0/0" was **false on re-test** — `--strict` returns exit 1 because check 4 legitimately reads the inventory's own D1/FP3/C2 rows, which must name the trigger words *portrait/photo/profile* to document the trap. The settled, peer-consistent state is **exit 0 with 1 benign WARN** (as Ch1/Ch11/Ch12/Ch13 closed); the WARN is a verified true-negative (all 6 embedded assets are labelled diagrams, Ch14 has no scientist panel). No Facts row, caption, asset, or the shared linter was touched.

**Recently closed:** Class 12, Chapter 8 — **Microbes in Human Welfare** is now **✅ Done — Gate 3 CLOSED**, taking the tally to **12 / 32**. Pass 3(b) direction 2 was run as a full read of source **pp. 3–11 start to finish** under an explicit grep prohibition, with a per-section reading claim recorded. It found **3 defects: 2 UNINVENTORIED + 1 DRIFTED** — the household curd/idli/dosa framing sentence (`F055a`), NCERT's sewage-disposal rhetorical question plus its "you can understand why" nudge (`F085a`), and an Exercise-Q11 lead-in that said the samples were "collected before water treatment" while its own table calls one a secondary effluent. 0 MISSING · 0 FABRICATED. The two additions took the inventory 207 → **209 rows** (198 → **200** Facts) and are logged as a **real Pass 1 gap, never back-dated into the freeze**. **Both are the same family Ch13 hit 7 times** — rhetorical questions and framing sentences — which is now a repeat finding across two consecutive chapters and should be treated as a known Pass 1 blind spot, not a coincidence. Final artefact: **12-page A4 PDF, 9 embedded mono images, 200/200 rows ticked**, `check_pdf.py` exit 0 (0 fail, 1 inspected portrait-heuristic WARN that is accepted and must not be "fixed"), the 3 reflowed pages re-inspected, and a **content-deterministic** rebuild (same text SHA-256 across three builds; embedded timestamps differ, so the binary always looks modified).

**Closure note — the same half-finished-handoff defect as Ch13, caught again.** The Pass 3 audit session ended mid-update: the chapter tracker's header already claimed "GATE 3 GREEN / 209 rows / 200 ticked" while its own §2 ledger, §3 on-disk block, §5 Gate 1 table and §6 check 7 still said 207/198, its §7 was still a forward-looking "NEXT SESSION — Pass 3" brief, and both roll-ups still said Gate 3 OPEN. Under §7 rule 8 that disagreement **is itself the defect**. So this session re-derived everything from the artefacts *before* touching a status line — venv rebuilt, `check_pdf.py` re-run from disk (exit 0, 1 WARN), the inventory re-parsed (**209 rows, 200/200 ticked, 0 duplicates, census sums to 209**), all three fixes re-confirmed in the PDF's text layer, the committed PDF re-derived from the committed script, and pages 5/6/12 re-rendered — then reconciled `Ch8_TRACKER.md`, this file and `CHAPTER_STATUS.md` in one operation. **The most dangerous item was not a number but an instruction:** §7 still told the next agent to begin a Pass 3 that had already run and would have re-litigated a closed gate, so it was rewritten into a closure record.

**Previously:** Class 12, Chapter 13 — Biodiversity and Conservation (the last chapter of the Ecology unit) is now **✅ Done — Gate 3 CLOSED**, which also **completes the entire Class 12 Ecology unit** (chapters 11, 12, 13). Pass 3(b) was run as a bidirectional full read: 13/13 source pages read start to finish against the named `# ---- 13.n ----` blocks, per-section reading claim recorded, and **no grep, coverage percentage or similarity score used to clear any row**. Direction 2 found **7 UNINVENTORIED defects — all one family**, NCERT's rhetorical questions and framing/opening sentences (the insect-diversification question; the two "inventory of our biological wealth" questions; the tropics framing sentence; "What exactly is stability…?"; "no direct answers to such naive questions"; the Evil Quartet's opening sentence; the ex-situ "beyond enclosures" sentence). All 7 were fixed and 7 rows added — `F035a`, `F048a`, `F065a`, `F085a`, `F092a`, `F112a`, `F171a` — logged loudly as a **real Pass 1 gap and never back-dated into the freeze**, taking the inventory from 189 to **196 rows**. 0 MISSING · 0 FABRICATED · 0 DRIFTED. The final artefact is an **11-page PDF (35,632 chars, 2 mono images)** with **196/196 rows ticked**, `check_pdf.py` exit 0 (0 fail, 1 inspected benign warn on "photo**synthesis**" in F143), **11/11 pages re-inspected after the reflow**, and a reproducible rebuild (two builds identical, same text SHA-256).

**Closure note — what the last session left open.** The 3(b) audit session ended before it could update the two tracker documents, so the inventory said "Gate 3 CLOSED" while both trackers still said "3(b) not started". Under Gate 3(b) rule 2 that disagreement **is itself the defect**, so the closure session re-derived every claim from the artefacts (fix presence and verbatim wording against the source, the `VERIFICATION FIX` audit trail, a machine re-parse giving 196/196 ticked, a fresh linter run, a byte-comparable rebuild, and an 11/11 visual re-inspection) **before** touching any status file, then reconciled all three in one operation per rule 8.

**Count-derivation note (method; figures re-derived 2026-09-01, Ch19 Gate 3(b) closure session):** the roll-up is **re-derived by counting the ✅ rows, never incremented** — a per-class-section parse of `^\| *[0-9]+\..*\| *✅` over the unit tables returns **17 Done (Class 11: 11, Class 12: 6)** out of 19 + 13 = 32 parsed chapter rows, matching both section footers. Ch19's row flipped from **▶️** to **✅** this session (Gate 3(b) bidirectional full read closed with zero confirmed content defects; the 2 defects found were documentation-only and fixed), which is what moved the parse from 16 to 17; Ch5's / Ch6's / Ch7's / Ch17's **▶️** rows remain deliberately excluded because Gate 1 or Gate 2 closure is not chapter completion. Their frozen or working inventory rows do **not** move this tally. *(This note read "16 Done (Class 11: 10, Class 12: 6)" until the Ch19 closure re-ran the parse; before that "15 Done (Class 11: 9, Class 12: 6)" at Ch18, "14 Done (Class 11: 8, Class 12: 6)" at Ch16, "13 Done (Class 11: 7, Class 12: 6)" at Ch15, "12 Done (Class 11: 6, Class 12: 6)" at Ch14, and "11 Done (Class 11: 6, Class 12: 5)" earlier still. The method was right each time; only the cached output was stale.)*

**Count-correction note (closure session):** the roll-up was **re-derived by counting the ✅ rows, not by incrementing** — `grep -c` returns 11 Done overall, Class 11 6/19 and Class 12 5/13, matching the section footers. The earlier note below records the same lesson from the previous drift.

**Count-correction note (2026-08-22):** this header previously read "9 / 32 · Class 12: 3/13" while the Class 12 section footer read "4 / 13". The footer was correct — Class 12 has four closed chapters (9, 10, 11, 12) — so Ch12's closure had been recorded in its own unit table and section total but never propagated to this header. Corrected at the time to 10 / 32, and now to **11 / 32** with Ch13 genuinely closed.

---

## Class 11

### Unit I — Diversity in the Living World

| Chapter | Status |
|---|---|
| 1. The Living World | ✅ Done |
| 2. Biological Classification | ✅ Done |
| 3. Plant Kingdom | ✅ Done |
| 4. Animal Kingdom | ⬜ Not done |

### Unit II — Structural Organisation in Plants and Animals

| Chapter | Status |
|---|---|
| 5. Morphology of Flowering Plants | ⬜ Not done |
| 6. Anatomy of Flowering Plants | ✅ **Done — ALL GATES (1, 2, 3a, 3b) CLOSED (2026-09-02).** Inventory frozen at 132 rows (`F001`–`F132`); final PDF **7 pp** (the D2 `Xylem`-label commit reflowed the tail onto a 7th page), 5 mono figures; `check_pdf.py` re-run from disk → exit 0, WARN (0 fail / 1 benign warn — check 4 "photo" substring in *photosynthesis*/*photosynthates* rows F106/F126/S3, a verified true-negative), **45/45 labels, 132/132 ticked, 5/5 mono images**. **Gate 3(b):** bidirectional full read of all 8 source pages against the `# ---- N.N ----` blocks — 132/132 COVERED, 0 MISSING/FABRICATED/DRIFTED, **0 UNINVENTORIED**, per-section reading claims + Coverage note in the inventory. **Gate 3(a):** 7/7 pages rendered (150 dpi colour + 1-bit B&W) and inspected individually, **0 layout/orphan/overflow/clipping/squashed-figure defects**, cross-page style consistency confirmed; reproducible rebuild 7 pp · 14,034 chars · 5 imgs (findings `scratch/ch6_gate3a/PASS3A_FINDINGS.md`, folded into the inventory's `## Pass 3(a) / Gate 3 — CLOSED`) |
| 7. Structural Organisation in Animals | ⬜ Not done |

### Unit III — Cell: Structure and Function

| Chapter | Status |
|---|---|
| 8. Cell: The Unit of Life | ✅ Done |
| 9. Biomolecules | ✅ Done |
| 10. Cell Cycle and Cell Division | ✅ Done |

### Unit IV — Plant Physiology

| Chapter | Status |
|---|---|
| 11. Photosynthesis in Higher Plants | ✅ **FULLY COMPLETE — CLOSED; Gates 1, 2, 3(a), 3(b) closed (2026-09-02). A banned-glyph Gate-2 regression was FIXED, not waived (2026-09-02): a real `check_pdf.py` re-run caught check 5 FAILing (exit 1) on a literal `→` rendered from three `&rarr;` entities (~line 1525); all three were replaced with ASCII `->` and the PDF was rebuilt, and the real gate now returns exit 0 — VERDICT WARN (0 fail / 1 benign warn — check 4's "photo"-substring true-negative). Every check is green.** All five sessions ran, each with its own machine-derived count: `1-S` 218 (`F001`–`F218`) · `1-H` 28 (`F219`–`F246`) · `1-O` 20 (`F247`–`F266`) · `1-F` 12 (`F271`–`F282`) · `1-Z` 4 (`F267`–`F270`). Inventory **FROZEN at 282 rows, `F001`–`F282`, contiguous, monotonic, 0 gaps, 0 duplicate IDs, 0 ticked** (Pass 2 does the ticking) · type census machine-derived, 9 values all lowercase: `concept` 128 · `definition` 31 · `number` 28 · `heading` 28 · `process` 26 · `opener` 20 · `caption` 12 · `equation` 5 · `example` 4 = 282 · heading census **19 numbered + 9 unnumbered = 28** (includes the 3 unnumbered Calvin-stage sub-headings and the Melvin Calvin profile heading) · openers **1 intro + 19 numbered = 20** · summary **20 sentences = 15 BODY-PRESENT + 5 SUMMARY-UNIQUE** folded into `F267`–`F270` · exercise gaps **4 of 9 exercises**, each with a planned home · figures **12/12 `Mono: yes` + `Verified: yes`** (six rects re-pinned on the 440 dpi / 5-pt grid; `fig_11_3b` re-rendered as mode `L` after an inherited RGB reference image was rejected) · `check_pdf.py`'s own `_extract_labels` returns **12 figure rows / 116 labels, no doubling, no phantom `Fig #` row**. **Three inherited defects were fixed at this gate, all metadata-only:** the parser first returned **118 labels + a phantom `Fig #` figure** because the matrix's *column header* itself began "Figure labels…" (reworded to the "In-figure labels…" phrasing every closed chapter uses — no label, caption or asset touched); the pre-`1-Z` freeze header described a **158-row** table (`F001`–`F158`, 106/21/15/4/12 blocks, 21 headings, 15 openers, folds at `F155`–`F158`) against a table that actually holds **282 rows**, so every derived count was rewritten from the parse; and the summary table carried **2 page-3 *introduction* sentences** as if they were summary sentences (removed — the facts remain carried by `F016`), which also resolved the 4-vs-5 SUMMARY-UNIQUE tally. **PASS 2 COMPLETE — GATE 2 CLOSED (2026-09-01).** Script `Ch11_PhotosynthesisInHigherPlants.py` + rendered **17 pp A4-portrait PDF** are on disk; `check_pdf.py "notes/class 11/Ch11_PhotosynthesisInHigherPlants"` **exits 0 — VERDICT WARN (0 fail, 1 warn)**. The single WARN is the standing benign check 4 (fires on inventory rows whose text contains *portrait*/*photo*; the Melvin Calvin profile is text-only and no human-subject image is embedded). The one open Gate 2 item — check 7, **270/270 Facts rows unticked** — was closed by reconciling the written PDF against the freeze with a token-coverage aid (`scratch/ch11_gate2/coverage.py`, Pass-2 evidence): **246/270 rows had every content word present; the 24 flagged rows were each read and confirmed benign** (inventory-scaffolding tokens or morphological rewrite variants of facts that ARE present, e.g. "radioisotopic"/"radioisotope"), **no script or asset changed**; all **270 Facts rows + 12 matrix rows now ticked `x`**. Checks 1,2,3,5,6 (116/116 labels),7 (270/270),8 (17/17 A4),9,10 all green. **▶️ PASS 3 STARTED — GATE 3(a) (visual render check) IN PROGRESS** (§6 Pass 3(a): every page rendered + cross-page style consistency); Gate 3(b) bidirectional full read still OPEN, so Ch11 stays out of the Done tally. |
| 12. Respiration in Plants | ✅ **Done — ALL GATES (1, 2, 3a, 3b) CLOSED (2026-09-02; real `check_pdf.py` re-run against the on-disk PDF returns exit 0 at 184 rows / 99 labels / 11 pp).** Figure extraction had already run prior to this session (**6 verified `mode=L` monochrome assets, `Fig 12.1`–`Fig 12.6`**, owner-confirmed correct and left untouched). The existing inventory was **extraction-only** — no Facts table, no heading/opener sweep, and a second label table that would have made `check_pdf.py`'s own `_extract_labels` double-count — so it was **quashed and rebuilt to the §6 Gate 1 standard**, after extracting and reading the full 13-page source text. Rebuilt inventory **FROZEN at 164 rows, `F001`–`F164`, contiguous, 0 gaps, 0 duplicate IDs** = **158 Facts + 6 figure-label matrix rows** · type census (9 values, all lowercase, machine-derived): `fact` 86 · `number` 20 · `heading` 12 · `opener` 10 · `definition` 9 · `term` 9 · `reaction` 6 · `caption` 6 · `label` 6 = 164 · heading census **12** · opener census **10** · `_extract_labels` **imported from `check_pdf.py`** (not replicated) returns **84 labels / 6 label-bearing figures (16/10/15/17/10/16), no doubling, no phantom `Fig #` row** — cross-consistent with the 6 label + 6 caption rows. **GATE 2 CLOSED:** script written linearly from the freeze importing the repo-level `neet_template.py`; **10-page A4-portrait PDF (595×842pt, 1.0 MB), 6 mono figures embedded**; `check_pdf.py` **exit 0, VERDICT WARN (0 fail / 1 inspected-benign warn)** — the WARN is check 4 matching "photo" inside *photosynthesis* fact rows, a verified true-negative. **PASS 3(a) DONE:** whole-chapter visual render check — every page rendered via `pymupdf` (150-dpi colour + 300-dpi 1-bit threshold), **10/10 pages inspected, 0 layout defects**, cross-page style consistency confirmed across H2/H3/table/NOTE/MEMORY-AID/process-flow/figure-box (each sampled ≥3 points, identical). **Pass 3(b) (bidirectional full read) COMPLETE — GATE 3 CLOSED:** the Fig 12.4 label-restoration cross-check was folded into the inventory as 20 verification-fix rows (`F034a`, `F117a`, `F125a`–`F125q`, `F147a`), taking the table 164 → **184 rows** and Fig 12.4 from 17 → 33 catalogued labels; every restored label is present in the running text (real gate check 6: **99/99**) and the added running text reflowed the PDF to **11 pp**. Closure re-confirmed 2026-09-02 by re-running the real `check_pdf.py` against the on-disk PDF → **exit 0 at 184 rows / 99 labels / 11 pp / 0 fail**, so all five Gate 3 conditions hold and **Ch12 is counted in the Done tally.** |
| 13. Plant Growth and Development | ✅ **Done — ALL GATES (1, 2, 3a, 3b) CLOSED (2026-09-02).** 84 Facts frozen (`F001`–`F084`) · 20 headings · 16 openers · 11/11 figures (`mode=L`) · `check_pdf.py` exit 0, WARN (0 fail / 1 benign warn — check 4 "photo" substring in phototropism/photoperiodic rows) · 84/84 rows ticked · 49/49 labels in running text · **Gate 3(a):** 10/10 pages rendered (150 dpi colour + 1-bit B&W) and inspected, 0 layout defects, cross-page style consistency confirmed · **Gate 3(b):** bidirectional full read of all 15 source pages, 84/84 COVERED, 0 MISSING/FABRICATED/DRIFTED, 0 UNINVENTORIED (7 candidates investigated and dismissed as FP-1…FP-7, 1 compression decision CD-1, 1 source-level exercise gap SRC-1 unrelated to the chapter). Full findings in `scratch/ch13_gate3/PASS3A_FINDINGS.md` / `PASS3B_FINDINGS.md`, folded into the inventory's `## Pass 2 / Gate 2`, `## Pass 3(a)`, `## Pass 3(b) / Gate 3` sections |

### Unit V — Human Physiology

| Chapter | Status |
|---|---|
| 14. Breathing and Exchange of Gases | ✅ Done |
| 15. Body Fluids and Circulation | ✅ **Done — ALL GATES CLOSED (2026-08-31)** — normal 3-pass protocol (12 source pages, textbook pp. 193–204) · inventory **FROZEN at 245 rows** (`F001`–`F245`, contiguous, 0 gaps, 0 dupes) = **241 Facts + 4 figure-label matrix**, **245/245 ticked** · 22 heading rows (15 numbered + 7 unnumbered) · 20 openers · 24 summary sentences = 19 BODY-PRESENT + **5 SUMMARY-UNIQUE all folded** (`F071`/`F105`/`F111`/`F166`/`F199`) · 14 exercises / 3 gaps, each closed in its planned home · **4 numbered figures → 4 assets, all `mode=L`, individually verified** · `_extract_labels` returns **43 labels / 4 figure rows, no doubling, no phantom `Fig #` row** · PDF **11 A4 pages / 4 embedded mono images** · `check_pdf.py` **exit 0 — PASS, 0 fail / 0 warn, also green under `--strict`** (9/9 checks) · `verify_inventory.py` green (header **and** artifact claims matched against a fresh parse) · **Gate 3(a): 11/11 pages rendered and inspected**, no layout defect · **Gate 3(b): bidirectional full read** of the 12-page source text layer against the frozen inventory and the 11 rendered pages, per-source-page reading claim recorded — **0 MISSING · 0 FABRICATED · 0 DRIFTED · 0 UNINVENTORIED**, freeze intact, no row added; one factless §15.3 navigational transition considered and **correctly dismissed** (logged so it is not re-raised) · **rebuild reproducible** (11 pp / 30,724 chars / 4 images / text SHA `1a49f8f83c7d142b`, identical) · all four §0.5 deliverables present. Vector-callout figures 15.2–15.4 reach check 6 only via their verbatim callout NOTEs — **do not paraphrase or delete those NOTEs** |
| 16. Excretory Products and their Elimination | ✅ **Done — ALL GATES (1, 2, 3a, 3b) CLOSED (2026-08-31).** Normal 3-pass protocol (12 source pages) · inventory **FROZEN at 178 rows** (`F001`–`F178`, contiguous, 0 gaps, 0 dupes, **172/172 Facts ticked**) = **172 Facts + 6 figure-label matrix** · type census (11 values, all lowercase): concept 84 · process 20 · number 15 · heading 15 · opener 13 · definition 13 · example 8 · caption 6 · disorder 2 · list 1 · question 1 = 172 · heading census **8 numbered + 4 run-in (all in 16.3) + 3 unnumbered = 15** · openers **13** · summary **26 = 21 BODY-PRESENT + 5 SUMMARY-UNIQUE all folded** (corrected at Gate 3(b) per D7; earlier stated 22 = 18 + 4) · 12 exercises / 4 gaps, each with a planned home · **6 numbered figures → 6 assets, all `mode=L`, individually verified; script + PDF built (18 pp, 6/6 embedded)** · `_extract_labels` (imported from `check_pdf.py`) returns **76 labels / 6 figure rows (12/9/11/4/16/24), no doubling, no phantom `Fig #` row** · `check_pdf.py --strict` **exit 0, 0 fail / 0 warn** across all 10 checks · `verify_inventory.py` **PASS, 46/46 checks** · Pass 2 dropped-embed of Fig 16.4 caught and fixed (`# [VERIFICATION FIX]`; linter-invisible — check 6 tests label text, not embedding) · **Gate 3(a):** 18/18 rendered pages inspected (115 dpi + 300 dpi 1-bit), zero layout defects, four observations logged as accepted · **Gate 3(b):** bidirectional full read of the 12-page source against all 178 rows, per-section reading claim, defects D4–D7 + carry-overs CO-1..CO-4 all resolved (adjudication in the inventory's Pass 3(b) findings) · rebuild **content-identical to committed** (18 pp / 51,036 chars / 6 images / text SHA `0d805b5f0256990a`, re-derived this session) · **counted in the Done tally** |
 | 17. Locomotion and Movement | ✅ Done |
| 18. Neural Control and Coordination | ✅ **Done — ALL GATES (1, 2, 3a, 3b) CLOSED (2026-09-01).** Normal 3-pass protocol (9 source pages, textbook pp. 230–238) · inventory **FROZEN at 135 rows** (`F001`–`F135`, contiguous, monotonic, 0 gaps, 0 dupes, 0 case variants, **135/135 ticked**; `check_pdf.py` check 7 scopes to the 131 Facts and reports 131/131) = **131 Facts + 4 figure-label matrix rows** · type census (10 values, all lowercase): concept 55 · definition 21 · process 20 · heading 12 · opener 10 · caption 4 · figure-label 4 · example 4 · prompt 3 · number 2 = 135 · heading census **12** · openers **10** · summary **18 = 14 BODY-PRESENT + 4 SUMMARY-UNIQUE, all 4 folded** · exercise-gap scan **10 exercises / 15 table rows / 5 `GAP`-marked = 3 answered in the PDF's gap block (`9(b)`, `10(b)`, `10(f)`) + 2 discharged by SUMMARY-UNIQUE folds (`8(d)`→18.4.3, `5(c)`→18.4.2)** · **4 numbered figures → 4 assets, all `mode=L`, each opened individually**; census closed against every page image, no unnumbered plate · `_extract_labels` (imported from `check_pdf.py`) returns **35 labels / 4 figure rows (18.1 = 10 · 18.2 = 3 · 18.3 = 9 · 18.4 = 13), no doubling, no phantom `Fig #` row** · PDF **9 A4 upright pages / 2,063 KB / 4 embedded mono images** (12 pp at first Gate 2 close; the Pass 4 exercise-scope correction deleted the seven COVERED answers) · `check_pdf.py` **exit 0 — 0 fail / 1 warn**, smallest glyph 6.0pt, **35/35 labels in running text**, 9/9 A4 portrait, 39 banners 0 orphaned · `verify_inventory.py` **PASS, 105/105** · **Gate 3(a):** 9/9 pages rendered at 115 dpi + 300 dpi 1-bit, **0 layout defects**, O1–O5 accepted (source watermark, Fig 18.2 greys at 1-bit, template table-icon plate, non-NCERT `Recap`/`Ex`/`Appendix` badges); figure-size audit **0 of 4 resized** · **Gate 3(b): bidirectional full read, no grep substitute** — Direction 1 traced **131/131 Facts + 35/35 labels** to their `# ---- N.N ----` block; Direction 2 (never previously run) scored every source sentence and produced **2 flags, both adjudicated non-defects** (`nonmyelinated` spelling variant of `F041`; `Reprint 2026-27` page furniture), **0 UNINVENTORIED rows** · rebuild **content-identical to committed** (9 pp / 21,938 chars / 4 images / text SHA `a1bddee44bc2f2d4`, re-derived 2026-09-01 in the propagation session) · the lone WARN is check 4's `portrait`-substring true negative — the word sits in a **plate-geometry note**, the chapter embeds no person image; **do not "fix" it** · source typos (`passess`, `sagital`, `Schwan cell`, `spiral cord`) transcribed verbatim and asserted by the verifier · **counted in the Done tally** |
| 19. Chemical Coordination and Integration | ✅ **Done — ALL GATES (1, 2, 3a, 3b) CLOSED (2026-09-01; real `check_pdf.py` re-run 2026-09-02 confirms exit 0, 0 fail). PASS 1 COMPLETE — GATE 1 CLOSED (2026-09-01).** All five sessions ran, each with its own machine-derived count: `1-F` (2026-08-30) · `1-S` · `1-H` · `1-O` · `1-Z` (all 2026-09-01). Inventory **FROZEN at 218 rows, `F001`–`F218`, contiguous, monotonic, 0 gaps, 0 dupes, 218/218 ticked** = **211 content Facts + 7 figure-label matrix rows** · type census (9 values, all lowercase): `concept` 108 · `process` 27 · `definition` 19 · `heading` 19 · `opener` 19 · `caption` 12 · `example` 7 · `contents` 4 · `number` 3 = 218 · heading census **14 numbered + 5 unnumbered = 19** · opener census **5 chapter-opener + 14 section-opener = 19**, and `1-O`'s machine check confirms **14/14** sections' `opener` row is the earliest-positioned prose row of its section in the source, with Content Order asserted (0 out-of-order body rows over 189 body-prose rows; 17 folds + 5 captions excluded and printed) · summary **32 = 29 BODY-PRESENT + 3 SUMMARY-UNIQUE, all folded pre-freeze** (`F125`, `F179`, `F191` — this is what took 208 Facts to 211) · exercise-gap scan **9 questions / 39 lettered sub-parts (`3+0+12+5+6+6+3+0+4`) + 4 Q9 Column II options = 43 labelled items / exactly 1 gap** (Q1(a) `Exocrine gland` → planned home §19.1 beside `F013`, ductless-vs-duct-bearing only, Rule 5 respected) · **5 numbered figures → 7 assets** (`19.3`/`19.5` split a/b; `19.4` deliberately ONE combined asset — its panels interleave horizontally and any rectangular split cuts the kidney, labels or connector), all `mode=L` at 440 dpi, each opened individually · `_extract_labels` **imported from `check_pdf.py`** returns **38 labels / 7 figure rows (9/5/3/1/4/8/8), no doubling, no phantom `Fig #` row** · every row **verbatim-verified against the source PDF**: 205 wholly on their stated `Src` page + 6 legitimate cross-page sentences starting on it, **0 misattributed, 0 not found**. **Three stale counts corrected this session, each in every live restatement:** unnumbered headings **4 → 5** (the `NOTE` row `F211` existed while the census omitted it), sub-parts **41 → 39** (the `(s)` in Q6's `Give example(s) of:` inflates a naive scan to 40), and figure labels **35 → 38** (the only live restatement in the file; a Pass 2 script written against 35 would have reached check 6 three labels short). **`1-H` finding carried forward:** the source's heading **font names** are inconsistent (26 `Bookman-Demi` vs 7 `Bookman,Bold`), so a name-based sweep silently drops `19.2.7`, `19.3`, `19.4` and `SUMMARY` — the sweep filters on size; and page 6 is italicised by accident, which Pass 2 must not reproduce. Stale duplicate `_inventory.new.md` deleted. **PASS 2 COMPLETE — GATE 2 CLOSED (2026-09-01, re-verified from disk):** script written linearly from the freeze in Content Order importing the repo-level `neet_template.py` (**0 `ParagraphStyle`, 0 `fontName`** in the chapter script), **18 `# ---- N.N ----` blocks** covering the title, intro, 19.1, 19.2 with all ten glands in NCERT's order, 19.3, 19.4, the `Recap` and the `Appendix`; PDF **14 A4 upright pages / 1,657 KB / 7 embedded mono images / 31,137 chars / text SHA `08d68d03f8d3c05f`**; **218/218 rows ticked**; `check_pdf.py --strict` **exit 0 — PASS, 0 fail / 0 warn** on all 10 checks (**38/38 labels in running text**, smallest glyph 6.0 pt, 7/7 grayscale, 57 banners 0 orphaned, 147 plates 0 collisions), **no warn waived**; rebuild **content-identical** and the committed PDF restored byte-for-byte; all 8 carry-forwards honoured without editing a frozen row. Each of the 7 plates is followed by a verbatim "Read the plate" NOTE — **that is the only route the 38 labels take into the text; do not paraphrase or delete those NOTEs**. The Pass 2 `README.md` was **deleted** as non-conventional (§0.5 has four deliverables; gate records live in the inventory) and its content folded into the inventory's `## Gate 2 record`. **✅ GATE 3 CLOSED (2026-09-01) — Pass 3(a) 14/14 pages inspected (clean); Pass 3(b) bidirectional full read complete with ZERO confirmed content defects** (Dir 1: 10 coverage flags all confirmed false positives — function words / inflections + the intentional `SUMMARY`→`Recap` rename; Dir 2: every source sentence maps to a row incl. 3 SUMMARY folds + section headings). **2 documentation defects found + fixed** — D1 stale Gate 2 fingerprint corrected to `31,149` chars / pymupdf SHA `f4850a48c881f3b3` (the committed PDF was rebuilt by commit `957c1dd` which added +12 chars via two Pass 3(a) string fixes), D2 stale roll-up docs. Gate 2 re-confirmed `--strict` 0 fail / 0 warn, rebuild content-identical. **FULLY COMPLETE — CLOSED; now counted in the Done tally** |

**Class 11 total: 16 / 19 done** — a machine count of the ✅ rows in the Class 11 unit tables above (2026-09-02, re-derived while reconciling Ch11/Ch12/Ch19) returns 16: Unit I 3 (Ch1, Ch2, Ch3), Unit II 1 (Ch6), Unit III 3 (Ch8, Ch9, Ch10), Unit IV 3 (**Ch11**, **Ch12**, Ch13), Unit V 6 (Ch14, Ch15, Ch16, Ch17, Ch18, **Ch19**). This agrees with the header's Class 11: 16/19. Only Ch4, Ch5 and Ch7 remain **Not done**. *(This line read "13 / 19" until this session flipped Ch11, Ch12 and Ch19 to ✅ Done: each was confirmed by re-running the real `check_pdf.py` against its on-disk PDF — all exit 0, 0 fail — and Ch11 required a genuine banned-glyph fix (three `&rarr;` → ASCII `->`, PDF rebuilt) before its gate would pass, not a waiver.)*

---

## Class 12

### Unit VI — Reproduction

| Chapter | Status |
|---|---|
| 1. Sexual Reproduction in Flowering Plants | ⬜ Not done |
| 2. Human Reproduction | ✅ **Done — ALL GATES (1, 2, 3a, 3b) CLOSED (2026-09-02).** Inventory **FROZEN at 220 rows, `F001`–`F220`, contiguous, 0 gaps, 0 duplicate IDs** = 1 title + 8 openers + 10 headings (7 numbered + 3 unnumbered) + 14 captions + 187 content rows · **14 figure-label matrix rows** (2.1a/2.1b, 2.2, 2.3a/2.3b, 2.4–2.12), `_extract_labels` (imported from `check_pdf.py`) returns **131 labels / 14 label-bearing figures, no doubling, no phantom `Fig #` row** · 3 SUMMARY-UNIQUE facts folded (`F076`, `F077`, `F204`); 2 exercise gaps (Q20 twins, Q21 litter) given a planned home · **GATE 2 CLOSED:** script written linearly from the freeze importing `neet_template.py`; PDF **13 A4 portrait pages, 3.86 MB, 14 embedded mono images**; `check_pdf.py` **exit 0 — VERDICT PASS, 0 fail / 0 warn, also green under `--strict`** across all 10 checks (smallest glyph 6.0pt · 131/131 labels in running text · 220/220 rows ticked · 48 banners no orphan · 106 plates no collision) · **Gate 3(a):** all 13 pages rendered at 150 dpi (plus 300 dpi re-crops) and inspected; **2 defects found and fixed** — a design-system table badge mis-attached to the §2.1 heading (moved to §2.2/§2.6, the two sections that actually carry a `data_table`) and baked-in NCERT scan artifacts (watermark + a grey margin bar/dash) on `fig_2_12.png` white-washed in place with a zero-artwork-pixel check first; per user scope decision, the same faint watermark on 8 other figure assets was left intact as faithful to source · linter re-confirmed green after both fixes · **Gate 3(b):** true bidirectional full read across source → inventory → script (not a grep/coverage screen) — Direction 1 confirmed nothing in the NCERT source is silently dropped, Direction 2 confirmed the chapter invents no fact absent from source; high-risk facts reconciled verbatim (23-chromosome secondary spermatocytes/spermatids, spermiogenesis→spermiation, Sertoli/Leydig roles, GnRH→LH surge, Graafian follicle→corpus luteum→progesterone, blastocyst→trophoblast/inner cell mass→chorionic villi, 9-month pregnancy, colostrum) · **0 confirmed defects; no script or asset change required at 3(b)**, so the green Gate 2 above already matches the final delivered PDF. Full findings in `notes/class 12/Ch2_HumanReproduction/Ch2_TRACKER.md`; **hard stop after Gate 3(b) per instruction — no further passes started** |
| 3. Reproductive Health | ⬜ Not done |

### Unit VII — Genetics and Evolution

| Chapter | Status |
|---|---|
| 4. Principles of Inheritance and Variation | ⬜ Not done |
| 5. Molecular Basis of Inheritance | ▶️ **Gates 1 + 2 CLOSED, Gate 3 OPEN** — 646 frozen rows (`F001..F646`), **646/646 ticked**, 136 figure-label rows, **18/18 verified mono assets** (16 figure numbers; `5.4` splits a/b, plus the unnumbered p4 central-dogma plate); `check_pdf.py --strict` **PASS 0/0**; PDF 30 pp / 17 embedded images; **Gate 3a COMPLETE** — 30/30 pages + 18/18 assets inspected, defect list fully dispositioned: **badges FIXED** (`5.2a`/`5.2b`/`5.9a`, call sites only), **Quick Recap bullets FIXED** (12 inline `&bull;` normalized to hanging `<bullet>`; the "21 literal glyphs" claim was re-derived as false — 0 typed U+2022), watermark + baked-in double borders (`5.4b`/`5.6`/`5.7`/`5.9`) **ACCEPTED, do not re-raise**; strict re-run after fixes still **PASS 0/0**. **Gate 3b (bidirectional full read of 646 rows) NOT STARTED — the only work left** |
| 6. Evolution | ▶️ **Pass 1 partial — session `1-F` (figures) COMPLETE (2026-08-26); `1-S`/`1-H`/`1-O`/`1-Z` NOT STARTED; GATE 1 OPEN** — inventory is a **working file, not frozen**: 13 figure-label rows, no Facts table, no IDs (`—`, assigned by `1-Z`), **0 ticked by design**. **11 numbered figures → 13 assets** (`6.3`/`6.4` split a/b; `6.8` kept as ONE asset — three panels share a frame and the arrows carry the meaning); **no unnumbered plate**, census taken from all 17 page images; **no scientist portrait in the chapter**, so §4.4's photo hard-no has no manifest row. All 13 assets 300 dpi, `mode=L`, opened individually at full size. `_extract_labels` (imported from `check_pdf.py`) returns **116 labels / 9 label-bearing figures / no doubling / no phantom `Fig #`**; the 4 unlabelled rows contribute 0 by design. **The source is a 100% scanned raster — 0 text-layer words and 0 drawings on all 17 pages — so skill audit checks A and B are vacuous**, and the numeric gate is `audit_figures.py`'s raster A' slack + B' edge-band probe with **`BAND_TOL` 12, not 40** (two real clipped-label defects sat under 40): re-run from disk, **GATE PASSED 13/13, all edge ink named**. **8 rects re-pinned, 4 of them caught by eye rather than by the gate** (`fig_6_1` had shipped 5 lines of body prose; `fig_6_3a`/`6_3b` had lost whole panels; `fig_6_4b` sliced its "(b)" label; `fig_6_10` read "arboniferous"). Also fixed in-session: all 13 assets had shipped `mode=RGB` because the first script autocontrasted the RGB pixmap. **Not done: 4 of 5 Pass 1 sessions, the freeze, script, PDF, Gates 1-3** — excluded from the Done tally |

### Unit VIII — Biology in Human Welfare

| Chapter | Status |
|---|---|
| 7. Human Health and Disease | ▶️ **PASS 2 COMPLETE — GATE 2 CLOSED (2026-08-23). Gate 3 OPEN.** Script (1527 lines) + 20-page PDF exist; `check_pdf.py` exits **0 — 0 FAIL, 1 WARN** (check 4, adjudicated legitimate: see Gate 2 record below). **All 335 Facts rows ticked** (`x`, the repo-wide ticked marker used by all six delivered chapters); the 11 label-matrix rows sit outside check 7's `## Facts` scope **by design** and are covered by check 6 (21/21 labels in running text). Still **not** countable in the done tally — Gate 3 (per-page visual + bidirectional full read) has not run. All five mandatory Pass 1 sessions ran, each reporting its own machine-derived count: **`1-S` 276 · `1-H` 29 · `1-O` 28 · `1-F` 11 matrix rows · `1-Z` 2**. Inventory **FROZEN at 346 rows** (`F001..F346`, contiguous, 0 gaps, 0 duplicates, 0 ticked) = **276 Facts + 29 heading + 28 opener + 2 summary-unique + 11 figure-label matrix**; `276+29+28+2+11 = 346`, equal to the highest ID and the parsed row count. **29 heading rows** (16 numbered + 13 unnumbered, the list-derived census) · **28 opener rows** (`F306`-`F333`; the roll-call is `29 - 2 + 1 = 28`, the two un-paired heads being the title plate and EXERCISES, plus the unheaded chapter-intro opener `F306`) · `Type` vocabulary **17 values, all lowercase** (no Ch13 casing split) · **18 summary sentences = 16 BODY-PRESENT + 2 SUMMARY-UNIQUE**, both folded pre-freeze into `F334`/`F335` · **17 exercises scanned, 2 gaps** each with a planned home (Ex 5 "DNA vaccines" — genuine, 0 body hits, goes to a *Terms used in the exercises* appendix, **not** the main text, since Rule 5 forbids importing the mechanism from Ch10; Ex 4 "water-borne" — terminology only, NCERT writes solely "air-borne"/"vector-borne"). **Re-derived independently at closure** (`scratch/ch7_1z/derive_counts.py`, exit 0, 10/10 assertions green): all counts re-parsed from the finished file, and the real `_extract_labels` **imported from `check_pdf.py`** returns **21 labels / 4 labelled figures** (7.1→7, 7.4→3, 7.5→3, 7.6→8), **no doubling, no phantom `Fig #` row** — the Ch12 trap was executed and **did not fire**. Source-side spot-checks re-run at closure: "cholera" **1 hit, p21 summary only** (0 in body, so `F335` holds); the body defines health as "physical, mental and social" (p4) while the summary adds **"psychological"** (p21), which is exactly the qualifier `F334` records; "DNA vaccine"/"suitable gene"/"water-borne" occur **only on p22** (exercises). **Figures: 11/11 done and verified.** 11 numbered figures 7.1–7.11 on artwork pages 6/7/9/12/13/16/17 → **11 assets, all `mode=L`**; denominator is 11 (no split sub-plates, no unnumbered bonus plate). Rects hand-pinned in `extract_figures.py` off a 20 pt grid; re-running it regenerates all 11 PNGs **byte-identical** to the committed ones. **Five-part crop gate** (`scratch/ch7_figs/audit.py`, rebuilt this session — the first sitting never committed its script): **A word-grazing · B drawings-extent · B2 raster-extent · B3 raster-TILE union · C dark border ink (<110) · C2 light border ink (<205)**. **Check A is vacuous for ALL 11 assets** — zero text-layer words fall in any rect, every callout is artwork, so A is *no evidence at all* here and must never be cited as if it were. **C2 was required** because fig 7.10's leaf is mid-green (luma ~177) in a pale grey frame, sitting entirely above the dark threshold: C passed while the crop was clipping the panel border, so `fig_7_10` was **re-pinned** (`348,224,460,366`→`350,219,456,364`) and its top band is now clean. **B3 was added this session and is not optional:** figs **7.8 and 7.11** are stipple artwork of ~1800 and ~4900 sub-pixel raster tiles, invisible to both B (0 drawings) and B2 (3 pt size floor) — before B3 those two plates were passing **vacuously with no mechanical edge check at all**. 3 C2 hits explained by coordinate (page-number tab, two corner motifs); `fig_7_5`'s L24.8 raster overflow explained as the raster's own white margin (bbox from x=29.2 but **first inked column x=57.9**, rect edge x=54); `fig_7_10`'s B overflow explained as gradient shapes killed by the panel's clip path. **All 11 assets opened individually at full size** (not just contact-sheeted). **Label matrix: 21 labels across 4 labelled assets**, harvested by *opening* the assets — a text sweep returns 0 labels; the 7 unlabelled rows are worded to avoid `_extract_labels`' semicolon fallback and contribute 0 **by design** (verified by running the real parser: **exactly 21, no phantoms**). 4 non-assets excluded by rule: the p2 Swaminathan **portrait** (§4.4 hard no), the p3 opener plate/QR, and the p21/p22 wheat motifs. **The two 1-F carry-overs are now CLOSED:** the process-arrow sentences in figs 7.1/7.6 have real Facts rows — `1-S` wrote **18 `figure-text` rows** (`F045`-`F052` malaria, `F171`/`F172` HIV, etc.) — and `1-Z` assigned the matrix IDs **`F336`-`F346`** (they were `—`). `1-Z` also **reset those 11 rows' `Ticked` column from `x` to empty**: `x` means "verified present in the generated PDF", no PDF exists, and leaving them ticked would have fed check 7 a false green. **Live Pass 2 obligations** (full numbered list in the inventory's carry-over section): check 6 needs the 21 labels written into running text as each figure is placed — figs 7.6 (8) and 7.1 (7) are the Ch9 defect-5/6 profile; keep the Widal-test definition adjacent to the Typhoid Mary anecdote (NCERT splits them); write `H2L2` flat and **"alpha-interferon"** not `α-interferon` — **verified against the linter this session: `α` IS in its banned GREEK set, while `°` (`F023`, "39° to 40°C") is NOT banned and needs no workaround.** |
| 8. Microbes in Human Welfare | ✅ **Done — ALL THREE PASSES COMPLETE, GATES 1 + 2 + 3 CLOSED (2026-08-23)** — `Ch8_MicrobesInHumanWelfare.py` (51.6 KB) + **12-page A4 PDF** written linearly from the frozen inventory · `check_pdf.py` **exit 0, 0 fail / 1 inspected WARN** · **200/200 Facts rows ticked** · **17/17 in-figure labels in running text** · 9 embedded images all mono · smallest text 6.0pt · 88 badge plates clear · 37 banner headings all followed by content · no banned glyphs · the lone WARN is **check 4's portrait heuristic** matching the word "photograph" in `F050`/`F203`–`F206` and the four `raster photograph` manifest rows — **inspected and accepted**: the check exists to stop a *scientist portrait* being embedded, and figures 8.4/8.6/8.7 contain **no people at all** while 8.5 is an **industrial plant photograph with two incidental workers**, kept embedded **by explicit user decision** because it is NCERT Figure 8.5 itself and dropping it would break the 9-asset denominator. **Do not "fix" this WARN by removing `fig_8_5`.** Inventory **FROZEN at 209 rows** (`F001..F207` contiguous + the two Pass 3 additions `F055a`/`F085a`, 0 duplicates) = **200 Facts + 9 figure-label matrix rows** (it was 207 = 198 + 9 until Pass 3); **15 heading rows** (9 numbered + 6 unnumbered) · **14 opener rows** · **8 caption rows** · `Type` vocabulary 10 values, all lowercase · `_extract_labels` parses **17 labels across 5 parsed figure rows** — 9 matrix rows, the four `No in-figure labels` rows (`F203`-`F206`) contributing 0 **by design** — no doubling, no phantom `Fig #` row · 18 summary sentences (16 BODY-PRESENT + 2 SUMMARY-UNIQUE, both folded into `F195`/`F196`) · 4 exercise-gap terms each with a planned home · all 5 sessions ran with machine-derived counts (`1-F`/`1-S`/`1-H`/`1-O`/`1-Z`). **All 200 Facts rows TICKED (the 9 figure-label matrix rows are not Facts rows and are not counted by check 7); the script and PDF both exist, so Gate 2 was judged green — and Gate 2 was then re-verified from disk at the closure session, after the Pass 3 fixes, rather than trusted from the handoff.** Re-validation fixed one **count restatement** (the inventory twice described the parse as "9 figure rows" where the parser returns 5); no frozen row was touched and no count changed. Figure sweep: **9/9 verified mono assets** (`mode=L`, 300 dpi) covering **8 numbered figures**, 8.2 split into `8_2a` (panels a+b) + `8_2c`; no unnumbered plates, so the denominator is 9 · rects hand-pinned in `extract_figures.py`, each with a comment recording what pinned it · four-part crop gate passed (**A word-grazing · B drawings-extent · B2 raster-extent · C border ink**) — **B2 was added this chapter** because check B reports `no drawings` for the four photographic plates 8.4–8.7 and would have left them mechanically unchecked · check A is **vacuous for 5 of 9 assets** (0 words in rect; every callout is artwork, not text) · **2 rects re-pinned after the gate caught real clipping**: `fig_8_1` (`82,78,…`���`56,76,…`, flagellum sliced off panel c) and `fig_8_3` (`148,…`→`93,…`, petri dish a clipped) · 4 gate hits explained and accepted in writing (page-header motif, neighbouring 8.2c leader artwork, caption tint panels) · all 9 PNGs eyeballed on a fresh contact sheet · figure manifest + caption-verbatim check + figure-label matrix written; 9/9 re-confirmed `mode=L` on disk at re-validation. **GATE 3 CLOSED.** 3(a) done — 12/12 PDF pages and 9/9 assets opened and inspected, no clipped badges, orphaned captions or collisions. 3(b) done in **both** directions: direction 1 confirmed all 207 then-existing rows present *and* faithful in the PDF; direction 2 read **source pp. 3–11 start to finish under an explicit grep prohibition**, with a per-section reading claim recorded and **no grep, coverage percentage or similarity score used to clear any row**. It found **3 defects — 2 UNINVENTORIED + 1 DRIFTED, 0 MISSING, 0 FABRICATED**: the household curd/idli/dosa framing sentence (row **`F055a`**), NCERT's sewage-disposal rhetorical question and its "you can understand why" nudge (row **`F085a`**), and an Exercise-Q11 lead-in that claimed the three samples were "collected before water treatment" while the table's own row calls one a **secondary effluent** — rewritten to match the data. The two additions are logged as a **real Pass 1 gap and were never back-dated into the freeze**, taking the inventory 207 → **209 rows**. They are the **same family Ch13 hit 7 times** — NCERT's rhetorical questions and framing sentences, which read as connective tissue and get skipped by a sweep hunting facts. After the fixes: `check_pdf.py` **exit 0 / 0 fail / 1 accepted WARN**, **200/200 ticked**, all three fixes re-confirmed in the rebuilt PDF's text layer, and pages **5, 6 and 12** — the three that reflowed — re-rendered and re-inspected. Rebuilt twice more: **same 12 pages and same text-layer SHA-256**, so the build is **content-deterministic, not byte-deterministic** (embedded timestamps/IDs differ, so a rebuild always shows as a modified binary in `git status` — expected, not a defect). |

### Unit IX — Biotechnology

| Chapter | Status |
|---|---|
| 9. Biotechnology: Principles and Processes | ✅ Done |
| 10. Biotechnology and its Applications | ✅ Done |

### Unit X — Ecology

| Chapter | Status |
|---|---|
| 11. Organisms and Populations | ✅ Done |
| 12. Ecosystem | ✅ Done |
| 13. Biodiversity and Conservation | ✅ Done |

**Class 12 total: 6 / 13 done** — Units IX and X are both complete

**Count-correction note (2026-08-24, Ch8 Gate 1 re-derivation session):** this footer previously read "5 / 13" while the document header read "Class 12: 6/13". Counting the ✅ rows in the table above returns **6** (Ch8, Ch9, Ch10, Ch11, Ch12, Ch13), so the header was right and this footer was a stale increment — Ch8's closure was recorded in the header and in its own row but never propagated here. Re-derived by machine (`grep`/parse of the ✅ rows per class section), never incremented: Class 11 **6 / 19**, Class 12 **6 / 13**, total **12 / 32**. *(Those figures are the 2026-08-24 state and are kept as history; the current machine re-derivation after Ch14's 2026-08-31 closure is **Class 11 7 / 19**, Class 12 6 / 13, total **13 / 32** — see the header and the count-derivation note above. Class 12 is unchanged.)*
